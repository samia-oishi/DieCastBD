# DiecastBD — Project Plan

Living architecture document for DiecastBD v1.0 — a premium, collector-focused diecast e-commerce platform for Bangladesh (Hot Wheels Premium + MINI GT now, more brands later). This file reflects the **current, as-built state** of the project, kept current as phases complete. For a chronological account of what happened and when, see `docs/log.md`.

---

## 1. Business Context

- **Brand positioning:** premium/luxury/minimal — "Apple, Porsche," not a toy shop. Dark theme, large product photography, restrained lime-green accent.
- **Current brands:** Hot Wheels Premium, MINI GT. Planned future brands: Tarmac Works, Kaido House, Inno64, Pop Race, Tomica Limited Vintage, Auto World.
- **Market:** Bangladesh. Currency: BDT (৳). Language: English only for v1.0.
- **Source of truth for inventory:** `docs/Inventory.md` (32 real SKUs, July 2026 purchase batch) — imported via `backend/src/seeds/`, never hand-typed into the frontend.
- **Payment:** Cash on Delivery (COD), plus manual bKash "Send Money" (System 4, post-launch) — customer sends payment to an admin-configured merchant number/QR outside the app and types the resulting Transaction ID at checkout; there's no live bKash merchant API/gateway integration, an admin verifies manually via `bkashTransactionId` on the order. A real gateway integration remains deferred until real bKash sandbox merchant credentials are available.

---

## 2. Tech Stack (as implemented)

**Frontend:** React 19, Vite, `react-router` v8 (no `react-router-dom`), Tailwind CSS v4, shadcn/ui (primary component layer, Radix-based) + DaisyUI (scoped to Rating/Steps/Loading only), Framer Motion, Embla Carousel (+ autoplay plugin), React Hook Form, Zod, Zustand (+ persist middleware for guest cart / recently-viewed), TanStack Query, Axios, React Hot Toast, React Helmet Async, Lucide React, Tiptap (`@tiptap/react` + `@tiptap/starter-kit`, System 9 — the admin CMS page-content editor, isolated to that one code-split admin route). Testing: Vitest + React Testing Library + jsdom (Phase 12).

**Backend:** Node.js (ESM), Express 5, MongoDB Atlas, Mongoose 9, Firebase Admin SDK, `jsonwebtoken`, Multer, Cloudinary SDK, Zod, Helmet, `express-rate-limit`, Morgan, CORS, Compression, Dotenv, `cookie-parser`, Resend, `node-cron` (nightly analytics rollup + hourly stale-reservation release), `bcrypt` (unused — Firebase owns credentials, no passwords stored locally), `sanitize-html` (System 9 — strips admin-authored CMS page HTML down to an explicit tag allow-list server-side, on save). Testing: Vitest (Phase 12).

**Notable version-driven deviations from the original plan** (see §7 for details): custom NoSQL-sanitize middleware (Express 5 breaks `express-mongo-sanitize`), custom `req.query` handling in validation middleware (Express 5's `req.query` is an uncached getter), `returnDocument: "after"` instead of deprecated Mongoose `new: true`.

---

## 3. Repository Structure

```
DieCastBD/
├── docs/                       # Inventory.md (source data), plan.md (this file), log.md (changelog)
├── frontend/                   # React 19 + Vite SPA — see structure below
└── backend/                    # Express API — see structure below
```

### Frontend (`frontend/src/`)
```
app/                    # router.jsx, providers/ (AppProviders), layouts/ (Public, Auth, Admin)
features/                # one folder per domain — each owns api/, components/, schemas/, hooks/
  auth/ account/ home/ products/ categories/ brands/ cart/ checkout/ orders/
  wishlist/ addresses/ newsletter/ settings/ pages/
  admin/{dashboard,products,brands,categories,catalog,orders,analytics,customers,coupons,inventory,settings,reports,newsletter,pages}/
  about-contact/          # AboutPage, ContactPage, FaqPage — deliberately NOT migrated onto pages/
components/
  ui/                    # shadcn-generated primitives (Accordion added Phase 11 for the FAQ page)
  shared/                # cross-feature composed components (ProductCard, Container,
                          #   ProductCarouselSection, Breadcrumb, Footer, Pagination, SocialIcons,
                          #   OrderStatusStepper (DaisyUI steps, shared customer+admin),
                          #   AnnouncementBar...)
stores/                  # zustand: authStore, cartStore (guest, persisted), recentlyViewedStore (persisted)
lib/                     # axios instance (+ refresh interceptor), queryClient, firebase.js, utils.js
hooks/                   # useDebounce...
constants/               # routes.js (ROUTES, ROLES)
assets/logo/             # brand logo (logo.jpg)
```

### Backend (`backend/src/`)
```
config/                  # env.js (Zod-validated, fail-fast), db.js, firebaseAdmin.js, cloudinary.js,
                          #   constants.js (LOW_STOCK_THRESHOLD — shared by analytics + inventory)
modules/                 # one folder per domain, each: *.model.js, *.controller.js, *.service.js,
                          #   *.routes.js, *.validation.js
  auth/ users/ brands/ categories/ products/ settings/ newsletter/ wishlists/ cart/
  addresses/ coupons/ orders/ inventoryLogs/ auditLogs/ analytics/ inventory/ contact/ pages/
middlewares/             # authenticate, optionalAuthenticate (guest-or-logged-in routes),
                          #   authorize(role), validate(schema), errorHandler,
                          #   rateLimiters, upload (Multer), auditLog, sanitize (custom)
utils/                   # apiResponse, apiError, asyncHandler, slugify, cloudinaryUpload,
                          #   jwt, cookies, parseDuration, generateOrderNumber
emails/                  # resendClient.js, orderConfirmation.js (HTML template + send)
jobs/                    # analyticsRollup.cron.js (nightly), releaseReservedStock.cron.js (hourly),
                          #   scheduler.js (registers both via node-cron, started from server.js)
seeds/                   # index.js (idempotent) + data/ (catalog, settings, coupons, pages)
routes/index.js          # mounts every module router under /api/v1
```

---

## 4. Database Schema (MongoDB / Mongoose, as built)

Naming: `camelCase` fields, `PascalCase` model names, plural collections.

| Model | Purpose | Key fields | Notes |
|---|---|---|---|
| **User** | Account identity | `firebaseUid` (unique, **sparse** — optional since a guest-checkout User has none), `email` (unique, **sparse** — optional for guests), `phone` (indexed, not unique), `name`, `photoURL`, `role` (customer\|staff\|admin), `isActive`, `isGuest`, `lastLoginAt`, `tokenVersion` | No password — Firebase owns credentials. Role auto-assigned via `ADMIN_EMAILS` env on first *verified* sign-in. `tokenVersion` (Phase 12) is stamped into every JWT as `tv`; bumping it on deactivation/role-change instantly invalidates the account's outstanding refresh tokens. `isGuest` (added for Guest Checkout, post-launch systems) marks a User doc created directly from checkout with no Firebase identity — matched/linked by email-then-phone on repeat guest orders, never by a hard unique phone constraint (see §7). |
| **Brand** | Hot Wheels Premium, MINI GT, ... | `name`, `slug` (unique), `logo`, `isActive`, `sortOrder` | |
| **Category** | Premium Singles, Multi-Packs, Accessories | `name`, `slug` (unique), `parentCategory` (self-ref), `image`, `isActive`, `sortOrder` | Hot Wheels' own sub-lines (Car Culture, F1 Gold Label, etc.) live on `Product.series` (free text), not as categories. |
| **Product** | Catalog item | `sku` (unique), `slug` (unique), `title`, `brand` (ref), `category` (ref array), `manufacturer`, `series`, `modelNumber`, `scale`, `material`, `color`, `description`, `features[]`, `specifications` (Map), `thumbnail`, `gallery[]`, `price`, `salePrice`, `costPrice` (`select:false` — never sent to storefront), `stock`, `reservedStock`, `status` (draft\|active\|archived), `isFeatured`, `isHeroProduct`, `isNewArrival`, `isPreOrder`, `preOrderStartDate`, `preOrderEndDate`, `paymentOptions[]` (cod\|deliveryOnly\|partialAdvance\|full, default `["cod","full"]`), `advancePaymentPercent` (1-100, nullable), `tags[]`, `seo`, `isDeleted` | `availableStock` = `stock - reservedStock`, virtual, never stored. `profitMargin` (System 5, post-launch) = `round((effectivePrice - costPrice) / effectivePrice * 100)` where `effectivePrice` is `salePrice` when the item is on sale, else `price` — so a discounted item's margin reflects what it's actually selling for. `null` when `costPrice` isn't set/selected — resolves only on admin queries (`.select("+costPrice")`), harmlessly serializes as `null` on the public storefront rather than leaking cost data. `isPreOrderActive` (decision #58) is a virtual — `isPreOrder && (!preOrderEndDate || preOrderEndDate >= now)` — auto-expiring so a pre-order badge disappears on its own once `preOrderEndDate` passes, no admin action needed; badges/CTAs key off this, not the raw `isPreOrder` flag. `paymentOptions`/`advancePaymentPercent` (decision #59) default to today's only behavior for every existing product; `cod` and `partialAdvance` are mutually exclusive, enforced by a Zod `.refine()` on both create/update schemas (frontend mirrors the same refine). Text index on title/description/tags. |
| **Cart** | One per logged-in user | `user` (unique ref), `items[]` {product, qty, priceSnapshot} | *Not in the original spec's collection list — added because guest carts are client-only (Zustand/localStorage) and logged-in carts need a server home independent of the User doc (avoids write contention on auth-critical data).* Totals computed from **live** product price at read time, not the snapshot (snapshot is a "price changed" hint only). |
| **Wishlist** | Saved products | `user` (ref), `product` (ref) — compound unique | |
| **Address** | Saved shipping addresses | `user` (ref), `label`, `recipientName`, `phone`, `addressLine1/2`, `city`, `district`, `postalCode`, `isDefault` | First address auto-defaults; deleting the default promotes the next most recent. |
| **Order** | Placed order | `orderNumber` (unique, `DBD-YYYYMMDD-XXXXXX`), `user` (**always set** — guest orders get a find-or-create `User` too, see §7), `items[]` (**embedded snapshot** — title/sku/price/thumbnail/qty at purchase time, immune to later product edits), `shippingAddress` (embedded snapshot), `phone`, `deliveryNote`, `coupon` (ref, nullable) + `couponCode` (snapshotted), `subtotal`, `discount`, `shippingFee`, `total`, `paymentMethod` (cod\|bkash\|banglaqr), `paymentStatus`, `bkashTransactionId`, `banglaQrReference`, `paymentOption` (cod\|deliveryOnly\|partialAdvance\|full — decision #61), `advancePaymentPercent`, `amountPaid`, `amountDue` (hard invariant `amountPaid + amountDue === total`), `status` (pending\|confirmed\|packed\|shipped\|delivered\|cancelled\|refunded), `statusHistory[]` {status, note, changedBy, at}, `trackingNumber`, `courierName` | Schema unchanged by Guest Checkout — no new fields; only *how* an order gets created gained a second path (`createOrderFromItems`, alongside the original `createOrderFromCart`). *`OrderItems` deliberately not a separate collection — see §7.* Tracking fields set when status transitions to `shipped`; both customer and admin order-detail pages render a shared `OrderStatusStepper`. `paymentOption`/`advancePaymentPercent`/`amountPaid`/`amountDue` (Phase 4 of the pre-order/payment-options feature, decision #61) are computed once at order-creation time by `paymentPlan.service.js` and snapshotted — `paymentOption` is orthogonal to `paymentMethod`, which stays the payment *channel* (cod/bkash/banglaqr) rather than the business option chosen. Each of the 4 new fields carries a Mongoose function default (reading `this.paymentMethod`/`this.total`) so pre-Phase-4 historical orders retroactively satisfy the invariant on read with no migration script — see decision #61. |
| **AnalyticsDaily** | Nightly business rollup | `date` (unique, `YYYY-MM-DD`), `revenue`, `ordersCount`, `newCustomers`, `topProducts[]` {product, title, unitsSold} (top 5), `lowStockCount` | Computed by `computeDailyRollup()` from live `Orders`/`Users`/`Products`; upserted nightly at 00:05 UTC for the *previous* day (today is still in progress). `GET /admin/analytics/summary` computes today's rollup live (not from the stored table, which won't have today's row yet) plus a live last-7-days total. `LOW_STOCK_THRESHOLD = 2` — deliberately low because collector diecast is seeded with naturally thin per-SKU stock (1-4 units), so a >10-unit threshold would flag nearly the whole catalog. |
| **Coupon** | Discount codes | `code` (unique), `type` (percentage\|fixed), `value`, `minOrderValue`, `maxDiscount`, `usageLimit`, `usedCount`, `expiresAt`, `isActive` | Admin CRUD UI deferred to Phase 10; model + validate endpoint + 2 seeded test coupons exist now. |
| **InventoryLog** | Stock movement audit trail | `product` (ref), `type` (restock\|sale\|reservation\|release\|adjustment), `quantityChange` (signed), `reason`, `referenceOrder`, `performedBy` | Written automatically at every reserve/commit/release transition (see §7 stock lifecycle). |
| **AuditLog** | Admin mutation trail | `actor`, `action`, `entityType`, `entityId`, `before`, `after`, `ip` | Written by a middleware wrapping every admin mutation route — not hand-called per controller, so it can't be forgotten. |
| **Settings** | Singleton CMS content | `heroBanner[]`, `announcementBar`, `whyChooseUs[]`, `collectorPromise`, `testimonials[]`, `faqs[]`, `socialLinks`, `contactInfo`, `shippingZones[]` {name, fee, eta, requiresPrepay}, `freeShippingThreshold`, `bkashConfig` {merchantNumber, qrImage}, `homepageSections` {hero, collectorPicks, featuredProducts, brandsStrip, newArrivals, whyChooseUs, collectorPromise, testimonials, instagramFeed, newsletter — each `{enabled}`; `hero` also carries `autoplay`/`autoplayInterval`}, `navigation` {headerLinks[], footerLinks[] — each `{label, url}`, no separate `order` field, array position is the order — plus `footerText`}, `seoDefaults` | Testimonials/social/contact/faqs deliberately seeded empty — no fabricated content. Full admin editor UI (Phase 10, `faqs[]` added Phase 11). `shippingZones[]` (post-launch System 2) replaced the old flat `shippingFee` — an admin-editable list of named delivery zones (e.g. "Inside Dhaka", "Outside Dhaka"), each with its own flat fee, edited via the same `useFieldArray` pattern as `faqs[]`. `requiresPrepay` (decision #60) is a per-zone boolean, not a hardcoded "Dhaka" string match — zone names are admin-renamable free text, so per-zone is the only robust option; default `false`, dormant until Phase 4. `bkashConfig` (System 4) holds the manual-payment merchant number + QR image shown at checkout, seeded empty (no real merchant details to seed with). `homepageSections` (System 6) is a per-section show/hide map matching every section `HomePage.jsx` actually renders today, all defaulting to `enabled: true` so the field's absence/a fresh document never hides anything. `navigation` (System 10) replaced `PublicLayout.jsx`'s hardcoded "Shop" link and `Footer.jsx`'s hardcoded `FOOTER_LINKS`/tagline — seeded (and live-migrated) to match exactly what was already hardcoded, so nothing changed visually on deploy; both consumers fall back to the pre-System-10 hardcoded values if the arrays are ever empty. |
| **NewsletterSubscriber** | Email capture | `email` (unique), `subscribedAt`, `isActive` | |
| **Page** | Static CMS content (System 9, post-launch) | `slug` (unique), `title`, `content` (HTML, sanitized server-side on save), `seo` {title, description, canonicalUrl}, `isPublished` | New collection (not a `Settings` extension — `Settings` is a singleton, Pages is inherently a collection). Powers Terms/Privacy/Refund/Shipping-Policy; About/Contact/FAQ deliberately stayed on their existing hardcoded components, not migrated onto this. Seeded with all 4 titles/slugs but empty `content` and `isPublished: false` — no fabricated legal text, ever. Public `GET /pages/:slug` 404s on anything not `isPublished`. |

---

## 5. API Reference (as built)

Base path `/api/v1`. Envelope: `{ success, data, meta? }` / `{ success: false, message, errors? }`. Every write route: `authenticate` → `authorize(role)` → `validate(schema)` → controller → (admin mutations) `auditLog`.

| Domain | Endpoints |
|---|---|
| Auth | `POST /auth/session`, `POST /auth/refresh`, `POST /auth/logout`, `GET /auth/me` |
| Users (self) | `PATCH /users/me`, `DELETE /users/me` (soft-deactivate) · admin: `GET /admin/users` (search/filter by role/isActive), `GET /admin/users/:id`, `PATCH /admin/users/:id` (name/phone/isActive), `PATCH /admin/users/:id/role` (**admin-only**, not staff — guards: can't change own role, can't demote the last remaining admin) |
| Brands | `GET /brands` · admin: `GET/POST /admin/brands`, `PATCH/DELETE /admin/brands/:id`, `POST /admin/brands/:id/logo` |
| Categories | `GET /categories` · admin: same CRUD shape as Brands, `/admin/categories` |
| Products | `GET /products` (brand/category/series/price/inStock/featured/hero/newArrival/sort/q/page/limit), `GET /products/filter-options`, `GET /products/:slug`, `GET /products/:slug/related` · admin: full CRUD + `POST /admin/products/:id/thumbnail`, `POST /admin/products/:id/gallery`, `DELETE /admin/products/:id/gallery/:index` |
| Inventory | admin only: `GET /admin/inventory` (stock/reserved/available per product, `lowStockOnly` filter, search), `GET /admin/inventory/:id/logs` (history), `POST /admin/inventory/:id/adjust` (restock/adjustment, blocked if it would drop stock below `reservedStock`) |
| Coupons | `POST /coupons/validate` (**optionally authenticated** — guests can apply coupons at checkout too) · admin: `GET/POST /admin/coupons`, `GET/PATCH/DELETE /admin/coupons/:id` (code immutable after creation; delete blocked once `usedCount > 0` — deactivate instead) |
| Settings | `GET /settings` · admin: `PATCH /admin/settings`, `POST /admin/settings/upload-image` (standalone Cloudinary upload, attachment-target-agnostic — used for hero-slide images and, since System 4, the bKash QR code too — returns `{url, cloudinaryId}` for the client to attach and save with the next full PATCH) |
| Newsletter | `POST /newsletter/subscribe` · admin: `GET /admin/newsletter/subscribers` (search, paginated) |
| Pages | `GET /pages/:slug` (public, published only — 404 otherwise) · admin: `GET /admin/pages`, `GET /admin/pages/:id`, `POST /admin/pages`, `PATCH /admin/pages/:id` (no delete — pages are unpublished, not removed) |
| Contact | `POST /contact` (name/email/message → emails the store's configured contact address via Resend; no message persistence — see §7) |
| Wishlist | `GET /wishlist`, `POST/DELETE /wishlist/:productId` (all authenticated) |
| Cart | `GET /cart`, `POST /cart/items`, `PATCH /cart/items/:productId`, `DELETE /cart/items/:productId`, `POST /cart/merge` (all authenticated — guest cart never touches the server until merge) |
| Addresses | `GET/POST /addresses`, `PATCH/DELETE /addresses/:id` (all authenticated) |
| Orders | `POST /orders` (**optionally authenticated** — accepts `addressId` for a logged-in customer's saved address, or `shippingAddress` + `guestInfo` {name, phone, email?} for a guest/Buy Now; `items[{productId,qty}]` switches order creation onto the direct-items path instead of reading the server cart; body requires `shippingZone`, a name matched against `Settings.shippingZones` to look up the fee snapshotted onto the order; `bkashTransactionId`/`banglaQrReference` conditionally required based on `paymentMethod`; `paymentOption` (cod\|deliveryOnly\|partialAdvance\|full, decision #61, optional, defaults to `"cod"` for backward compatibility) — rejected with 400 if it's anything other than `"cod"` while `paymentMethod` is `"cod"` (Cash on Delivery can't collect an upfront delivery/advance/full amount), and rejected with 400 if the resolved option isn't allowed by every item in the cart or by the destination zone's `requiresPrepay` flag), `GET /orders` (mine, authenticated), `GET /orders/:orderNumber` (mine, authenticated) · admin: `GET /admin/orders` (paginated, filter by `status`, search by `q` on `orderNumber`), `GET /admin/orders/:id`, `PATCH /admin/orders/:id/status` (body: `status`, `note?`, `trackingNumber?`, `courierName?`). Every order response (customer `GET /orders*` and admin `GET /admin/orders*`) already serializes the full Mongoose document, so `paymentOption`/`advancePaymentPercent`/`amountPaid`/`amountDue` need no dedicated projection change — Phase 5 (decision #62) is purely a display layer surfacing these existing response fields on `OrderDetailPage.jsx` (admin + customer), `OrderReceipt.jsx`, and the order-confirmation email. |
| Analytics | admin only: `GET /admin/analytics/summary` (today live + last-7-days), `GET /admin/analytics/daily?days=30` (stored `AnalyticsDaily` history, max 90 — also powers the Reports page's CSV export, no separate reporting backend) or `GET /admin/analytics/daily?startDate=YYYY-MM-DD&endDate=YYYY-MM-DD` (System 8, post-launch — `startDate`/`endDate` take priority over `days` when both are present; powers the Dashboard's custom-range filter, `AnalyticsDaily.date` is a lexicographically-sortable string so the range query needs no Date conversion) |

**Admin UI status:** every domain now has full admin dashboard UI — Products/Brands/Categories (Phase 3), Orders + Dashboard/Analytics (Phase 9), Customers/Coupons/Inventory/Settings/Reports (Phase 10).

---

## 6. Frontend Architecture

- **Design system:** dark theme is a fixed brand identity choice, not a toggle — single `:root` palette in `index.css`, no light-mode variant. shadcn/ui is the primary primitive layer; DaisyUI covers only Rating/Steps/Loading. All page-width/padding logic lives in one `<Container>` component (see §7 — this was a real bug fix, not a preemptive abstraction).
- **State split:** Zustand for client-only state that has no server counterpart (guest cart, recently-viewed, auth session mirror). TanStack Query for everything that comes from the API. Nothing server-authoritative lives in Zustand.
- **Auth state:** `useCurrentUser()` (TanStack Query) is the source of truth; `authStore` (Zustand) mirrors just `{status, role}` for synchronous route-guard reads, always written from the query result — never fetched independently.
- **Cart duality:** one `useCart()` hook presents an identical interface whether the visitor is a guest (Zustand + localStorage) or logged in (server API) — components never branch on auth state. `CartMergeOnLogin` (mounted once at the app root) drains the guest cart into the server cart the moment login state flips.
- **Routing:** `react-router` v8 Data Router (`createBrowserRouter`). Layouts: `PublicLayout` (header/footer), `AuthLayout` (centered, no chrome), `AdminLayout` (sidebar). Guards: `ProtectedRoute` (any authenticated user), `RequireRole` (admin/staff only).

---

## 7. Key Architectural Decisions & Deviations

Flagged explicitly as they were made, not silently — this section is the running record.

1. **`OrderItems` embedded, not a separate collection.** Order line items are always read with their order, never queried independently, and must snapshot product state at purchase time so later edits/deletion never corrupt order history.
2. **`Cart` collection added** (not in the original spec list) — guest carts are client-only; logged-in carts need a server home independent of the `User` document to avoid write contention on auth-critical data.
3. **`Analytics` scoped to business rollups, not raw event tracking** — built in Phase 9 as `AnalyticsDaily`, computed nightly from Orders/Products/Users. Traffic/behavior analytics is intentionally out of scope — that's what GA4/Plausible are for.
4. **shadcn/ui + DaisyUI split**, not either/or — see §6.
5. **Express 5 compatibility fixes:**
   - `express-mongo-sanitize` reassigns `req.query` wholesale, which throws under Express 5 (`req.query` has no setter). Replaced with a custom middleware that mutates objects in place.
   - `req.query` in Express 5 is an **uncached getter** — it re-parses the raw query string on every access, so mutating the object it returns is silently discarded. The `validate()` middleware now shadows the property on the request instance via `Object.defineProperty` instead.
   - Mongoose 9 deprecated `findOneAndUpdate`'s `new: true` in favor of `returnDocument: "after"` — updated throughout.
6. **Mass-assignment fix:** `validate()` originally stripped unknown fields from the *validated result* but left them on `req.body` itself, letting a smuggled `"role": "admin"` ride through `PATCH /users/me` and self-promote a customer. Fixed at the middleware level (body is now fully replaced with validated data) and defense-in-depth in the controller (explicit field whitelist).
7. **Self-service profile CRUD landed in Phase 2**, admin user-management (list/search/role-change) deferred to Phase 10 alongside the admin UI that will consume it — building the API without the UI it's tested against would ship untested surface area. Role changes get their own dedicated endpoint (not folded into general profile update) so they can carry their own safety guards (can't demote yourself, can't demote the last admin) and their own audit-log action type.
8. **Stock reservation lifecycle** (Order module): `reservedStock` holds inventory for pending/unconfirmed orders without touching `stock`. Crossing into a "committed" status (`confirmed` and beyond) converts the hold into a permanent `stock` decrement (`InventoryLog` type `sale`). Cancelling before that commit just releases the hold (`release`); cancelling after commit restores the stock (`adjustment`). All of this runs inside a MongoDB multi-document transaction (Atlas clusters are replica sets even on the free tier, so this is available) — inventory/money correctness isn't a place to cut corners. **Generalized into a full 3-bucket state machine in System 7** (post-launch) — see decision #37; every status now maps to exactly one of `reserved`/`committed`/`released`, and `transitionOrderStatus` handles all six possible cross-bucket moves, not just the original one-directional `pending → confirmed → cancelled` path.
9. **Coupon math centralized** in `coupon.service.js` (`findValidCoupon` + `calculateDiscount`), shared by both the preview endpoint (`POST /coupons/validate`) and authoritative order creation — the discount calculation can never drift between preview and checkout.
10. **Transactional email uses a light-background template**, not the site's dark theme. Dark-mode HTML email renders inconsistently (sometimes illegibly) across clients that force their own color scheme — universal legibility wins over 1:1 brand-theme parity for this one surface.
11. **No fabricated content, ever.** Testimonials seed empty (frontend hides the section until real ones exist) rather than shipping fake customer quotes; social links/contact info stay blank rather than guessing at a real phone number or handle.
12. **`Container` component** (Phase 6): nine sections had independently hand-rolled "centered max-width content with padding," split between two subtly different nesting patterns that only diverge once the viewport exceeds the max-width. Consolidated onto one component so the bug class can't recur.
13. **bKash deferred**, COD-only for v1.0 checkout — real bKash sandbox merchant credentials require a business account the user doesn't have yet. The `Order` schema already carries `paymentMethod`/`paymentStatus`/`bkashTransactionId`, so wiring it up later is additive, not a rework.
14. **Stale-reservation release job** (Phase 9): a `pending` order older than 48 hours (never confirmed — customer abandoned COD or admin never actioned it) is auto-cancelled hourly by `releaseReservedStock.cron.js`, which reuses the existing `transitionOrderStatus` release path (`actorId: null` marks it system-initiated) rather than a bespoke stock-release code path — one lifecycle implementation, not two.
15. **`OrderStatusStepper` is one shared component** (`components/shared/`, built on the DaisyUI `steps` primitive reserved for this in Phase 0 §6) rendered on both the customer order-detail page and the admin order-detail page — the fulfillment flow (`pending → confirmed → packed → shipped → delivered`, or a terminal `cancelled`/`refunded` state) is one visual language, not two independently maintained progress indicators.
16. **Admin dashboard revenue chart is hand-built SVG**, not a charting library — the prescribed stack has no chart dependency, and the dashboard needs exactly one chart (single-series daily revenue line/area), which doesn't justify pulling in a general-purpose charting library. Follows the project's dataviz conventions: one hue (brand primary, since it's a single series — no legend needed), thin 2px line, hover crosshair + tooltip, recessive gridlines.
17. **Role changes are their own endpoint** (`PATCH /admin/users/:id/role`, admin-only — staff, who share every other `/admin/users` route, are explicitly excluded via a second `authorize("admin")` check on just this route), exactly as scoped in the Phase 0 addendum: guards against self-demotion and demoting the last remaining admin, both enforced server-side so the dashboard can never be locked out of.
18. **Coupon codes are immutable after creation**; the update endpoint doesn't accept `code`. Changing a distributed code would silently break every customer who already has it — deactivating and issuing a new code is the correct fix, not an edit. Deletion is similarly blocked once `usedCount > 0` (delete would erase real usage history); deactivation is the only path for a used coupon.
19. **`LOW_STOCK_THRESHOLD` extracted to `config/constants.js`**, shared by the analytics rollup (Phase 9) and the new inventory admin view (Phase 10) — two independent definitions of "low stock" would have been a real (if quiet) bug the moment one got tuned without the other.
20. **Manual stock adjustments are blocked from dropping `stock` below `reservedStock`** — reducing total stock below what's already promised to pending orders would silently break the reservation invariant the whole checkout/fulfillment lifecycle (Phase 8) depends on. The admin sees a clear error naming the reserved quantity rather than a generic validation failure.
21. **"Media" scoped into the Settings editor, not a standalone library.** The original Phase 0 folder sketch listed a separate `admin/media/`, but nothing in the spec needs a general asset browser beyond what per-product Cloudinary upload (Phase 3) and hero-banner images (Phase 10) already cover. `POST /admin/settings/upload-image` returns a Cloudinary `{url, cloudinaryId}` for the editor to attach to a hero slide and save with the next full settings PATCH, rather than a slide-index-addressed endpoint that would race against reordering.
22. **Reports reuses the Phase 9 `AnalyticsDaily` history**, not a separate reporting backend — `GET /admin/analytics/daily` already has everything a daily report needs; the Reports page adds a date-range picker and a client-side CSV export over the same data the Dashboard chart already fetches.
23. **Contact form is a stateless email relay, not a persisted collection.** The original DB schema list never included a `ContactMessage` model, and adding one (plus the admin inbox UI it would need) is real scope the spec never asked for. `POST /contact` sends straight to the store's configured contact email (`Settings.contactInfo.email`, falling back to the first `ADMIN_EMAILS` entry) via the existing Resend integration. Unlike order-confirmation email — a nice-to-have on top of an order that already succeeded — delivering the message *is* the entire point of this endpoint, so a missing recipient or a Resend failure throws and surfaces as a real error instead of silently reporting "Message sent."
24. **FAQ content is admin-editable (`Settings.faqs[]`), not hardcoded.** Same pattern as testimonials/social links since Phase 4: no real FAQ copy existed to ship, so the page renders an empty state pointing to Contact until the admin adds real questions through the Settings editor — never fabricated placeholder policy text.
25. **`components/ui/accordion.jsx` added in Phase 11** — reserved for exactly this (PDP specs/FAQ) back in the Phase 0 shadcn/DaisyUI split, but never actually generated until the FAQ page needed it. Hand-written against the project's `radix-ui` unified-package convention (matching `switch.jsx`/`separator.jsx`), not the shadcn CLI, since `tw-animate-css` (already imported project-wide) ships the `accordion-down`/`accordion-up` keyframes it needs.
26. **Session revocation via `tokenVersion`, not a token store** (Phase 12). Instant session kill (deactivation, demotion, log-out-everywhere) is done with a per-user counter stamped into each JWT (`tv`) and checked on refresh — a leaked/rotated refresh token whose `tv` is stale is rejected. This buys instant revocation of the long-lived (30-day) refresh credential with zero new infrastructure (no Redis/DB token blocklist). The access token stays deliberately stateless (no per-request DB hit), so its ≤15-min window is unchanged — an accepted tradeoff, since the refresh path is where durable access actually lives. Refresh also rotates *both* tokens now, not just the access token.
27. **`trust proxy` set to `1` in production, and admin auto-promotion requires `email_verified`** (Phase 12 hardening). The proxy trust is a deployment necessity behind Render/Railway (secure cookies + correct rate-limit keying); it's `1` (one known hop) rather than `true` so a client can't spoof `X-Forwarded-For` to dodge IP limits. The email-verified gate closes a narrow admin self-escalation path (Firebase doesn't require email verification for email/password sign-ups by default).

### Post-launch feature systems (business requirements doc)

A separate 11-item requirements doc was reviewed and approved as a full architecture review before any code — see the plan file's Guest Checkout → Homepage "See All" breakdown for the complete per-system review. Decisions from System 1 (the only one shipped so far):

28. **Guest Checkout extends `User`, not a parallel "Customer" model.** `firebaseUid`/`email` became optional + sparse-unique (existing accounts are unaffected — no data violates the looser constraint), `phone` gained a non-unique index. A repeat guest is matched by email first, then phone — deliberately "find first match" application logic (`findOrCreateGuestUser` in `user.service.js`), not a DB-level unique constraint on phone, since a hard constraint there would 500 on a plausible shared-household-number collision.
29. **`createOrderFromCart` and the new `createOrderFromItems` share one `buildAndSaveOrder` + `reserveStockForItems` core** (`order.service.js`). Guest checkout (and, later, Buy Now — System 3) can't read a server `Cart` document the way logged-in checkout does, since guest carts are Zustand/localStorage-only and never touch the server — so they send `items[{productId,qty}]` directly in the request body instead. Extracting the shared core means the stock-reservation/coupon/shipping/order-creation math can never drift between the cart-based and items-based paths, mirroring the project's existing centralized-discount-math precedent (decision #9).
30. **`optionalAuthenticate` middleware, not a second copy of `authenticate`.** Reads the JWT cookie if present and sets `req.user`, but (unlike `authenticate`) never rejects the request if it's absent — used only on the two routes that must serve both guests and logged-in users (`POST /orders`, `POST /coupons/validate`); every other authenticated route is completely untouched.
31. **Order confirmation is a separate public page (`OrderConfirmationPage`, `/order-confirmation`), not a loosened auth check on the existing order-detail route.** A guest has no session to authenticate an order-number lookup against, but making that lookup fully public would let anyone enumerate `orderNumber`s (a readable, guessable format, not a bearer secret) and read other customers' addresses/phone numbers. Instead, the create-order response already contains the full order — `CheckoutPage` passes it straight through via router state to a new page that only ever renders from that state (never fetches), used by guests *and* logged-in customers alike right after checkout. The authenticated `/orders/:orderNumber` (order history, revisited later) is completely unchanged. Both pages render the same `OrderReceipt` component so they can't visually drift apart.
32. **Shipping is a list of named zones (`Settings.shippingZones[]`), not a flat fee** (System 2). Matches the requirement's Inside/Outside Dhaka split without hardcoding zone names into the schema — an admin can rename/add/remove zones freely via the same `useFieldArray` editor pattern already used for `faqs[]`. `buildAndSaveOrder` looks up the fee by matching `shippingZone` (a name, sent from checkout) against `Settings.shippingZones`; a miss falls back to a `0` fee rather than throwing, so a stale/renamed zone can never hard-block a checkout — it just ships free instead of failing the order outright. The existing free-shipping-threshold override still applies uniformly on top of whichever zone fee is looked up. Live `Settings` document and its Mongoose index needed a one-time migration (native-driver `$unset` for the orphaned `shippingFee` field — Mongoose's schema-aware `$unset` silently no-ops on fields no longer in the schema; `User.syncIndexes()` to actually rebuild the `firebaseUid`/`email` indexes as sparse, since a schema-level `sparse: true` change doesn't retroactively alter an already-existing index).
33. **Buy Now is a pure frontend feature, zero backend changes** (System 3). It reuses System 1's `createOrderFromItems` + `optionalAuthenticate`/items-branching in `order.controller.js` verbatim — the controller already routed any request carrying `items[]` onto that path regardless of auth state, so a *logged-in* Buy Now just needed the frontend to start sending `items` too (previously only guests did). `BuyNowButton` (new, `features/products/components/`) never calls `useCart()`/`addItem()` — it hands `{product, qty: 1}` to `/checkout` via router `state` only, so a customer's real cart is provably untouched by a Buy Now purchase (verified by curl: placed a Buy Now order for a logged-in user with an unrelated item already in their server cart, confirmed the cart still held only its original item afterward, and the order contained only the Buy Now product). `CheckoutPage` reads `location.state?.buyNowItem`; when present it sources `items`/`subtotal` from a single normalized line item instead of `useCart()`, so `OrderSummary`, `CouponInput`, and the stock-issue guard all work unmodified against either source. Losing router state on refresh degrades to the existing empty-cart redirect (or the real cart, if non-empty) — an accepted tradeoff, not a bug, since Buy Now is an ephemeral one-click path.
34. **Manual bKash, not a live payment gateway** (System 4) — matches the requirement literally: the customer sends money to an admin-configured number/QR (`Settings.bkashConfig`, reusing the existing `imageSchema`/upload-image endpoint verbatim) outside the app and types the resulting Transaction ID at checkout; an admin verifies it manually (surfaced on both the admin and customer order-detail views) and updates `paymentStatus` themselves — there is no gateway callback, no `paymentStatus` automation. `bkashTransactionId` is conditionally required via a Zod `.refine()` on `createOrderSchema` (and mirrored in the frontend `checkoutSchema`) — only when `paymentMethod === "bkash"`, so COD orders are completely unaffected. `Order.paymentStatus`/`bkashTransactionId` needed no schema change at all — both fields were already built into the model in Phase 8 (decision #13) anticipating exactly this.
35. **`profitMargin` is a virtual, computed, never stored** (System 5), mirroring the existing `availableStock` virtual's exact pattern — resolves automatically wherever `costPrice` is already `.select("+costPrice")`ed. Returns `null` (not `0`) when `costPrice` is unset/unselected — an admin needs to tell "0% margin" apart from "cost unknown," and the public storefront (which never selects `costPrice`) gets a harmless `null` rather than a fabricated number. Both the Sale badge and Cost/Profit tracking requirements turned out to be mostly already built (Phase 6/Phase 3 respectively) — System 5 closed the two real gaps: the PDP was missing the Sale badge `ProductCard` already had, and the admin products table had no Cost/Profit/Margin columns despite `costPrice` already being a fully wired, editable field. **Revised same day per explicit user correction:** originally computed off list `price` only ("a catalog-level metric, doesn't dip during a sale"); the user asked instead for Price/Profit/Margin to reflect the *live* selling price — margin now uses `salePrice` whenever the item is actually on sale (same `salePrice != null && salePrice < price` check used everywhere else in the codebase), falling back to `price` otherwise. The admin products table's Price column now also shows the sale price with the original struck through (matching `ProductCard`'s existing storefront pattern) instead of always showing list price, and gained a Sale badge next to the title. The Sale badge itself was also restyled off `variant="destructive"` (a muted/low-contrast red, not actually eye-catching) to a solid `bg-amber-400 text-background` — a warm, high-contrast accent distinct from the lime-green "New" badge, applied consistently on `ProductCard`, the PDP, and the new admin table badge.
36. **`homepageSections` toggle map covers the sections `HomePage.jsx` actually renders, not the plan review's original list.** The architecture review's draft section list included a "faq" toggle, but no FAQ section is ever rendered on the homepage (FAQs live on their own `/faq` page) — building a toggle for a section that doesn't exist would be dead config, so the shipped map instead has one entry per real section: `hero` (+ `autoplay`/`autoplayInterval`), `collectorPicks`, `featuredProducts`, `brandsStrip`, `newArrivals`, `whyChooseUs`, `collectorPromise`, `testimonials`, `instagramFeed`, `newsletter`. Every toggle defaults to `enabled: true`, and the frontend's `isEnabled()` check also defaults to `true` when a key is missing entirely — so an un-migrated or partially-saved document can never accidentally hide a section. Implemented as a gate at each section's call site in `HomePage.jsx` (`{isEnabled("x") && <Section .../>}`) rather than editing every section component's internal render guard — same visual outcome, smaller and lower-risk diff (one file instead of seven). Hero's Embla `Autoplay` plugin delay, previously a hardcoded `6000`, is now `autoplayInterval * 1000` passed at hook-init time; Embla doesn't hot-reload a running plugin's delay after mount, so a live admin edit takes effect on the visitor's next page load, not instantly mid-session — consistent with how every other Settings-driven value already behaves in this app (nothing here supports true live-reload without a refresh).
37. **Order status transitions are a full 3-bucket state machine, not the original one-directional pending→confirmed→cancelled path** (System 7 — unrestricted status changes). Every status maps to exactly one stock bucket — `reserved` (pending: held via `reservedStock`, nothing decremented), `committed` (confirmed/packed/shipped/delivered: `stock` permanently decremented, no hold), `released` (cancelled/refunded: no claim on stock at all) — and `transitionOrderStatus` (`order.service.js`) now handles all six possible cross-bucket moves, not just the original forward ones. The terminal lock (`if (TERMINAL_STATUSES.includes(...)) throw`) is gone entirely; the only remaining guard is same-status (`pending → pending` etc.), which still 400s. **The original architecture review's plan text for the `committed → reserved` delta was wrong** (`{stock: -qty, reservedStock: +qty}`) — worked through concretely with real numbers before writing any code: undoing a commit must restore *both* fields (`{stock: +qty, reservedStock: +qty}`), or `availableStock` silently drifts further wrong on every confirmed↔pending bounce. The review also hadn't accounted for reverting *out of* a released status at all (`cancelled`/`refunded` → anything else) — naively reusing the existing reserved→committed branch for that direction would drive `reservedStock` negative, since a released order's hold is already fully cleared to zero. **`released → reserved` and `released → committed` are the only two directions that re-check availability** (the same atomic `findOneAndUpdate` + `$expr` compare-and-reserve pattern `reserveStockForItems` already uses) — every other direction trusts the order's existing hold/commit and applies its delta unconditionally, exactly as the original code did. This isn't optional defense-in-depth: another customer can genuinely buy up stock while an order sits cancelled, and un-cancelling it without checking would oversell. Verified with a real disposable order and a real competing order: reverting a cancelled order back to `pending` after a second order consumed all the freed stock correctly returns a 409, not a silent negative-stock write. **Verification approach:** curl-scripted the full transition matrix against a real product on the dev database (not a mocked/unit-tested state) — all six cross-bucket transitions, the same-bucket no-op case, the same-status guard, and the exact `confirmed → pending → cancelled` repro that was independently confirmed as a real latent bug in the pre-System-7 code (closed by this fix) — 24 assertions, all passing, cleaned up and restored to baseline afterward. `stockBucket()`, the pure classification function every branch dispatches on, is exported and covered by a dedicated Vitest unit test (`order.stockBucket.test.js`) — the six-branch `$inc`/atomic-guard logic itself stays curl-verified rather than adding new integration-test infrastructure (`mongodb-memory-server` or similar), consistent with how every other transaction-heavy path in this project has been verified so far.
38. **Dashboard Analytics ships Orders/Revenue with a date-range filter; "Total Visitors" is skipped, per explicit user confirmation** (System 8). Re-presented the architecture review's own recommendation before starting (traffic tracking needs a new event-ingestion endpoint, bot filtering, session dedup — a genuinely different category of infrastructure this project deliberately scoped out in favor of GA4/Plausible, decision #3) — user confirmed skip. `GET /admin/analytics/daily` gained an optional `startDate`/`endDate` query pair (takes priority over `days` when both are present); the existing `days` param and every current caller (`ReportsPage.jsx`'s 7/30/90 presets) are completely unchanged. Dashboard's new range picker (Today/Last 7 days/Last 30 days/Custom range) reuses the same `Select` pattern already proven in `ReportsPage.jsx`. **"Today" deliberately does not call the new range endpoint at all** — the nightly rollup hasn't computed `AnalyticsDaily` for the current day yet (same reason `GET /admin/analytics/summary` already computes today live instead of reading the stored table, decision documented on the `AnalyticsDaily` schema row), so a `startDate=endDate=today` range query would come back empty despite real sales existing. "Today" instead reuses the already-fetched `summary.today` object directly — it has the exact same row shape (`{date, revenue, ordersCount, ...}`) `computeDailyRollup()` produces, so it slots into the same chart/stat-sum logic as any other row with no special-casing needed downstream.
39. **CMS Pages is a new `Page` collection with its own module, not a `Settings` extension — and About/Contact/FAQ deliberately stay exactly as they are** (System 9). `Settings` is a singleton; Pages is inherently a collection of independent documents, so forcing it into `Settings` would be a real modeling mismatch (unlike every other post-launch extension in this batch, which correctly reused `Settings`). About/Contact/FAQ are richer than a flat content page (Contact has a live form, FAQ renders `Settings.faqs[]` as an Accordion) — migrating them onto the new generic `PageView` component was flagged as a scope question at review time and deliberately left undone; this system only ships the 4 net-new pages (Terms/Privacy/Refund/Shipping-Policy) the requirement actually asked for. **User chose Tiptap for the admin rich-text editor and to seed all 4 pages empty** (both explicitly confirmed before starting, per the review's own flagged blockers) — `isPublished: false` and `content: ""` for all four; an admin writes and publishes real copy later via Admin → Pages, same "no fabricated content" precedent as testimonials/bKash config/social links. Admin-authored HTML is sanitized **server-side on save**, not client-side on render (`page.controller.js`, `sanitize-html` with an explicit tag allow-list matching exactly what `@tiptap/starter-kit` produces) — verified against a real XSS attempt: a `<script>` tag was stripped entirely and a `javascript:` URL in an `<a href>` had its `href` attribute removed, while a legitimate relative link passed through untouched. Public `GET /pages/:slug` 404s for anything not `isPublished` — verified directly, then verified again after publishing that the same slug returns the sanitized content. Tiptap (`@tiptap/react` + `@tiptap/starter-kit`, ~427KB, ~134KB gzipped) is isolated entirely to the code-split admin page-editor route (`PageFormPage.jsx`) and never reaches the public storefront bundle.
40. **Header/Footer link management extends `Settings` (not a new collection) — the plain link lists only, not the cart/auth-state icons or announcement bar**, per the requirement's own "keep the interface simple" scoping (System 10). No `logo` override field either, despite being sketched in the original review — the requirement was specifically about link management, and adding upload/override machinery for something that's a static brand asset today would be unrequested customization. `navigation.headerLinks[]`/`footerLinks[]` have no separate `order` field — array position is the order, matching every other list field already on this document (none of them support explicit reordering either, only add/remove). **A live-DB migration was required, not just a seed update** — unlike a fresh install, the *already-existing* Settings document doesn't get `$setOnInsert` seed values applied to a newly-added field; Mongoose's schema-level `default: []` on the array sub-fields made `GET /settings` **appear** to already have `navigation` populated (an in-memory hydration artifact — confirmed empty on the raw stored document), which would have made the header/footer go visibly blank the instant this code deployed. Migrated the live document to hold exactly what `PublicLayout.jsx`/`Footer.jsx` already had hardcoded (one "Shop" header link; About/Contact/FAQ/Shipping-Policy/Refund-Policy/Privacy-Policy/Terms-Conditions footer links, matching System 9's just-added legal-page links too) — verified via curl before and after, confirming the migration changed nothing about what a visitor would actually see. Both `PublicLayout.jsx` and `Footer.jsx` additionally fall back to their original hardcoded values in-code if `navigation.headerLinks`/`footerLinks` is ever empty, so an admin accidentally deleting every link can't leave the site with no navigation at all.
41. **"See All" is two new optional props on the existing shared `ProductCarouselSection`, not a new component** (System 11 — the smallest, lowest-risk system in the whole batch, zero backend/DB/API changes). `seeAllHref`/`seeAllLabel` (default `"See All"`) render a plain text link next to the existing prev/next carousel buttons, only when `seeAllHref` is passed — every other consumer of this shared component (`ProductDetailPage.jsx`'s "Related Products"/"Recently Viewed") is completely unaffected since they never pass the new prop. All three homepage product carousels (Collector Picks, Featured Products, New Arrivals) point at plain `/shop`, per the requirement's literal reading ("navigate to the main Shop/Store page") rather than a per-section filtered deep-link — the architecture review had flagged this as an open question with that literal reading as the default if the user didn't specify a preference otherwise.

---

## Post-launch feature systems: complete

All 11 systems from the original architecture review have shipped, one at a time, each curl/build/test-verified and documented here and in `docs/log.md` before moving to the next: Guest Checkout, Shipping Zones, Buy Now, Manual bKash, Sale Badge + Cost/Profit, Homepage Section Toggles, Order Management (unrestricted status + symmetric stock), Dashboard Analytics, CMS Pages, Header/Footer Management, Homepage "See All". Two real pre-existing bugs were found and fixed along the way (hero slide images never rendering; a phantom `reservedStock` hold from a pre-System-7 refund), both documented in `docs/log.md` at the point they were discovered. Remaining open items, not part of this batch: real Terms/Privacy/Refund/Shipping-Policy copy (pages are seeded empty, waiting on the user), and the deferred bKash live-gateway integration (still COD + manual bKash for v1.0, per decision #13).

---

### Storefront redesign v2 — light "paper" theme (from `design_handoff_diecastbd_storefront/`)

The customer-facing storefront was rebuilt from scratch against an 18-file design handoff (Phases 0–10, hard-stop-per-page on `main`). The **admin stays byte-identically dark** — verified by `scripts/verify-theme-split.mjs` as the exit gate of every relevant phase. Decisions:

42. **Theme split is a fixed brand identity, not a runtime toggle.** `:root` in `index.css` holds the light "paper" palette (brand lime `#A8CD2F`, ink `#101208`, paper `#FAFAF7`, Archivo/Instrument Sans); `[data-theme="diecastbd-admin"]` (set by `AdminLayout`) re-declares the entire original dark palette + Geist fonts. Two DaisyUI themes mirror this. The redesign is **built from scratch** on `main`, never copied from the reverted `storefront-redesign` branch — its hard-won lessons are encoded as constraints instead. The reverted backend pieces (RestockAlert, `banglaqr` payment, hero `variant`/`highlightCard`, `shippingZones[].eta`) were restored via `git checkout storefront-redesign -- backend/`.
43. **Custom radius scale inflates named `rounded-*` — storefront uses explicit px.** `index.css` derives `--radius-xl/2xl/3xl` from `--radius: 0.75rem`, so `rounded-xl`=16.8px, `rounded-2xl`=21.6px, `rounded-3xl`=26.4px — *not* the design's 12/16/24. A project-wide audit converted every design-critical card/image/input to explicit `rounded-[Npx]` (values read per-element, per-breakpoint, from the `.dc.html`). **Rule going forward: explicit px for storefront design radii; named `rounded-*` reserved for shadcn primitives** (which depend on the derived scale). Same class of bug as the design's `box-sizing: content-box` radio dots (28px selected / 21px unselected) and its 44px inputs (`leading-[1.2]`, not the default 1.5) — both matched by measuring the rendered `.dc.html`, not by reading its CSS.
44. **`OrderTracker` (custom Tailwind) replaces the DaisyUI `OrderStatusStepper`.** DaisyUI's pseudo-element steps can't hit the reference pixel-for-pixel; the new component (lime+check up to current status, hollow rings after, gray connectors, danger banner for cancelled/refunded) is used on the confirmation, order-detail, *and* admin order pages. The stepper's unit test was migrated 1:1 to `OrderTracker.test.jsx`. `StatusChip` replaced the old `OrderStatusBadge` everywhere; both old components deleted.
45. **Design copy is shipped content; policy bodies are CMS-only.** Per the user's decision, the `.dc.html` copy (hero, About, FAQ questions, channel microcopy) ships as real content, not empty states — real backend data still wins where present. **Exception, explicit: policy pages render CMS `content` only, never hardcoded from the design.** To reconcile that with "not 404-ing on empty drafts," the design's policy copy was **seeded into the 4 CMS `Page` documents** (as sanitized `<h2>/<p>` HTML) and published — the frontend stays CMS-driven, the merchant edits from Admin → Pages. FAQs were likewise seeded into `Settings.faqs[]`. Contact WhatsApp/email/socials fall back to design defaults but a fabricated WhatsApp *number* is never invented — the card only renders when `socialLinks.whatsapp` is really set.
46. **Radix `Select` needs `key={value}` + RHF `values:` for async-loaded values.** The admin Settings form loads server data after mount; register-inputs sync fine via RHF's reactive `values` prop, but a controlled Radix `Select` shows its placeholder instead of the loaded value until keyed on that value (forces a remount so the trigger re-reads the matching item's text). Applied to the hero-style select.
47. **Contact endpoint accepts email-or-phone + optional order ID.** `POST /contact` was `{name, email, message}`; the redesigned form has "Email or phone" + "Order ID (optional)", so the backend `contact` field now takes either shape (existing `isEmail`/`isBdPhone`), `replyTo` is wired only for email-shaped contacts, and `orderId` is folded into the notification email. Frontend `contactSchema` uses the shared `phoneOrEmailSchema`.
48. **`Page.tldr` added for the policy TL;DR card.** New optional plain-text field (model + create/update validation + create controller); `PageView` renders the "short version" card only when a page provides it — no hardcoded TL;DR. `socialLinks.youtube` added the same way (model + validation + admin field; `SiteFooter`/Contact prefer it over the design default).
49. **Dedicated checkout & auth shells; hidden auth brand panel on mobile.** `/checkout` swaps the storefront nav for a minimal header (logo · progress stepper · Sign in/Create account or name/Sign out) via `PublicLayout`'s `isCheckout` branch. Auth pages render their own full-screen split (`AuthShell`, `grid-cols-1 md:grid-cols-2` — not `minmax(360px)`, which overflowed at 320px) with the brand panel `hidden md:flex`. All Firebase mutations + `CartMergeOnLogin` were preserved verbatim through the auth restyle. The signed-in `SiteHeader` gained a user dropdown (Account/Orders/Wishlist/Admin-dashboard-if-staff/Sign out) — sign-out previously had no UI entry point anywhere.

---

## Storefront redesign v2: complete

All 11 phases shipped, hard-stop-per-page, each verified with Playwright screenshots at 390/1440 (+320 overflow) against the `.dc.html` and committed to `main`. Every customer-facing route (Home, Shop, PDP, Cart, Checkout, Order Placed, My Orders, Order Detail, Account, Wishlist, Sign in, Create account, Forgot password, Contact, FAQ, About, 4 policy pages) matches the design at both breakpoints; the admin stayed dark (theme-split verified). Exit gates met: 4 real checkout orders (guest+COD/bKash/BanglaQR + buy-now), a fresh buy-now order re-verified in Phase 10, and no 320px overflow on any route. One real data/code bug was caught by the checkout exit gate — a stray `salePrice: 0` producing a ৳0 order — fixed in both the data and the shared order-pricing line (now mirrors the model's effective-price rule). Deliberately deferred (low value, storefront works via fallbacks): admin editing of announcement-bar segments and footer payment pills. Still open, unchanged: real policy copy is now seeded from the design but the merchant should replace it with genuine legal text; a real logged-in end-to-end (Firebase) pass on the auth/merge flow.

---

### Admin dashboard light redesign (from `design_handoff_admin_light_redesign/`)

The admin was the last dark surface in the app. It's now light, sharing the storefront's
`:root` palette and Archivo/Instrument Sans — the theme split from decision #42 is
**gone**, not inverted. All 12 screens rebuilt against the handoff (Phases 0–12, on
`main`, one commit per screen). Decisions:

82. **The dark admin theme was deleted outright, not re-skinned.** Removed the
    `[data-theme="diecastbd-admin"]` scope in `index.css`, the DaisyUI `diecastbd-admin`
    block, `src/styles/admin-fonts.css` + its import, the `data-theme` attribute in
    `AdminLayout`, and `@fontsource-variable/geist` from `package.json`.
    `scripts/verify-theme-split.mjs` — the Playwright guard whose whole purpose was
    keeping admin dark — was deleted too, since this redesign inverts its premise.
    Deleting the scope is safe precisely because nothing else referenced it: admin fell
    back to `:root` light tokens and every screen stayed functional while unstyled.
83. **A shared `admin/shell/` kit came before any screen.** `AdminShell`, `AdminPageHeader`,
    `SectionPanel`, `AdminButton`, `AdminModal`, `AdminThumb`, `AdminSearch`,
    `FilterChips`, `SaveBar`, `BulkBar`, `KpiCard`, `adminToast`, `adminFieldCls`. The
    field classes are load-bearing: shadcn's `Select` ships `data-[size=default]:h-8`,
    which out-specifies a plain `h-11`, so selects rendered 32px next to 44px inputs
    until `adminSelectCls` overrode the same variant. Same class of problem in
    `AdminModal`, where shadcn caps width at **both** the base and `sm:` breakpoints —
    tailwind-merge only dedupes within a variant, so the width is set inline.
84. **Two small backend endpoints were added, nothing more.** `PATCH /admin/products/bulk-status`
    and `DELETE /admin/products` (bulk soft-delete, per-product AuditLog rows following
    the `deleteOrders` precedent from #74, ids ≤100), plus
    `DELETE /admin/newsletter/subscribers/:id` (hard delete — the unique-email index lets
    a removed subscriber sign up again, which a soft-deleted row would block). Also fixed
    `listProductsAdmin`, which had been silently ignoring `status` and `q`: the admin
    product search box did nothing at all before this.
85. **Effective-price math moved into a shared module — a live ৳0 bug.** Two active
    products carried `salePrice: 0`, and both existing guards were wrong: `salePrice ?? price`
    (`0 ?? 200 === 0`) and `salePrice != null` (`0 != null` is true). Customers could have
    been charged ৳0. Now `frontend/src/lib/pricing.js` + `backend/src/utils/pricing.js`
    define one rule — a sale price counts only when `> 0 && < price` — and 14 hand-copied
    call sites use it. The data was cleaned and the boundary normalises `0 → null`.
86. **Blocks-based Pages builder ships UI-only, and says so.** *(Superseded by #89 — blocks persist and render now; kept for the reasoning behind the honest-notice approach.)* Eight block types, native
    HTML5 drag-and-drop (no `@dnd-kit` in this project; the brief bars new libraries) with
    arrow buttons as the touch/keyboard path. Blocks live in component state — the API has
    no field for them yet — so an amber notice above the canvas states plainly what saves
    and what doesn't rather than letting the merchant lose work silently. The Tiptap editor
    stays in a collapsed section because it still owns every page live today. Block JSON
    shape is `{ type, ...fields }`, page is `{ title, slug, status, seoTitle, seoDesc, blocks[] }`.
87. **Settings groups by context, not by scroll position.** 16 cards behind a 12-item
    sticky rail in 4 groups (card variant), one group visible at a time; cards declare
    their rail item and read the active one from context. The single-`useForm` /
    `values` / complete-`defaultValues` / whole-object-PATCH wiring is untouched — PATCH
    replaces the whole subtree, so a field missing from `defaultValues` is a field
    deleted on save. The save bar reads **`dirtyFields`, not `isDirty`**: with a
    server-fed `values` prop, `isDirty` stays true after a successful save (with an empty
    `dirtyFields`), leaving the bar permanently armed.
88. **Merchant-requested departures from the prototype**, all deliberate: a product photo
    column immediately after the checkbox in the Products list (the design has no
    thumbnail); the real site logo wherever the design used a `"DiecastBD."` wordmark, sized
    by width and with the ADMIN pill removed (sizing by height overflowed the 232px
    sidebar); and list thumbnails filling 100% of tile height like the storefront product
    card (`h-full w-auto max-w-none` in `AdminThumb`) rather than letterboxing under
    `object-contain`.

---

89. **Pages blocks became real: persisted, validated, rendered.** `blocks[]` on the
    `Page` model, validated by a Zod discriminated union (`page.blocks.js`) mirroring
    the admin block registry field for field, `.strict()` per block (unknown field →
    400) and capped at 60. **Blocks are structured data, never markup** — the
    storefront renders them as React elements, so unlike `content` (rich-text HTML,
    sanitized server-side) there is no `dangerouslySetInnerHTML` on the block path and
    nothing to strip. Links are therefore the only injection surface: image/button/
    carousel URLs must be relative or http(s), enforced in the API *and* again in the
    markdown renderer. Product/carousel blocks store **slugs**, resolved server-side in
    `getPageBySlug` (active products only) so a price edit propagates instead of
    freezing at build time and a public page stays one request. Merchant-built pages
    live at `/<slug>` via a catch-all placed last in the public layout — real routes
    still win, unknown slugs 404 as before. `ProductCard` gained an opt-in `hidePrice`
    for the grid's "show prices & buy button" toggle. Known gap, pre-existing: there is
    **no DELETE route for pages** — the admin can create pages but not remove them.

90. **Pages CRUD completed; a shared param schema was masking 500s.** `DELETE /admin/pages/:id`
    (+ `auditLog`) closes the gap where pages could be created but never removed. It
    **refuses the four policy slugs** with a 409 — `/terms-conditions` and friends are
    hardcoded storefront routes with footer links, so deleting one turns a linked page
    into a 404; unpublish or clear them instead. The admin list renders those rows with
    a disabled icon rather than a button that could only ever fail, and the confirm
    dialog names the real consequence per page (published → the URL starts 404ing for
    anyone holding the link; draft → nothing on the storefront changes). Separately, the
    shared `idParamSchema` was `z.string().min(1)`, so a malformed id reached Mongoose
    and threw a CastError — surfacing as a **500 on GET, PATCH and DELETE alike**. Both
    param schemas now require a real ObjectId. Image and carousel-slide blocks upload to
    Cloudinary through the existing `POST /admin/settings/upload-image` rather than a
    second endpoint doing the same job.

91. **SEO hardened along the whole read path, not just the settings card.** The SEO
    section gains three merchant-editable fields on `seoDefaults`: `shareImage`
    (default og:image), `googleSiteVerification`, and `returnWindowDays`. Wiring that
    actually matters: `Seo.jsx` now treats Settings as the real site-wide defaults
    (they previously applied to the homepage only), renders og:image/twitter:image
    (page image wins, e.g. PDP thumbnails), and emits the GSC meta. **Social crawlers
    (FB/WhatsApp) never run JS**, so their fallbacks are static: index.html carries the
    GSC token and an og:image pointing at `/share-image`, a new public route that
    302-redirects to the current Cloudinary upload (Vercel rewrite added; FB's scraper
    follows redirects). Per-product WhatsApp previews would need a bot-serving layer —
    deliberately deferred and documented. Also: the sitemap now includes published CMS
    pages; PDPs emit BreadcrumbList plus merchant-listing extras built from REAL data
    only — `shippingDetails` from the configured shipping zones and
    `hasMerchantReturnPolicy` only when the merchant sets a return window (blank =
    omitted, never invented). Meta keywords deliberately excluded (ignored by Google
    for 15+ years). Found while wiring: `optionalNumber` is a schema factory and was
    passed bare, so every settings save 500'd until called properly.

92. **Static prerendering for the stable marketing routes — a post-build Playwright
    snapshot, chosen over SSR to stay least-invasive.** Decision #91 relied on Google's
    JS rendering plus a static index.html for JS-blind crawlers; that left the marketing
    routes (about/contact/faq/policies) sharing the generic homepage meta for FB/WhatsApp.
    Rather than adopt SSR (a new server entry + StaticRouter + hydration — off-limits per
    the "don't change React Router / Vite / deploy" brief), `scripts/prerender.mjs` runs
    after `vite build`: it `vite preview`-serves the freshly built `dist`, drives Chromium
    (Playwright, already a devDep) through each static route, waits until react-helmet-async
    has injected a `<link rel="canonical">` whose path **matches the route** (both the
    "rendered" signal and a correctness guard — a route that renders an error/empty state
    never matches and is skipped, so no junk is ever baked), de-dupes the `<head>` (index.html's
    static fallback tags + helmet's per-route tags would otherwise leave two `<title>`s —
    `document.title` wins for the title, last-wins for meta/canonical), scrubs any leaked
    preview origin, and writes `dist/<route>/index.html`. **Product/shop pages stay CSR on
    purpose** — their data is MongoDB-at-runtime, so prerendering would freeze price/stock;
    they already emit dynamic client-side meta + Product/Offer/Breadcrumb JSON-LD, and the
    backend sitemap already lists every product. Serving: Vercel serves a real
    `dist/<route>/index.html` before any rewrite, so prerendered routes win and everything
    else falls through. **The homepage snapshot overwrites `dist/index.html`, so the neutral
    SPA shell is preserved as `dist/app.html` and `vercel.json`'s catch-all now points there**
    — otherwise a CSR product page would inherit the homepage's canonical. `app.html` is
    written FIRST, unconditionally, before any skip path, because it is the fallback for every
    dynamic route — if it were missing, products/shop/account would 404. The whole step is
    **fail-soft**: no Chromium, `PRERENDER=false`, a local `VITE_SITE_URL`, or a render error
    all leave `app.html` in place and the routes CSR, and the build still exits 0 — it can
    never break a deploy, only enhance it. **Deployment requirement:** the Vercel build must
    set `VITE_SITE_URL=https://diecastbd.com` (baked canonicals) and reach the API at build
    (`VITE_API_BASE_URL`) for the CMS policy pages to prerender; when the API is unreachable
    those four gracefully stay CSR. Shipped alongside: `manifest.webmanifest` (+ link/icons),
    four additive security headers in `vercel.json` (nosniff, X-Frame-Options SAMEORIGIN,
    Referrer-Policy, Permissions-Policy — CSP deferred to avoid breaking Firebase/Cloudinary/
    inline JSON-LD), Vite `manualChunks` vendor splitting (the initial catch-all `return
    "vendor"` broke module init order — "t is not a function" — fixed by returning `undefined`
    for unmatched deps and splitting only named vendors), canonical/og:url added to
    About/Contact/FAQ/Shop, a conditional `FAQPage` JSON-LD (only when the merchant has real
    FAQs), and robots.txt extended (/api, /private, /dashboard, /unauthorized).

92. **Order notification emails + WhatsApp chat widget.** Two new Resend templates
    (`src/emails/adminNewOrder.js`, `orderConfirmed.js`) on the existing infra:
    the store owner is emailed on EVERY order (recipient =
    `Settings.contactInfo.email` → `ADMIN_EMAILS[0]` fallback, resolved inside the
    fire-and-forget so nothing can touch order creation; includes bKash/BanglaQR
    references and a deep link to the admin order), and the customer is emailed when
    the admin confirms. `transitionOrderStatus` now returns `{ order, previousStatus }`
    (it mutates in place, so callers couldn't know the FROM status) and the send is
    gated by a pure exported guard: **pending → confirmed only** — packed→confirmed
    relabels and cancelled→confirmed restores stay silent, confirmed→pending→confirmed
    legitimately re-sends, and the auto-cancel cron (only ever →cancelled) can never
    fire it. The placement email's subject was renamed "Order received — …" (it said
    "Order confirmed" while the body said pending — the real confirmation email owns
    that word now). New templates escape user-typed strings (older ones interpolate
    raw — flagged, not refactored). **WhatsApp order-notify was deferred**: the only
    legitimate path is Meta's Business Cloud API (verification + tokens + template
    approval); third-party bridges route order data through unknown servers.
    `WhatsAppWidget` (storefront-only via PublicLayout, hidden on checkout per
    merchant decision) renders ONLY when `socialLinks.whatsapp` is set, reuses the
    contact page's URL normalisation, floats at bottom-[86px] on mobile to clear the
    docked bars, z-40 under sheets.

93. **Shop page: numbered pagination → infinite scroll, with a deliberate cap.**
    `useInfiniteProducts` (key `["products","infinite",params]` — a SIBLING of
    `list`, never nested, since `useQuery` caches `{data,meta}` while
    `useInfiniteQuery` caches `{pages,pageParams}` and a colliding key would
    corrupt the entry CollectionPage/HomePage read). `page` is gone from
    `useShopFilters` entirely (it would otherwise poison the query key, which
    spreads `filters`), with a mount-time strip for stale `?page=N` bookmarks;
    SEO is unaffected because `/shop?page=N` has always canonicalised to
    `/shop`. Scrolling **auto-loads 2 pages, then requires a tap** (a tap
    re-arms it) — pure infinite scroll makes the footer, and therefore the
    internal links to the `/brand` and `/category` landing pages, practically
    unreachable. The budget is guarded by a **ref**, not state: two
    intersections can arrive before React re-renders, and a state-only guard
    let both through — caught by a unit test, not by the browser, where
    `isFetchingNextPage` masks it. New reusable
    `hooks/useIntersectionObserver.js` (callback ref because the sentinel
    renders conditionally; `onIntersect` held in a ref so an inline arrow can't
    rebuild the observer every render; inert without `IntersectionObserver` so
    jsdom needs no stub). Two prerequisites shipped first: **`SORT_MAP` gained
    `_id` tie-breakers** — Mongo's sort is unstable for ties and 3 prices are
    shared in the live catalogue, so a tie on a page boundary could repeat or
    drop a product (numbered pagination hid it; appending would render a
    duplicate card) — and the **product grid now renders once instead of
    twice** (measured 48 ProductCards for 24 products, each with its own
    wishlist observer and restock subscription; unbounded once the list grows
    by scrolling). Client-side de-dupe on `_id` stays for live inventory
    shifts. Known follow-up: scroll position isn't restored when returning
    from a PDP (pre-existing; fixing it touches the shared `ScrollToTop`).

94. **Homepage first paint: the hero no longer flashes a wrong variant, and the
    page no longer blanks.** One root cause behind both symptoms: the prerender
    crawl runs `vite preview` on `127.0.0.1:4183`, which is **not in the API's
    CORS allowlist** (verified: 200 response, no `Access-Control-Allow-Origin`
    → the browser discards it). So every in-page API call during the build
    failed silently, and `dist/index.html` — which Vercel really serves for `/`,
    filesystem beating the `app.html` rewrite — baked the **lime default hero
    with a dashed placeholder** while the live setting was `photo-fullbleed`
    with a real image. The same failure timed out 4 of 8 routes (all four policy
    pages shipped as empty shells). Fixed by proxying `**/api/v1/**` through
    Playwright's `page.route()` to **Node's fetch**, which isn't subject to
    CORS: build-only, no backend change, no production CORS surface. Now 8/8.
    On top of that the home snapshot inlines a `__SETTINGS__` payload
    (`initialData` + `initialDataUpdatedAt: 0`, so it renders instantly *and*
    revalidates immediately rather than being frozen by the 10-minute
    staleTime) and a `<link rel="preload" as="image">` for the hero, whose
    `<img>` previously couldn't even be requested until a round trip finished.
    `HeroSection` no longer defaults `variant` to lime — unknown is its own
    state, because the variants are different components at different heights
    (a swap was also an 80px layout shift). The three sections that returned
    `null` while loading now reserve their space. **The measured finish:**
    `React.lazy` suspends for a render pass even with the chunk preloaded, so
    `createRoot` wiped the prerendered hero and showed the full-page spinner —
    hero at 110ms, spinner 217→520ms, hero again. HomePage is now the one eager
    route: hero at 82ms and never disappears (slow-3G: 1840ms → 80ms without a
    hero), and the main bundle *shrank* 198→116KB as Rollup restructured.
    Also fixed in passing: the script read `process.env.VITE_SITE_URL`, which
    Node never populates from `.env`, so it fell back to the production origin
    while the bundle had localhost baked in — the source of the
    `localhost:5173` canonical in `dist`. Fonts were **measured, not assumed**:
    all 8 carry `font-display: swap`, so they never blocked text paint and were
    left alone.

95. **Reports now track orders in real time; historical test data purged.** The
    daily rollup is a *derivation* of the orders collection, but the cron only
    ever recomputed **yesterday** — so any change to an older order left its day
    frozen. The merchant hit the visible case: test orders had been deleted, yet
    Reports still showed **৳33,046 of revenue across 13 orders that no longer
    existed** (live orders totalled ৳5,210 against a reported ৳38,256).
    `recomputeRollupsForOrders(orders)` (analytics.service.js) now re-derives the
    affected days, hooked at three points: order **creation** (fire-and-forget —
    the customer must never wait on analytics), **deletion** (awaited, after the
    transaction commits so it reads post-delete truth), and **status changes that
    cross the cancelled boundary** — cancelled orders are excluded from the
    rollup, so only that crossing changes the numbers; packed→shipped and
    friends skip the work. Because the rollup is derived, recompute is
    idempotent and self-healing. Data cleanup (backed up first to
    `/tmp/analytics-backup.json`): 19 pre-28-July rows deleted, 9 orphan guest
    records from the deleted test orders removed (guarded — never delete a guest
    still attached to an order), every remaining row re-derived. Report total
    now equals live orders exactly. Real-email accounts were kept per the
    merchant's decision, for them to review. **Flagged, not changed:**
    `refunded` orders still count toward revenue (only `cancelled` is excluded)
    — a business call, not a bug. **Merchant answered: refunds must not count.**
    `NON_REVENUE_ORDER_STATUSES = ["cancelled", "refunded"]` now lives in
    config/constants.js and is shared by the daily rollup, the dashboard
    summary, the per-customer lifetime spend (which already excluded both — so
    analytics was the outlier, and a refund used to inflate Reports while
    leaving the customer's totals correct) AND the recompute boundary above,
    which had to move in lockstep or marking an order refunded would silently
    leave the report stale.

---

## Admin light redesign: complete

All 12 screens shipped on `main`, one commit each, verified at 1440 / 390 / 320 with a
Playwright sweep (h1 present, zero console errors, no horizontal overflow on any screen
at any width) plus real round trips against the dev DB for every stateful flow: bulk
product status and bulk delete, stock adjust with restore, product save with restore,
coupon create + clipboard copy read back, subscriber delete (400/404/200 + count 2→1),
page save through a reload, and a settings save that left all 19 keys and every nested
array intact. Two bugs the build could not have caught were found by loading pages: a
dropped `Button` import (runtime ReferenceError, build clean) and the mobile product rows
overflowing because `grid-cols-[auto_1fr]` won't shrink below its content — `1fr` implies
`min-width:auto`, so rows need `minmax(0,1fr)`. Deferred by agreement: persisting and
rendering Pages blocks (backend + storefront), which is the next piece of work.

---

## Production deployment — Vercel (both apps)

Full runbook + env vars + DNS + troubleshooting table in `docs/DEPLOYMENT.md`. This decision records the load-bearing choices.

50. **Both apps deploy to Vercel as two projects from one repo; the backend is a serverless function on the `api.diecastbd.com` *subdomain*, not a bare `*.vercel.app` host.** Topology: `diecastbd.com` (apex, canonical) → frontend project (Root Directory `frontend`, Vite); `www` → 308 redirect to apex; `api.diecastbd.com` → backend project (Root Directory `backend`, Express serverless); DNS on Vercel nameservers (Vercel authoritative). **Four things had to be true, each learned by hitting the failure first:** (a) **jose ESM** — `firebase-admin` → `jwks-rsa` `require()`s ESM-only `jose@6` and crashes with `ERR_REQUIRE_ESM`; pinned via `overrides: { "jose": "^4.15.9" }` (CJS-compatible) in `backend/package.json`. (b) **Serverless entrypoint** — Vercel's modern `functions`/`rewrites` config left framework auto-detection on, which grabbed the Express app in `src/app.js` and demanded a default export (`Invalid export found in module src/app.js`); switched `backend/vercel.json` to legacy `builds`/`routes` pinning the single `api/index.js` handler (connect-DB-then-delegate), which disables auto-detection entirely — **do not re-add `functions`/`rewrites`**. As belt-and-suspenders `src/app.js` also `export default app` + a `process.env.VERCEL`-gated `connectDB()` middleware, so it's a valid handler even if served directly (local/tests unaffected — no `VERCEL` env). (c) **Same-site cookies** — auth is httpOnly cookies with `sameSite:"none"; secure:true`; with the backend on `die-castbd-backend.vercel.app` those were cross-site third-party cookies that browsers block (Safari always, Chrome increasingly) → **401 on every authed route** and the admin dashboard couldn't load. Fixed with zero code change by moving the backend to `api.diecastbd.com` (same registrable domain as the storefront → first-party cookies). `CLIENT_URL` stays the single canonical frontend origin (`https://diecastbd.com`) — it's used for CORS *and* sitemap/email links, so it can't be a list; a new optional `CORS_ORIGINS` env var adds extra credentialed-CORS origins (e.g. `http://localhost:5173`) without corrupting those links. (d) **Frontend lockfile** — `@fontsource-variable/*` were declared in `package.json` but unresolved in `package-lock.json`, failing `npm run build` (and Vercel `npm ci`); reconciled with `npm install`. Cron jobs run via Vercel Cron over HTTP to `/api/v1/cron/*` (guarded by `CRON_SECRET`), not `node-cron`. `VITE_` vars are build-time-inlined, so changing `VITE_API_BASE_URL` requires a frontend **rebuild**, not just a redeploy of the same build.

51. **Hero copy is admin-editable per variant, with the shipped design copy as the render fallback.** Previously only the hero's floating highlight card was editable; the badge/headline/subtitle/both CTAs/footnote were hardcoded per variant in `HeroSection.jsx`. Added `homepageSections.hero.content.{limeShowroom,darkSpotlight,photoFullbleed}` to the settings model + Zod validation — one content object per visual style (only the selected `variant` renders, but all three are editable so switching styles keeps each one's own copy). Every field is **optional**; `resolveCopy()` in `HeroSection.jsx` trims each value and falls back to that variant's `DEFAULTS` (the existing shipped copy) when blank, so an un-migrated/un-edited document — including live production, which has no `content` field yet — renders byte-identical to before. Verified against the live backend: production's contentless hero rendered the exact default copy. CTAs now use the editable `primaryCtaLink`/`secondaryCtaLink` (default `/shop`). **Mobile-first cleanup:** dropped the separate mobile/desktop copy strings (title stays two lines on desktop via a responsive `<br>`, one value everywhere) so admins edit one field, not two. **Alignment fix:** the hero hard-coded `max-w-[1440px]` while every other section uses `Container`'s `max-w-[1360px]` — 80px wider, so its edges bled past the sections below; changed to `1360px`, and normalized the dark-spotlight panel's oversized `md:p-[72px_64px]` to `md:p-16` (matching lime) and trimmed `photo-fullbleed` height `560px→520px` so the hero sits in proportion. The shared `highlightCard` path was deliberately left in place (not moved under `content`) to avoid orphaning the live card data. All new settings fields are additive/optional — safe for the running Vercel/Render deployment, but the backend must be redeployed for the new admin fields to persist.

52. **Every storefront text input renders at 16px on mobile to kill iOS Safari's zoom-on-focus; bottom-sheet modals gained a max-height + internal scroll.** The reported "typing breaks the whole layout" bug on mobile was iOS Safari's mandatory auto-zoom when a focused `<input>`/`<textarea>` has a computed `font-size < 16px` — it zooms the entire page and pans it sideways, leaving the site visibly "broken" and stuck zoomed. The custom storefront fields were all `text-[13.5px]` (and the auth fields `text-sm`/14px), so every one triggered it. Fix is CSS-only and mobile-scoped: each custom input/textarea now leads with `text-base` (16px) and restores its design size behind `md:` (`md:text-[13.5px]` / `md:text-sm`) — desktop is pixel-unchanged. Covered the four shared `inputCls` constants (AddressForm, AccountPage, checkout `parts.jsx`, ContactPage), the auth `authInputCls`, and the standalone inputs (ShopPage search, CartDrawer/cart CouponRow, footer NewsletterForm, RestockAlertDialog). The shadcn `Input`/`Textarea` primitives (admin) were already `text-base md:text-sm`, so admin was never affected. Separately, the two bottom-sheet modals (`ResponsiveModal`, `RestockAlertDialog` — the only two `side="bottom"` sheets) had `h-auto` with no height cap or scroll container, so with the keyboard open the lower form fields sat under it unreachable; added `data-[side=bottom]:max-h-[92dvh]` + `data-[side=bottom]:overflow-y-auto` at the `SheetContent` primitive level (scoped to the bottom side only — the right-side CartDrawer already scrolls its own item list and is untouched) plus `interactive-widget=resizes-content` on the viewport meta so the layout viewport shrinks to the visible area when the keyboard appears. Verified in the mobile preview: the shop search input computes to `16px` at 375px width. All changes are frontend-only CSS — no backend, no schema, safe for the live deployment (needs a frontend rebuild/redeploy to take effect since it's build-time CSS).

53. **Product images fill their placeholder (`object-cover`) instead of floating inside padding (`object-contain p-[X%]`).** The user reported product photos sitting inside visible whitespace across the storefront — the images were `object-contain` with percentage padding (`p-[4%]`–`p-[6%]`), which letterboxes the photo inside the box. Switched every product-photo surface to `object-cover` so the image fills the whole placeholder: `ProductCard`, `FeaturedSpotlight` (both images), `ProductGallery` main + thumbnails, `CartLineItem`, `WishlistCard`, `CheckoutSummary`, `OrderReceipt`, `RestockAlertDialog` summary, and the three `ShopByShelf` tiles. Two surfaces were intentionally left `object-contain`: the `ProductGallery` **lightbox** (full-screen zoom must show the whole piece uncropped) and the `PaymentMethods` **bKash QR** (cropping a QR breaks it). **ShopByShelf caveat:** its tiles render `brand.logo`/`category.image`; the current catalog stores framed promo/product imagery there so cover fills correctly, but a genuinely transparent brand *wordmark* logo would crop under cover — revisit if the brand-logo field is ever populated with a bare transparent logo. Also removed the now-dead `isLogo` prop from `ShopByShelf`'s `Tile`. Frontend-only CSS — no backend/schema change, safe for the live deployment; needs a frontend rebuild/redeploy. Verified: lint + build clean; in the running preview all 20 product images on the home page compute to `object-fit: cover` (0 `contain`) at both mobile 375px and desktop.

54. **The "Premium Shelf" homepage banner (`collectorPromise`) is now fully admin-editable — title, description, banner image, background color, text color, and CTA text/link — with the shipped design as the render fallback.** Previously only `title`/`description` were editable; the banner's dark background, white text, dashed empty-image placeholder, and "Shop featured" CTA were all hardcoded in `PremiumShelfBanner.jsx`. Extended `settings.collectorPromise` (model + Zod validation) with `image` (shared `imageSchema`), `bgColor`, `textColor`, `ctaText`, `ctaLink` — same "blank field falls back to shipped default" rule as decision #51's hero content. The banner image renders raw `image.url` with `object-cover` (like the hero, not run through `cloudinaryCard()`'s 1:1 pad — banner photography isn't square) with the existing dashed placeholder as the empty-state fallback. The fixed "THE PREMIUM SHELF" eyebrow label deliberately stays brand-lime regardless of `textColor` (a brand marking, not body copy). Admin form added a new `ColorField` component (native `<input type="color">` swatch synced to a hex `Input`) — the first color-picker pattern in the admin dashboard, reused for both `bgColor`/`textColor`; the image field reuses the existing `QrImageField` (generalized with optional `emptyLabel`/`uploadLabel` props, backward-compatible for its two existing bKash/BanglaQR call sites). All new settings fields are additive/optional — safe for the live Vercel/Render deployment, but backend must be redeployed for the new admin fields to persist. Verified: backend `npm test` (35/35) and frontend lint + build all clean; DOM-level inspection in the running preview confirmed the fallback path renders byte-identical to the pre-change design (`rgb(16, 18, 8)` background, white headline, dashed placeholder, default CTA/link) when `collectorPromise`'s new fields are unset — i.e., no regression against the current live document. The admin form itself could not be visually screenshotted (no admin credentials available in this environment to sign in against the live-pointed dev backend) — verified by code review only, reusing the same `Input`/`Textarea`/`Controller`/`QrImageField` primitives already proven working elsewhere on the same page.

55. **`cloudinaryCard()`'s product-card transform tried `c_fill,g_auto` (crop-to-cover) same-day, then reverted to `c_pad,b_white` (contain, never crop) after the user showed it was cutting real product content.** First pass (this entry, originally): switched `CARD_TRANSFORM` from `c_pad,b_white,ar_1:1,w_800` to `c_fill,g_auto,ar_1:1,w_800` so the photo fills the 1:1 placeholder in both dimensions with no letterbox bars — verified by pixel-sampling a 900×600 source (134px white band top/bottom under `c_pad` → 0px under `c_fill,g_auto`). **Reverted the same day**: the user then showed screenshots of blister-pack product cards (e.g. "HW Fast & Furious — 2021 Toyota GR Supra", "Card Protector — Top-Open Gen-2 Shell") where `c_fill`'s crop was cutting into the actual retail packaging photo — text clipped at the frame edge (e.g. "FURIOUS" → "URIOUS"). Root cause: this catalog's product photos aren't studio glamour shots with disposable background margin — they're photos *of the retail package itself* (blister card/box), shot at a landscape 3:2 framing (900×600, 1500×1000, 1536×1024) or square (checked 11 live catalog images: 5 were 3:2 landscape, 6 were exactly square — **zero were portrait**), so `c_fill`'s crop has no "safe" margin to trim and always eats into real package content regardless of `g_auto`. Reverted `CARD_TRANSFORM` back to `c_pad,b_white,ar_1:1,w_800` (contain — never crops; pads white, which blends with the product-card container's own white background). **Trade-off now explicit and accepted:** since ~half the catalog's source photos are 3:2 landscape, not square, `c_pad` still letterboxes those with ~134px top/bottom white bars at 800×800 — there is no crop/pad choice that eliminates both padding *and* cropping for a mixed-aspect-ratio catalog being forced into a square placeholder; the two options are "guaranteed full photo, occasional bars" (current) vs. "guaranteed full bleed, occasional clipped content" (the reverted attempt). Chose the former because hiding part of the actual product (what the buyer receives) is worse than a white margin. Affects the one shared `cloudinaryCard()` helper and therefore all 8 call sites (`ProductCard`, `FeaturedSpotlight`, `ProductGallery` main+thumbnails, `CartLineItem`, `WishlistCard`, `CheckoutSummary`, `OrderReceipt`, `RestockAlertDialog`); the `ProductGallery` **lightbox** is unaffected either way (renders the raw untransformed `url`). Frontend-only, no backend/schema change, no redeploy needed. Verified: `npm run lint` clean (pre-existing warnings only) both before and after the revert.

56. **Retired the forced 1:1 Cloudinary pad entirely — `cloudinaryCard()` now only caps width (`c_limit,w_800`), and every consumer uses `object-contain` in a box with a matching background, so letterboxing happens at the CSS layer instead of being baked into the pixels.** Two more rounds of user screenshots after decision #55: (1) a wide product card ("HW F1 Gold Label — Ferrari SF-25") still showed real photo content cropped — because decision #55's `ar_1:1` square pad was still being fed through `object-cover` on 4 non-square boxes (`ProductCard`, `FeaturedSpotlight`'s Spotlight, `WishlistCard`, `ProductGallery`'s main image), and CSS `cover` crops a square source to fit a wide/tall box just as surely as Cloudinary's own `c_fill` did — fixed by switching those 4 to `object-contain`. (2) Immediately after, a different card (MINI GT #1106, a 1500×1000 3:2 source) showed the *opposite* symptom — a visible white gap below the image, "height not filling" — because decision #55's `c_pad,ar_1:1` bakes white letterbox bars into the pixels for every non-square source (confirmed: this source's raw is 1500×1000, its `cloudinaryCard()` output is a literal 800×800 square with the car photo only occupying the vertical band Cloudinary's own pad left it); the square placeholder always fills its CSS box, but the photo *inside* it visibly doesn't, which reads as a layout bug even though the box-level math is correct. Root cause: **two independent fit/pad layers stacked on top of each other** (Cloudinary's `c_pad,ar_1:1` baking a square+bars into the bytes, then CSS `object-fit` fitting *that* into the card's actual box) — the fix is to only ever pad once, at the layer that has the real box dimensions (CSS), not at the layer that doesn't (Cloudinary, which only knows a fixed 1:1 target). Changed `CARD_TRANSFORM` from `c_pad,b_white,ar_1:1,w_800` to `c_limit,w_800` — caps delivered width, keeps the source's native aspect ratio, never crops, never pads. Switched the remaining `object-cover` consumers of `cloudinaryCard()` to `object-contain` for the same reason as the 4 above, now that the source is no longer guaranteed square: `FeaturedSpotlight`'s `RowCard`, `CartLineItem`, `CheckoutSummary`, `OrderReceipt`, `RestockAlertDialog`, and the `ProductGallery` thumbnail strip — all 9 `cloudinaryCard()` call sites are now `object-contain`, each in a box with a background color (`bg-white`/`bg-tile`/`bg-brand-tint`) so any CSS-level letterboxing blends in rather than showing a hard edge. This finally satisfies the user's explicit ask ("full height of the image, width could be flexible"): height is now always the box's height (never less), width adapts to whatever the box's own aspect ratio allows, and nothing is ever cropped. Frontend-only, no backend/schema change, no redeploy needed. Verified: `npm run lint` clean (pre-existing warnings only), `npm run build` clean, frontend `npm test` (17/17); visually confirmed in the running preview at 390×844 and 1280×900 that the MINI GT #1106 card's photo now fills its box's full height with no gap, and the wide Spotlight/gallery surfaces show no cropping.

57. **All 9 `cloudinaryCard()` consumers switched from `object-contain`/`size-full` to explicit `max-h-full w-auto max-w-none mx-auto block`, per the user's explicit instruction, trading decision #56's "never crop" guarantee for "never gap."** Even after #56 eliminated the letterbox-in-pixels bug, small vertical gaps remained possible under `object-contain` whenever a box's aspect ratio was wider than the photo's (a contain-fit image never exceeds the box in either dimension, so a wide box under a 3:2 or square photo still shows empty space beside/below it depending on which dimension is the constraint). The user showed a DevTools screenshot of an `img, video { max-height: 100%; ... }` rule and gave an explicit instruction: "In product image always use max-height 100% & remove any max width & image position will be always in center position." I flagged the resulting trade-off before implementing — this approach guarantees the box's full height is always filled (no gaps), but sacrifices #56's "never crop" property: a box whose aspect ratio is *narrower* than the photo's native ratio will now crop the image's sides, since width is only capped by the (now height-driven) intrinsic scaling, not by the box width. The user's explicit response was "do what i say," overriding the caution. Implemented by replacing each consumer's className with `mx-auto block max-h-full w-auto max-w-none` (height fills the box, width auto-scales off the image's own intrinsic ratio, `mx-auto` centers any resulting horizontal overflow, `block` avoids inline-image whitespace/baseline gap) across all 9 call sites: `ProductCard` (grid+carousel), `WishlistCard`, `FeaturedSpotlight` (`Spotlight` + `RowCard`), `ProductGallery` (main image + thumbnail strip — the separate full-res lightbox `<img>` was deliberately left on `object-contain`, since it's meant to show the whole image at its natural proportions rather than fill a fixed box), `CartLineItem`, `CheckoutSummary`, `OrderReceipt`, `RestockAlertDialog`. One case needed extra verification: `CartLineItem`'s `Thumb` wraps the image in a `<Link>` (`<a>`, normally `display: inline`, which would ignore explicit sizing) — confirmed via live DOM inspection that it computes as `display: block` anyway because it's always a direct child of a `flex` container in both the mobile and desktop rows (CSS flex-item blockification), so no extra class was needed there. Every affected box already has `overflow-hidden` (either on itself or on a same-width ancestor) and a definite (non-auto) height, so any new horizontal overflow from `w-auto max-w-none` clips cleanly at the box's rounded corners rather than bleeding outside the card. `CARD_TRANSFORM` (`c_limit,w_800`, from #56) is unchanged — this is a CSS-only change, orthogonal to the Cloudinary transform. Frontend-only, no backend/schema change, no redeploy needed. Verified: `npm run lint` clean (pre-existing warnings only), `npm run build` clean, frontend `npm test` (17/17 passing).

**Same-day correction #1 — `mx-auto` doesn't center overflowing content; switched to `object-fit: cover` for true center-cropping.** The user reported (ProductDetailPage screenshot, MINI GT #1106) that the image was anchored to the left edge with all the crop happening on the right, not centered as instructed. Root cause: per the CSS2.1 spec (§10.3.3), when a block box's content is *wider than its containing block*, `auto` margins resolve to `0` rather than splitting the overflow evenly — `margin: auto` only centers content that fits; it cannot center-crop overflowing content. That's exactly what `mx-auto` was being asked to do once `max-w-none` let the image grow wider than its box. Fixed by replacing `mx-auto block max-h-full w-auto max-w-none` with `size-full object-cover object-center` at all 9 call sites — `object-fit: cover` scales the image to fully cover a box with known width+height (`size-full`, valid here since every parent box has a definite width, either explicit or via flex-stretch, and a definite height), and `object-position: center` (Tailwind `object-center`, redundant with the default but kept explicit) crops symmetrically from both sides rather than anchoring to one edge. This is the standards-compliant technique for "always fill the box, crop is centered," and delivers exactly what the user asked for without the margin-auto edge case. Verified: `npm run lint` clean, `npm run build` clean, frontend `npm test` (17/17 passing).

**Same-day correction #2 — `object-cover` can still crop the *height*, not just the width; switched to height-driven intrinsic scaling inside a centering flex box so height is never cropped, only width.** The user then posted a 3-card grid screenshot (Hot Wheels Premium blister packs — Cyberpunk 2077, Racing Bulls #6, Ferrari SF-25) explaining the real problem: these product photos are shot **portrait** (tall packaging card), while the product-card image box is **landscape** (wider than tall). `object-fit: cover` picks whichever scale factor makes the image cover *both* dimensions — for a portrait photo in a landscape box, that means scaling to match the box's width, which then makes the scaled photo *taller* than the box, so `cover` crops it top/bottom (cropping the height) to fit — the opposite of what "full height should always be visible" means. The user's ask was specific: "full height should be view inside the product card... width could crop it's not matter about the width" — i.e. height must be pinned exactly to the box height (never cropped, never gapped), and only the width is allowed to overflow/crop, symmetrically centered. `object-fit` (cover or contain) cannot express "pin one axis exactly, let the other float freely" — both modes derive a single scale factor from *both* dimensions together. Replaced it with plain replaced-element sizing: `h-full w-auto max-w-none` on the `<img>` (height locked to the box's own height; width derives from the image's intrinsic aspect ratio with no cap) inside a parent box that gained `flex items-center justify-center` (added to its existing `relative overflow-hidden` classes). Flexbox's `justify-content: center`, unlike block-level `margin: auto`, correctly centers a child even when that child is wider than the flex container — the excess splits evenly on both sides and is clipped symmetrically by the box's `overflow-hidden`, rather than zeroing out to one edge (the bug fixed in correction #1). Net effect across all 9 call sites (same list as decision #57's body): the box's height is now *always* fully occupied by real photo pixels (matching the earlier "HW F1 Gold Label" / "MINI GT #1106" full-height asks), and only width is ever sacrificed, centered. `CartLineItem`'s `Thumb` (`<Link>`/`<a>`) now explicitly declares `flex` itself, which also sidesteps the earlier flex-item-blockification reasoning entirely — an explicit `display: flex` needs no help from being a flex child to become a block-level box. Verified: `npm run lint` clean, `npm run build` clean, frontend `npm test` (17/17 passing).

**Same-day follow-up — `ProductCard`'s image box height increased to 300px on desktop (grid + carousel variants), mobile scaled up proportionally to preserve each variant's existing aspect ratio.** With height now the pinned, always-fully-visible axis (per correction #2), a taller box shows more of each portrait product photo before any width-cropping kicks in — the user asked for a taller box: "for carousel & store product card use 300px height for web same ratio increase the height for mobile as well." Changed `VARIANTS.grid.img` in `frontend/src/components/shared/ProductCard.jsx` from `h-[150px] md:h-[220px]` to `h-[205px] md:h-[300px]` (desktop 220→300, mobile scaled by the same ratio: 300 × 150/220 = 204.5 → 205px) and `VARIANTS.carousel.img` from `h-[172px] md:h-[240px]` to `h-[215px] md:h-[300px]` (desktop 240→300, mobile: 300 × 172/240 = 215px exactly). Only `ProductCard`'s two variants were touched — this request was specifically about the store grid and carousel cards, not the other 7 `cloudinaryCard()` surfaces (wishlist, spotlight, gallery, cart, checkout, receipt, restock dialog), which keep their existing box sizes. Verified: `npm run lint` clean, `npm run build` clean, frontend `npm test` (17/17 passing).

58. **Pre-order product fields (Phase 1 of the pre-order/payment-options feature) — `isPreOrder`/`preOrderStartDate`/`preOrderEndDate` added to Product, with an auto-expiring `isPreOrderActive` virtual driving every badge/CTA instead of the raw flag.** First of a 5-phase plan (decisions #58–#62) for merchant-requested pre-order + per-product payment-option + per-zone delivery-prepay features. `product.model.js` gains `isPreOrder: Boolean` (default `false`), `preOrderStartDate`/`preOrderEndDate: Date` (default `null`), plus a new virtual — `isPreOrderActive = isPreOrder && (!preOrderEndDate || preOrderEndDate >= now)` — computed at read time, following the exact same "derive, never store" convention already established by `availableStock`/`profitMargin` in this file. The virtual, not the raw `isPreOrder` boolean, is what every frontend surface reads: once `preOrderEndDate` passes, the badge disappears on its own with zero cron job or admin follow-up action required. `product.validation.js` gains the three fields on `productFields` plus a new `withProductRefinements(schema)` wrapper applied to both `createProductSchema` and `updateProductSchema`, with one refine so far — `preOrderEndDate >= preOrderStartDate` when both are present — deliberately structured as an extensible wrapper (not an inline one-off refine) because Phase 2 adds two more cross-field rules to the same chain rather than duplicating it across schemas. On the frontend, `productSchema.js`/`ProductForm.jsx` add a "Pre-order" admin section (a `Switch` for `isPreOrder`, two `type="date"` inputs) using the same `toDateInputValue()` ISO-string round-trip convention already established in `CouponsPage.jsx`, so no new date-handling pattern was introduced. Four badge-consumer surfaces — `ProductCard.jsx`, `WishlistCard.jsx`, `ProductDetailPage.jsx`, `FeaturedSpotlight.jsx` — each render a "Pre-order" badge/tag off `product.isPreOrderActive`, reusing each file's existing local tone-based `Badge` component with zero changes to `Badge` itself (a new tone string just falls into the existing default/else branch); `ProductDetailPage`'s Buy Now CTA also swaps its copy to "Pre-order now" when active. All fields are additive with safe defaults (`isPreOrder: false`), so every existing product/order continues to behave and render exactly as before. Verified: backend `npm test` (35/35 across 6 files); frontend `npm run lint` clean (only 4 pre-existing unrelated warnings) and `npm run build` clean.

59. **Per-product payment options (Phase 2) — `paymentOptions[]`/`advancePaymentPercent` added to Product, a mutual-exclusion refine blocks `cod`+`partialAdvance` together, and a new `paymentPlan.service.js` module holds the shared payment-resolution math ahead of Phase 4's checkout wiring.** `product.model.js` gains `paymentOptions: [String]` (enum `cod`\|`deliveryOnly`\|`partialAdvance`\|`full`, default `["cod","full"]` — zero behavior change for the existing catalog) and `advancePaymentPercent: Number` (1-100, default `null`, meaningful only when `partialAdvance` is selected). `product.validation.js` extends the Phase 1 `withProductRefinements(schema)` chain (rather than duplicating refines) with two more rules: (a) `advancePaymentPercent` must be present and in 1-100 when `paymentOptions` includes `partialAdvance`; (b) `paymentOptions` may never contain both `cod` and `partialAdvance` simultaneously — this is the confirmed design decision that partial-advance products can't also offer plain COD, since COD would let a customer skip the required advance entirely. The frontend `productSchema.js` mirrors both refines client-side for immediate form feedback (server stays authoritative — the two schemas are kept in sync manually, there's no shared-schema mechanism between the two apps). `ProductForm.jsx` renders a new "Payment options" section: four `Switch` toggles (one per option, following the same toggle pattern as `isFeatured`/`isPreOrder` rather than introducing a new Checkbox primitive, since none existed in `components/ui/`) driven by a single `Controller`-managed array field, with the `cod` toggle disabled client-side whenever `partialAdvance` is checked (visible enforcement of the mutual-exclusion rule, not just a submit-time error) and a conditionally-rendered `advancePaymentPercent` number input that only appears when `partialAdvance` is selected. New `backend/src/modules/orders/paymentPlan.service.js` exports `resolveItemPaymentRequirement(product)` → `{ allowsCod, allowsDeliveryOnly, allowsFull, requiresAdvance, advancePercent }`, a pure function with no DB access, mirroring `coupon.service.js`'s shared-math-module pattern so the same resolution logic can be reused by cart preview, checkout, and order creation without drifting. **This module is created and unit-tested now but is completely inert until Phase 4** wires `assertPaymentMethodAllowed`/`calculateAmountPaid` into `buildAndSaveOrder` — it is not imported by any request-handling code yet. Verified: backend `npm test` (42/42 across 7 files, including 7 new table-driven tests for `resolveItemPaymentRequirement` covering the no-options default, empty-array default, single-option products, stale-percent-clearing, and the all-four-options case); manual validation-schema execution (`createProductSchema.body.safeParse(...)` run directly via `node -e`, standing in for curl since no admin auth token is available in this sandbox — same limitation noted in decision #54) confirmed both refine rejections (`cod`+`partialAdvance` together; `partialAdvance` without a percent) and two acceptances (a valid `partialAdvance`+`full`+15% combo; the no-`paymentOptions`-given case correctly defaulting to `["cod","full"]`). Frontend `npm run lint` clean (same 4 pre-existing unrelated warnings) and `npm run build` clean.

60. **Per-zone outside-Dhaka delivery-prepay requirement (Phase 3) — `requiresPrepay` boolean added to `Settings.shippingZones[]`, deliberately per-zone rather than a hardcoded "Dhaka" string match.** The merchant wants shipments outside Dhaka to optionally require prepaying the delivery charge before an order is placed, to cut down on COD delivery refusals for out-of-city orders. Rejected a global "is this zone Dhaka" string check because `shippingZoneSchema.name` is admin-renamable free text (an admin could rename "Outside Dhaka" to "Countrywide" or add a third zone like "Chittagong Metro"), so a name-based check would silently stop working on rename with no error — a per-zone boolean field is robust to any renaming/reorganizing of zones and requires zero special-casing of any particular zone name. `settings.model.js`'s `shippingZoneSchema` gains `requiresPrepay: Boolean` (default `false` — every existing zone keeps today's exact checkout behavior until an admin explicitly opts a zone in); `settings.validation.js`'s matching `shippingZone` Zod object gains `requiresPrepay: z.coerce.boolean().optional()`. `SettingsPage.jsx`'s existing `shippingZones` `useFieldArray` section gains a `Switch` (via `Controller`, same toggle pattern used throughout this file for `homepageSections.*.enabled`) per zone row labeled "Require prepaying the delivery charge before placing an order"; the `append()` default for a new zone now includes `requiresPrepay: false` explicitly. No frontend Zod schema exists for this admin form today (`SettingsPage.jsx` uses plain `useForm({ values: settings })` with no `zodResolver`), so there was nothing to mirror client-side — the backend schema is the only validation layer, consistent with how every other Settings field already works. Like Phase 2's `paymentPlan.service.js`, this field is **completely dormant** — nothing reads `requiresPrepay` yet; it starts being enforced only when Phase 4 wires `zoneRequiresPrepay` into `buildAndSaveOrder`'s payment-plan resolution. Verified: backend `npm test` (42/42, unchanged from Phase 2 — no new backend logic to unit-test yet, this phase is pure schema plumbing); manual validation-schema execution (`updateSettingsSchema.body.safeParse(...)` via `node -e`, same curl-substitute reasoning as decision #59) confirmed a zone with `requiresPrepay: true` parses correctly and a zone omitting the field parses with it simply absent (no forced default at the Zod layer — the Mongoose schema default of `false` is what existing persisted zones fall back to on read). Frontend `npm run lint` clean (same 4 pre-existing warnings) and `npm run build` clean.

61. **Checkout enforcement (Phase 4, the integration phase) — `paymentPlan.service.js` gains `assertPaymentMethodAllowed`/`calculateAmountPaid`, wired additively into `buildAndSaveOrder` between the existing `reserveStockForItems(...)` call and `Order.create([...])`; `reserveStockForItems`, `transitionOrderStatus`, and `stockBucket` were not touched.** `assertPaymentMethodAllowed({ normalizedItems, paymentOption, zoneRequiresPrepay })` checks the zone's `requiresPrepay` flag first (an outside-Dhaka prepay zone forbids `"cod"` outright, regardless of any product's own config), then loops every cart item and requires each one's `resolveItemPaymentRequirement(product)` to allow the chosen `paymentOption` — deliberately has **no special-case "mixed cart" detection**: a cart mixing a cod-only item with a partialAdvance-required item is rejected no matter which of the two options is picked, because whichever one is chosen, the *other* item in the loop fails its own check, giving one clear rejection message rather than a silent split (the confirmed design decision from this feature's kickoff). `calculateAmountPaid({ normalizedItems, subtotal, total, shippingFee, paymentOption })` returns `{ amountPaid, amountDue, advancePaymentPercent }`: `full` collects the entire `total` upfront, `cod` collects nothing upfront, `deliveryOnly` collects `min(shippingFee, total)`, and `partialAdvance` collects `round(subtotal × percent / 100)` capped at `total` — where `percent` is the **max** `advancePaymentPercent` across every partialAdvance-requiring item in the cart, so a cart with differently-configured products never under-collects relative to any single item's own configured rate. `order.model.js` gains `paymentOption`, `advancePaymentPercent`, `amountPaid`, `amountDue` — each with a Mongoose function default (`default: function() { ... this.paymentMethod ... this.total ... }`) reconstructing a semantically-correct value from the two pre-existing fields, so every historical pre-Phase-4 order retroactively satisfies `amountPaid + amountDue === total` the moment it's read, with zero migration script: `paymentMethod === "cod"` backfills to `paymentOption: "cod"`/`amountPaid: 0`/`amountDue: total` (nothing was ever collected upfront under the old flow), anything else backfills to `paymentOption: "full"`/`amountPaid: total`/`amountDue: 0` (the pre-existing manual bKash/BanglaQR proof flow always collected the full amount). Each default function is self-contained (reads `this.paymentMethod`/`this.total` independently) rather than relying on Mongoose's field-declaration-order guarantees across function defaults. `order.validation.js`'s `createOrderSchema.body` gains `paymentOption` (optional, default `"cod"` — an old/unaware frontend build that never sends this field keeps behaving exactly as before) plus a fourth refine: `paymentOption === "cod" || paymentMethod !== "cod"`, rejecting the combination of "an upfront-payment option" with "a payment channel that has no way to collect it upfront". `order.service.js`'s `buildAndSaveOrder` now also captures `zoneRequiresPrepay` off the same `Settings.shippingZones.find(...)` lookup already used for the shipping-fee snapshot (no second DB read), calls the two new payment-plan functions right after `total` is computed, and threads a new `paymentOption` param through `buildAndSaveOrder`/`createOrderFromCart`/`createOrderFromItems`/`order.controller.js`'s `createOrder` — every other line of the stock reservation/transition machinery is byte-for-byte unchanged. `cart.controller.js`'s `serializeCart()` product-population `select` list gained `paymentOptions`/`advancePaymentPercent` (previously omitted), since the frontend checkout page needs them to preview payment-option availability for a logged-in customer's server-backed cart, not just guest/Buy Now carts (whose product objects already carried every field straight from the product-detail API). On the frontend: `checkoutSchema.js` mirrors the same `paymentOption` field + refine as a client-side preview (server stays authoritative); a new `frontend/src/features/checkout/lib/paymentPlanPreview.js` is a deliberately-duplicated **read-only preview mirror** of `resolveItemPaymentRequirement`/`calculateAmountPaid` (the two apps are separate packages with no shared code) — `resolvePaymentOptionAvailability({ items, zoneRequiresPrepay })` computes, per the same "every item must allow it" rule as the backend, which of the 4 options the whole cart can use, plus a `codDisabledReason` message. `PaymentMethods.jsx` becomes cart-aware: the COD radio card renders disabled with the inline reason when `codDisabled` (per the requirement's explicit "disabled or display a message" wording) instead of just erroring on submit; a new `PaymentOptionPicker` segmented control appears inside the bKash/BanglaQR panels *only* when the cart's items actually offer a choice among delivery-only/advance/full, showing each option's preview pay-now/due-on-delivery amounts. `CheckoutPage.jsx` computes `paymentPlan`/`nonCodOptions` from the live cart + selected zone on every render and a `useEffect` keeps the form's `paymentOption` in sync (forces `"cod"` when the COD channel is chosen and allowed, auto-switches the payment channel off COD entirely if a cart change makes COD newly disallowed, and auto-picks the first available non-cod option otherwise) so the customer is never left with a stale/invalid selection. `DeliveryOptions.jsx` shows a small "Delivery charge must be paid upfront" note under any zone with `requiresPrepay: true`. **Deployment note:** default `paymentOption: "cod"` + the historical-backfill function defaults mean no currently-placeable order type is blocked for unmodified products/zones — recommend deploying backend first, verifying via the same schema-execution scripts used below, then frontend. Verified: backend `npm test` (57/57 across 8 files) — **explicitly confirmed `order.stockBucket.test.js` (the stock-bucket classification regression guard) still passes unmodified**; added 15 new table-driven tests in `paymentPlan.checkoutEnforcement.test.js` covering pure-cod, pure-full, deliveryOnly, partialAdvance-only, the max-percent-across-mixed-items case, both directions of mixed-cart rejection (partialAdvance-required item + cod chosen, and vice versa), zone-forced-prepay overriding an otherwise-cod-allowed product, and an amountPaid-never-exceeds-total edge case; a `node -e` script constructing `new Order({...})` documents (no DB connection needed, since Mongoose applies schema defaults on construction) confirmed the historical-backfill defaults produce the exact expected `{paymentOption, amountPaid, amountDue}` for both a `cod` and a `bkash` legacy order, and that explicitly-provided Phase-4 values are never overridden by the defaults; a `createOrderSchema.body.safeParse(...)` script (curl-substitute, same reasoning as decisions #59/#60) confirmed the `cod`+non-`cod`-option rejection, and acceptance of `bkash`+`partialAdvance`/`bkash`+`deliveryOnly`. Frontend `npm run lint` clean (same pre-existing warnings, no new ones) and `npm run build` clean.

62. **Order/receipt/email display of payment-plan fields (Phase 5, the final phase of the pre-order/payment-options feature — wraps up decisions #58–#61).** Purely a display layer: no schema, validation, or checkout-logic changes — every field surfaced here (`paymentOption`, `advancePaymentPercent`, `amountPaid`, `amountDue`) already exists on the Order document and was already present in every admin/customer order API response (full-document serialization, no projection), so this phase only teaches the existing views to render it. `frontend/src/features/admin/orders/OrderDetailPage.jsx`: added `PAYMENT_METHOD_LABELS`/`PAYMENT_OPTION_LABELS` maps and a payment-details block showing the payment channel (fixing a pre-existing display bug where `paymentMethod === "banglaqr"` incorrectly rendered as "bKash" — the ternary only ever checked for `"cod"`), the bKash transaction ID or BanglaQR reference (whichever applies), the human-readable `paymentOption` (with `advancePaymentPercent` shown for partial-advance orders), and a "Paid X · Due on delivery Y" line when `amountDue > 0`. `frontend/src/features/orders/components/OrderReceipt.jsx` (shared by the guest-safe order-confirmation page and the authenticated customer order-detail page — the two intentionally never drift): desktop `SummaryCard` gains a payment-option row and a "Due on delivery" row when `amountDue > 0`; the mobile-only inline total block in `OrderItems` (the only summary mobile ever sees, since `SummaryCard` is `hidden md:block`) gains the same due-on-delivery note so mobile customers aren't missing this information. `backend/src/emails/orderConfirmation.js`: added the same `PAYMENT_METHOD_LABELS`/`PAYMENT_OPTION_LABELS` maps (duplicated, not imported — the email module is a standalone Resend-facing renderer with no shared-code path to the frontend, consistent with the existing `paymentPlanPreview.js` duplication rationale from decision #61), fixed the same cod-vs-bkash-only ternary bug, appended the resolved payment option in parentheses when it isn't `"cod"`, and added an amber-highlighted "You've paid X — the remaining Y is due on delivery" block that only renders when `amountDue > 0` (so a full-payment or COD order's email is byte-for-byte unchanged from before this phase). Verified: backend `npm test` still 57/57 (no backend logic changed, this confirms the email template edit didn't break module loading — `node --check` on the edited file plus the full Vitest suite); frontend `npm run lint` clean (identical 4 pre-existing warnings, no new ones) and `npm run build` clean. No new curl/schema-execution verification was needed since no request/response contract changed — this phase is additive rendering of already-flowing data.

63. **Zone-forced-prepay bug fix + prepay-notice UX pass (post-Phase-4 bug fix, reported against the live checkout with a zone that has `requiresPrepay: true`).** A customer in an outside-Dhaka (`requiresPrepay: true`) zone correctly saw COD disabled, but the bKash/BanglaQR `PaymentOptionPicker` then offered **only "Full payment"** — forcing prepayment of the *entire order total* rather than just the delivery charge, which is the actual point of the zone requirement. Root cause: `assertPaymentMethodAllowed` (backend `paymentPlan.service.js`) and its frontend preview mirror `resolvePaymentOptionAvailability` (`paymentPlanPreview.js`) only ever used `zoneRequiresPrepay` to *block* `"cod"` — never to *expand* `"deliveryOnly"` availability for products that don't themselves list `deliveryOnly` in their own `paymentOptions`. Fix (mirrored in both files, per the established shared-math-duplication pattern): the per-item `deliveryOnly` check is now `requirement.allowsDeliveryOnly || zoneRequiresPrepay` — a zone that forces prepay is *itself* satisfied by paying just the delivery charge, so `deliveryOnly` must always be allowed in that case regardless of the product's own config (this doesn't touch `partialAdvance`/`full`/`cod` branches, which were already correct). Two new backend tests added to `paymentPlan.checkoutEnforcement.test.js`: one confirming `deliveryOnly` is now allowed for a `["cod","full"]` product when `zoneRequiresPrepay: true`, and one confirming it's still correctly rejected for the same product when the zone does *not* force prepay (guards against over-widening the fix). No `CheckoutPage.jsx` selection-logic change was needed — `nonCodOptions` is already built in the fixed order `["deliveryOnly","partialAdvance","full"]`, so `deliveryOnly` naturally becomes the default-selected option once available. Alongside the logic fix, addressed a UX complaint bundled in the same report: (a) `DeliveryOptions.jsx`'s zone radio cards used `items-center` on a flex row wrapping multi-line content (zone name + eta + prepay tag), visually mis-centering the radio dot against the whole block instead of the first line — changed to `items-start` (the alignment `RadioDot`'s own `mt-px` nudge is designed for) and upgraded the zone's prepay indicator from plain muted text to a visible amber pill badge (`Wallet` icon + "Delivery charge paid upfront", `#FFF4E5`/`#9A5B13`) so the requirement doesn't get lost among the eta/fee copy. (b) The same `items-center`-on-multiline-content risk was proactively fixed in `PaymentMethods.jsx`'s COD `RadioCard` and the shared bKash/BanglaQR `Head` component, for consistency. (c) Added a new prominent `PrepayNotice` callout at the top of `PaymentMethods.jsx` (reusing the same amber palette as the Phase-5 email's "balance due" block: `#FFF7ED` background/`#FED7AA` border/`#9A3412` text) shown whenever `zoneRequiresPrepay` is true, explicitly telling the customer to pick bKash/BanglaQR and select "Delivery charge only" and naming the exact amount — the pre-existing inline `codDisabledReason` text on the COD card explains *why* COD is off, but had no actionable next step; this new banner supplies it. Required threading two new props, `zoneRequiresPrepay`/`shippingFee`, from `CheckoutPage.jsx` into `PaymentMethods.jsx` (both values already computed there for other purposes, no new derivation needed). (d) Reconciled a pre-existing bKash-vs-BanglaQR display asymmetry noticed while in this code: the "pay now" amount note was gated behind `showPicker` (i.e. only shown when the cart offered more than one non-cod option) for bKash but shown unconditionally for BanglaQR — now shown unconditionally for both, since even a cart with only one available option benefits from a customer being told the exact amount to pay before scanning a QR. Verified: backend `npm test` 59/59 (57 pre-existing + 2 new, all green, `order.stockBucket.test.js` unaffected); frontend `npm run lint` clean (same 4 pre-existing unrelated warnings, no new ones) and `npm run build` clean.

64. **Zone-requiresPrepay narrowed to a cod-only-item safety net, not a blanket override (superseding part of decision #63/#61).** Follow-up merchant clarification: once a product has been explicitly configured with *any* non-cod business option (`deliveryOnly`, `partialAdvance`, or `full` — including the ordinary default `["cod","full"]`), that product's own configuration is trusted to already manage payment risk, and the zone's `requiresPrepay` flag must NOT override it — the customer can still choose plain COD for that item even in a zone that "requires prepay", and the zone must NOT force-allow `deliveryOnly` for it either. The zone-level force is now scoped to **cod-only items specifically** — a product whose `paymentOptions` resolve to allowing COD and nothing else (no `deliveryOnly`, no `partialAdvance`, no `full`) — since that's the one case where the merchant has given the customer no built-in way to pay anything upfront, and the zone's "cut down on COD refusals" goal genuinely needs a fallback. `paymentPlan.service.js` (`assertPaymentMethodAllowed`) and its frontend mirror (`resolvePaymentOptionAvailability`) both gained an `isCodOnlyRequirement(requirement)` helper (`allowsCod && !allowsDeliveryOnly && !requiresAdvance && !allowsFull`) and now compute a **per-item** `zoneForcesThisItem = zoneRequiresPrepay && isCodOnlyRequirement(requirement)` inside the existing per-item loop, rather than the previous single cart-level `if (zoneRequiresPrepay && paymentOption === "cod") throw` check up front. This means: `cod` is allowed for an item unless that specific item is cod-only AND the zone forces prepay; `deliveryOnly` is allowed for an item if the product supports it OR that specific item is zone-forced. A mixed cart (e.g. one cod+full item plus one cod-only item) shipped to a prepay zone with `paymentOption: "cod"` still correctly rejects, because the cod-only item's own per-item check fails even though the other item's wouldn't — consistent with the project's existing "no special-case mixed-cart detection, the per-item loop naturally rejects" pattern. The frontend preview's `resolvePaymentOptionAvailability` now also returns a `zoneForcesPrepay` boolean (true only if `zoneRequiresPrepay` AND at least one cart item is cod-only) which `CheckoutPage.jsx` passes to `PaymentMethods.jsx`'s `zoneRequiresPrepay` prop instead of the raw zone setting — so the `PrepayNotice` banner (added in decision #63) only appears when the requirement is actually binding for the customer's current cart, not whenever the raw zone flag happens to be on. Rewrote the zone/prepay test block in `paymentPlan.checkoutEnforcement.test.js` to match: cod is now allowed for a cod+full product even under a forcing zone; deliveryOnly is now rejected for a cod+full product even under a forcing zone (the force no longer applies to it); a cod-only product still has cod blocked and deliveryOnly force-allowed under a forcing zone; a new mixed-cart test confirms a cod+full item alongside a cod-only item still correctly rejects `cod` in a forcing zone. Verified: backend `npm test` 63/63 (59 prior + 4 net-new/rewritten in this block); frontend `npm run lint`/`npm run build` clean (same 4 pre-existing unrelated warnings).

65. **Reverted the decision-#63 zone-prepay pill badge on `DeliveryOptions.jsx` — direct merchant rejection of the visual design.** After shipping the amber `Wallet`-icon pill badge (`rounded-full`, `#FFF4E5` background, `#9A5B13` text, "Delivery charge paid upfront") added in decision #63, the merchant's explicit feedback on a screenshot of the live zone card was that it "look[s] wired [weird] ... i don't like it at all" — a direct rejection of that specific treatment, not the underlying information (the prepay requirement still needs to be visible; only *how* it's shown was disliked). Replaced the pill/background-box treatment with plain inline text matching the existing eta line's typographic weight (no `rounded-full`, no background fill, no padding box) — a small `Wallet` icon plus "Prepay delivery charge" at `text-[11.5px] font-semibold`, colored `#9A3412` to match the `PrepayNotice` banner's amber text in `PaymentMethods.jsx` (previously the zone card used a *different* amber pair, `#FFF4E5`/`#9A5B13`, than the payment-section banner's `#FFF7ED`/`#FED7AA`/`#9A3412` — this fix also reconciles that inconsistency by adopting the banner's `#9A3412` text color instead of introducing a third shade). The underlying `zone.requiresPrepay` logic (decisions #61/#63/#64) is completely unchanged — this is a pure visual/copy edit to one `<div>` in `DeliveryOptions.jsx`, no backend, validation, or test changes. Verified: frontend `npm run lint` clean (same 4 pre-existing unrelated warnings, no new ones); visual review of the updated zone card confirms the note now reads as a single-line, low-emphasis annotation consistent with the eta text above it rather than a competing badge.

66. **Zone-forced prepay now offers BOTH "Delivery charge only" AND "Full payment" for a cod-only item, not delivery-only alone (extends decision #64).** Merchant feedback against the live checkout, testing with a product deliberately configured as cod-only (`paymentOptions: ["cod"]`) shipped to the `requiresPrepay: true` "Outside Dhaka" zone: the bKash/BanglaQR `PaymentOptionPicker` offered *only* "Delivery charge only" — the merchant wants the customer to be able to choose between paying just the delivery charge OR the full amount upfront ("there should be two option delivery charge only or full payment if delivery area require pre payment"). Root cause: decisions #63/#64 widened only the `deliveryOnly` branch under a zone force (`requirement.allowsDeliveryOnly || zoneForcesThisItem`), leaving the `full` branch gated on the product's own `requirement.allowsFull`, which a cod-only product never has. Fix (mirrored in `paymentPlan.service.js`'s `assertPaymentMethodAllowed` and the frontend preview `paymentPlanPreview.js`'s `resolvePaymentOptionAvailability`, per the shared-math-duplication pattern): the `full` branch is now `requirement.allowsFull || zoneForcesThisItem` — so a cod-only item in a forcing zone makes *both* `deliveryOnly` and `full` available (but NOT `partialAdvance`, which needs a per-product configured advance percent a cod-only product doesn't have, so forcing it would be meaningless). `CheckoutPage.jsx` needed no change — `nonCodOptions` already maps over `["deliveryOnly", "partialAdvance", "full"]` in that fixed order and renders every available one, so "Full payment" now simply appears as a second picker option alongside "Delivery charge only" the moment availability flips. The `cod`/`partialAdvance` branches and the non-forcing (product already offers an alternative) path are all unchanged — this only affects the genuinely-cod-only-item + forcing-zone case. Two new backend tests in `paymentPlan.checkoutEnforcement.test.js`: `full` now allowed for a cod-only product under a forcing zone, and `full` still correctly rejected for the same product when the zone does *not* force prepay (guards against over-widening — a cod-only product has no full option of its own). Verified: backend `npm test` 65/65 (63 prior + 2 new, `order.stockBucket.test.js` unaffected); frontend `npm run lint` clean (same 4 pre-existing unrelated warnings) and `npm run build` clean.

67. **Zone-requiresPrepay reverted to a blanket COD override for the whole cart (supersedes decision #64's cod-only-item safety-net narrowing).** Direct merchant correction after testing the decision-#64 behavior: pasting their original requirement doc plus an explicit clarification — *"I just need to fix if in the backend Require prepaying the delivery charge before placing an order is enabled & product set on cash on delivery still customer need to pay the delivery charge for confirming order... cash on delivery will be disable & it will open next Payment option. There will be two option One is Default Pay Delivery Charge & Second is Full payment."* Decision #64 had scoped the zone force to only cod-only items (no `deliveryOnly`/`partialAdvance`/`full` configured), meaning an ordinary default product (`paymentOptions: ["cod","full"]`) could still be ordered as plain COD in a `requiresPrepay: true` zone — the opposite of the merchant's actual intent. Fix (mirrored in `paymentPlan.service.js`'s `assertPaymentMethodAllowed` and the frontend preview `paymentPlanPreview.js`'s `resolvePaymentOptionAvailability`, per the shared-math-duplication pattern): deleted the `isCodOnlyRequirement` helper and its per-item `zoneForcesThisItem`/`zoneForcesAnyItem` derivations entirely (confirmed via grep these were the only references anywhere in either codebase); every use is replaced with the raw `zoneRequiresPrepay` flag applied directly and unconditionally across all cart items — `cod` becomes `requirement.allowsCod && !zoneRequiresPrepay`, `deliveryOnly` becomes `requirement.allowsDeliveryOnly || zoneRequiresPrepay`, `full` becomes `requirement.allowsFull || zoneRequiresPrepay`. The per-item loop structure itself is unchanged (still needed to validate each item's own `paymentOptions` independent of the zone, e.g. rejecting a `deliveryOnly`-only product from being ordered as `full`). `partialAdvance` is completely untouched — gated purely on the product's own `requiresAdvance`/`advancePercent`, per the merchant's explicit "don't touch anything else already built." This carries forward decision #66's principle (offer both `deliveryOnly` and `full` under a zone force) but now applies it universally rather than only to cod-only items — so a plain default product in a forcing zone now correctly offers exactly two picker options, "Delivery Charge Only" (first/default, since `CheckoutPage.jsx`'s `nonCodOptions` already filters `["deliveryOnly","partialAdvance","full"]` in that fixed order — no frontend selection-logic change needed) and "Full Payment." The frontend preview's returned `zoneForcesPrepay` field keeps its name (`CheckoutPage.jsx` still reads `paymentPlan.zoneForcesPrepay` unchanged) but its value now simply equals the raw `zoneRequiresPrepay` flag instead of the per-item-derived boolean. `DeliveryOptions.jsx` and `PaymentMethods.jsx` needed no changes — both already consume either the raw zone flag or the (now-corrected) prop value as-is. Two existing tests in `paymentPlan.checkoutEnforcement.test.js` flipped to match the blanket rule (cod now rejected, not allowed, for a cod+full product under a forcing zone; deliveryOnly now allowed, not rejected, for the same case), the mixed-cart forcing-zone test's rationale updated (both items now reject cod under the blanket force, not just the cod-only one), and two new tests added confirming `partialAdvance` availability is completely unaffected by the zone force in either direction. Verified: backend `npm test` 67/67 (65 prior + 2 net-new, all zone-related titles/assertions consistent with the blanket rule, `order.stockBucket.test.js` unaffected); frontend `npm run lint`/`npm run build` clean (same pre-existing unrelated warnings only, no new ones).

68. **Checkout's prominent prepay banner generalized to fire on product-driven COD-disablement too, not just the zone force (UI-only follow-up to #66/#67).** Merchant clarified that a product can independently disable "cod" via its own admin `paymentOptions` toggles (e.g. `["deliveryOnly","full"]`, COD off) and expects the same prominent prepayment notice at checkout "for both inside and outside dhaka... this is a different thing from the global delivery charge in settings" — i.e. the banner must fire whenever COD is unavailable for the cart for *any* reason, independent of which zone/city is selected. The underlying availability math (`resolveItemPaymentRequirement`/`assertPaymentMethodAllowed` in `paymentPlan.service.js`, mirrored in `paymentPlanPreview.js`) already disabled `cod` correctly per-item regardless of zone — confirmed by re-reading both files fresh, no backend change needed. The gap was purely the frontend's prominent amber `PrepayNotice` callout in `PaymentMethods.jsx`, gated solely on the zone flag (`zoneForcesPrepay`, itself just the raw zone `requiresPrepay` post-#67). Fix (frontend-only): `paymentPlanPreview.js`'s `resolvePaymentOptionAvailability` now returns `showPrepayNotice: !availability.cod` (renamed from `zoneForcesPrepay`) — cause-agnostic, true whenever the whole-cart `cod` availability resolves false for any reason. `CheckoutPage.jsx` passes this renamed field through to `<PaymentMethods showPrepayNotice=.../>`. `PaymentMethods.jsx`'s `zoneRequiresPrepay` prop renamed to `showPrepayNotice`, and the `PrepayNotice` component's copy was generalized: it no longer claims "this delivery zone requires..." (no longer always true) and instead says "Cash on Delivery isn't available for this order," branching only on whether `deliveryOnly` is actually one of the offered options (`nonCodOptions.some(o => o.key === "deliveryOnly")`) — true for every zone-forced case and for products that themselves opt into `deliveryOnly`, but not for e.g. a `partialAdvance`/`full`-only product, where the copy instead points at "choose a payment option below" generically rather than naming an unavailable one. `codDisabledReason`'s own zone-vs-product wording (unchanged) already covered both causes correctly, so no change there. `DeliveryOptions.jsx`'s zone-card note is untouched — it's legitimately zone-specific info shown while picking a zone, distinct from this cart-level banner. No backend files touched; no new frontend test file created (none existed for this preview module or checkout components before this change, matching the project's existing test-coverage boundary). Verified: backend `npm test` unchanged at 67/67 (sanity-check only, no backend files touched); frontend `npm run lint`/`npm run build` clean.

69. **Zone-requiresPrepay re-scoped to COD-offering products only (refines the decision-#67 blanket override).** Merchant spec, sent with an admin `ProductForm` screenshot (a product with COD off, Delivery Charge Only on, Partial Advance off, Full Payment on) and the Settings→Shipping screenshot (Inside Dhaka fee 60 prepay-off, Outside Dhaka fee 120 prepay-on): *"The 'Require prepaying the delivery charge before placing an order' setting should be ignored when the product payment option is Partial Advance Payment [or] Delivery Charge Only. It should only apply when the product payment option is Cash on Delivery. In all other product payment options, this setting should have no effect."* This corrects decision #67, which made the zone flag a cart-wide blanket that injected `deliveryOnly`/`full` onto *every* item — including products that never offered cod. The reconciling principle (grounded in #67's own phrasing *"product set on cash on delivery still customer need to pay the delivery charge"*): the zone flag is fundamentally a **COD-disabling** mechanism, so it must only bite on items that actually offer cod. Fix (mirrored in `paymentPlan.service.js`'s `assertPaymentMethodAllowed` and `paymentPlanPreview.js`'s `resolvePaymentOptionAvailability`, per the shared-math-duplication rule): a per-item `zoneForcesItem = zoneRequiresPrepay && requirement.allowsCod` gate replaces the raw `zoneRequiresPrepay` in the `deliveryOnly`/`full` branches (`requirement.allowsDeliveryOnly || zoneForcesItem`, `requirement.allowsFull || zoneForcesItem`) and in the special "…requires paying the delivery charge upfront…" error condition. The `cod` branch (`requirement.allowsCod && !zoneRequiresPrepay`) and the `partialAdvance` branch (`requirement.requiresAdvance`) are unchanged. Net behavior: a cod-offering product (`["cod"]` or the default `["cod","full"]`) in a forcing zone still gets cod disabled + Delivery-Charge-Only/Full offered exactly as in #67; but a product that never offered cod (`["deliveryOnly"]`, `["partialAdvance"]`, `["partialAdvance","full"]`, etc.) is now completely unaffected by the zone flag — it keeps only its own configured options and behaves identically inside or outside a forcing zone, no longer receiving injected `deliveryOnly`/`full`. The frontend `codDisabledReason` likewise now uses `zoneDisablesCod = zoneRequiresPrepay && requirements.every((r) => r.allowsCod)` for its zone-vs-product-message ternary, so a non-cod product in a forcing zone shows the accurate product-config reason rather than the misleading zone-prepay wording. `showPrepayNotice: !availability.cod` (decision #68) is unchanged and still fires correctly for both causes. Tests: in `paymentPlan.checkoutEnforcement.test.js`, the partialAdvance-only-in-forcing-zone `cod` case flipped from the special `/requires paying the delivery charge upfront/` message to the generic `/cannot be ordered/` (zone force no longer bites a non-cod item), three "blanket override" test titles reworded to the cod-scoped framing (assertions unchanged, since those products all offer cod), and new cases added locking in that a forcing zone injects neither `deliveryOnly` nor `full` for a partialAdvance-only product and that a deliveryOnly-only product behaves identically regardless of the zone flag. Verified: backend `npm test` 70/70 (67 prior + 3 net-new); frontend `npm run lint`/`npm run build` clean (same pre-existing unrelated warnings only).

70. **Checkout reskinned from `design_handoff_checkout/`, with the design's `paymentRule`/`advanceAmount` model *derived* from this app's real payment system rather than adopted (front-end only — zero business-logic change).** The handoff assumes the page is handed a single `paymentRule` (`cod|delivery|partial|full`) plus a flat taka `advanceAmount`. **Neither exists in this codebase** and inventing them would have broken decisions #61–#69: what actually exists is a per-PRODUCT `paymentOptions[]` array + `advancePaymentPercent` (a **percent of subtotal**, not a flat taka), intersected across the cart, plus the zone-level `requiresPrepay` COD-disabling flag. So a new pure module `frontend/src/features/checkout/lib/checkoutCopy.js` (`deriveCheckoutView`) *derives* the design's view-model from the availability this app already computes in `paymentPlanPreview.js`: `rule` = `cod` when COD is available, else named after the **cheapest non-COD option the whole cart allows** (that option IS the design's "minimum to confirm"); `min` = that option's `amountPaid`. It invents no money — every figure comes from the existing `resolvePaymentOptionAvailability` + `calculateAmountPaidPreview` pair, and the backend still re-validates authoritatively on order creation. `CheckoutPage.jsx`'s buy-now router state, coupon flow, `paymentOption` sync effect, `checkoutSchema`, and `onSubmit` payload are preserved **verbatim**; only layout/props changed. New `PaySplit.jsx` (two plan cards + split bar) and rewritten `parts.jsx` (`SectionCard`/`Radio`/`OptionCard`/`FieldBox`; the old `NumberedCard`/`RadioCard`/`RadioDot` exports were removed after grepping for other importers — there were none), `PaymentMethods.jsx`, `CheckoutSummary.jsx`, `DeliveryOptions.jsx`, `AddressSelector.jsx`. Three deliberate deviations from the handoff, all flagged to the merchant before implementation: **(a)** the advance is a percent, not the handoff's flat amount, so the banner renders the *computed* taka figure; **(b)** the handoff's "remaining ৳{subtotal}" is arithmetically wrong once a coupon applies, so the true `total − min` is rendered instead; **(c)** zone-forced prepay (decisions #67/#69) has no design copy at all, so the app's own accurate zone message is kept for that branch. Per-project radius rule observed throughout (explicit `rounded-[Npx]`, never named `rounded-*` — `index.css` inflates that scale). The mobile pay bar deliberately does **not** follow the handoff's white full-width bar: on merchant instruction it matches the app's existing PDP `StickyBuyBar` dark-glass floating pill, so the two sticky bars in the app are consistent. New `checkoutCopy.test.js` builds its fixtures through the **real** availability logic (not hand-made view-models), so the 8 tests assert the whole chain — product `paymentOptions` → availability → amounts → view-model — across all four rules, the partial→full switch, the zone-forced case, and the split-bar 4% clamp. Verified beyond tests: a **real partialAdvance order placed through the redesigned UI** returned `paymentOption: partialAdvance, amountPaid: 1000, amountDue: 1060` — exactly matching the UI's preview (test order then cancelled and its stock released).

71. **Checkout UX copy rewritten to carry the *reason*, not restate amounts already on screen (supersedes the handoff's "use the verbatim copy" instruction).** Merchant reviewed the shipped reskin and rejected the handoff's own copy as too long, asking for "shorter & better UX content" — this deliberately overrides the handoff README's verbatim-copy rule, which is why it's recorded rather than silently applied. Researched checkout microcopy guidance (Baymard et al.) and applied three rules: **each line answers one doubt**; **state the exact problem**; **never repeat what's already visible**. The load-bearing observation is that every taka figure appears **three times already** (the plan cards, the split-bar legend, and the summary's Pay-now/Cash-on-delivery box), so the scenario banners were rewritten to carry the *reason* and drop the arithmetic — e.g. delivery went from "One small step to confirm — prepay the delivery charge. / Your ৳60 delivery charge lands now and the remaining ৳2,000 is cash on delivery." to "Prepay the delivery charge to confirm. / Pay it with bKash or BanglaQR. The rest is cash on delivery." The one deliberate exception is the partial-advance **headline**, which keeps its ৳ figure — that number is the single thing a pre-order customer scans for. The locked-COD row was also made **rule-based instead of product-named**: it previously embedded the full product title ("MINI GT Toyota Supra A80 Top Secret GT-300 needs a payment to confirm"), which repeated a 40+ character title twice inside 100px, since the banner directly above already names it — it now reads "Pre-orders need an advance to confirm." / "This order needs the delivery charge prepaid." / "Your delivery zone needs prepayment." It also no longer prefixes "Not available", which the row's own `UNAVAILABLE` chip already says. `codDisabledReason` (from `paymentPlanPreview.js`) consequently became dead in the view layer and was dropped from `deriveCheckoutView`'s signature — the zone-vs-product distinction is re-derived locally from whether any cart item actually disallows COD, which is the same condition. Shortened likewise: the bKash/BanglaQR panel hints, the transaction-reference helper ("From your bKash confirmation SMS — we'll match it instantly."), and the CTA sub-line. Three of these copy rules are now **enforced by test**, not just convention: banners carry no ৳ (except the partial headline), titles stay ≤8 words / bodies ≤16 / codSub ≤10, and the blocking product's title must appear in the banner but **not** in the locked-COD row. Verified: frontend `npm test` 25/25 (8 checkout-copy tests, one of which caught a 17-word banner body and forced it shorter), `npm run lint` (only the one pre-existing `shippingZones` exhaustive-deps warning, present at HEAD), `npm run build` clean; copy re-read out of the live rendered DOM at 390px for both the partial and delivery products.

72. **Checkout's scenario banner and Pay-now/Cash-on-delivery box now render only when they carry information; desktop summary made genuinely sticky (fixes a latent `items-start` bug).** Three merchant-driven refinements to the decision-#70/#71 checkout, all front-end. **(a) The scenario banner is hidden on the COD happy path** — merchant: *"Don't need to show this when cash on delivery will be available. Because generally most of the item will have cash on delivery."* The banner was firing on essentially every order (the default product config is `["cod","full"]`), where it told the customer nothing the three method rows below it don't already say; it now earns its space only when COD is *off the table* (a `deliveryOnly`/`partialAdvance`/`full`-only cart, or a `requiresPrepay` zone per #69), i.e. exactly when this order is genuinely different from the norm. **(b) The summary's Pay-now/Cash-on-delivery split box is hidden unless the money is actually split** — on plain COD it read "Pay now ৳0" and on a full prepayment "Cash on delivery ৳0", both of which merely restate the Total directly above and the CTA directly below. Same fix applied to the mobile pay bar, which was headlining a meaningless `৳0` on COD and now shows the amount actually owed with the sub-line "due on delivery". Both visibility rules were pushed *out* of JSX and into `deriveCheckoutView` as `bannerVisible` / `splitVisible`, so they live in the same tested layer as every other view decision on this page rather than being buried as inline conditions (2 new tests, 27 total). **(c) The desktop summary was never actually sticky.** `sticky top-[92px]` sat on the *card*, whose parent was the `hidden md:block` grid-item wrapper — and because the grid is `items-start`, that wrapper collapsed to exactly the card's height (540px), leaving the sticky child zero room to travel, so it simply scrolled away. The `sticky` now sits on the grid item itself, which is the pattern the ShopPage filter aside and `CartSummaryCard` already use (both are direct grid children, which is *why* theirs work), and the offset was aligned to those two at `top-[98px]` (was an inconsistent `92px`). Verified in a real browser at 1440×900, not by inspection: the summary starts at `y=225` and pins at exactly `98px` after scrolling, with no `overflow` ancestors in the chain. Frontend `npm test` 27/27, `npm run lint` (same single pre-existing `shippingZones` warning), `npm run build` clean.

73. **bKash/BanglaQR payment reference changed from a transaction ID to the LAST 4 DIGITS of the number/account the customer paid from; provider logos and a "Personal" account badge added (checkout + the admin surfaces that display the value).** Merchant request: rename "bKash Transaction ID" to a last-4-digits field, do the same for BanglaQR's "Payment reference" (last 4 of the *account* number), badge the merchant bKash number as **Personal**, and drop in the real provider logos. The rationale is operational: the merchant reconciles manual payments against their own bKash/bank statement, where the useful key is *who paid*, not a long transaction code — and four digits a customer can read off their own phone is a far lower-friction ask than copying a code out of an SMS. The **Personal** badge exists because sending via bKash "Payment" (merchant) instead of "Send Money" (personal) is the classic manual-bKash mistake. **Scope beyond checkout, deliberately:** this changes what the *stored value means*, so every surface that renders it had to follow or become misleading — admin `OrderDetailPage` now reads "paid from bKash no. ending 4821" (was "Transaction ID: …") and the admin Settings bKash section explains the new flow. Checked the DB first: all 5 existing bkash/banglaqr orders hold junk test values (`TXNVERIFY777`, `iuyhgbihbioubiupbniup`, …), so no real payment history is misrepresented by the relabel. **Validation:** the frontend `checkoutSchema` now enforces `/^\d{4}$/` on both fields (with `inputMode="numeric"` + `maxLength={4}`) — without this the label would be a lie. The **backend deliberately keeps its permissive non-empty rule** and only had its *message strings* reworded: the 4-digit shape is a presentation concern the client owns, the columns also hold older free-form IDs, and tightening it would be a business-logic change nobody asked for. DB field names (`bkashTransactionId` / `banglaQrReference`) are unchanged — they're the stored payment reference either way, and renaming them would be a migration for zero gain. **Logos:** the old text/icon chips are gone; the method rows now carry the real provider marks from a new `src/assets/payments/` module, which resolves `bkash.*` / `banglaqr.*` by filename via `import.meta.glob` so swapping an asset is a drop-in with no code change. **Sourcing note, because it constrains how this must be maintained:** image attachments in a chat reach the assistant as pixels, not as files it can write to disk — so the merchant's attached icons could be *seen* but not *saved*. Rather than hand-redraw them (an approximated payment mark misrepresents the provider and is never acceptable), the real assets were fetched and then visually diffed against the merchant's images: `banglaqr.png` is the official mark (600×600), and `bkash.png` was composed from the **official bird geometry** — the 8 origami polygons lifted verbatim out of the official bKash logo SVG — inverted to white facets on brand crimson `#E2136E`, which is exactly what the bKash app icon is. Both are PNG at the merchant's explicit request ("no svg"), rasterised at 512px, which is far beyond the 36px they render at. Verified: frontend `npm test` 27/27, backend `npm test` 70/70, lint/build clean; and end-to-end in a real browser — an old-style value (`9HK2ABCDEF`) is now rejected with "Enter the last 4 digits…" while `4821` places the order, which came back from Mongo as `bkashTransactionId: "4821", paymentOption: full, amountPaid: 2560, amountDue: 0` (test order then cancelled via `transitionOrderStatus`, stock released).

74. **Admin order bulk-select + permanent delete (`DELETE /admin/orders`), with the stock claim handed back inside the same transaction.** Merchant asked for multi-select + delete + a confirmation modal on the orders list (their list is full of test orders). No delete endpoint existed, and orders are **stock-bearing**, so a bare `deleteMany` would have silently corrupted inventory — this is the decision-#8/#37 three-bucket problem again, from a new direction. Merchant was given the semantics choice explicitly and picked **permanent delete, any status** (over "cancelled/refunded only" and soft-delete): deleting means *"this order never happened"*. New `deleteOrders({ orderIds, actorId })` in `order.service.js` therefore reuses the **same `stockBucket()` classifier** as `transitionOrderStatus` rather than re-deriving it — `reserved` (pending) releases its `reservedStock` hold (skipping this would leak the reservation FOREVER: `availableStock` would drop with no order left on the books to explain it); `committed` (confirmed..delivered) returns the already-decremented units to `stock`; `released` (cancelled/refunded) holds no claim and gives nothing back. Stock updates, `InventoryLog` rows, `AuditLog` rows and the `deleteMany` all run in **one transaction**, so a mid-flight failure can't leave stock adjusted for an order that still exists. Two deliberate details: (a) the `InventoryLog.referenceOrder` is left null and the order NUMBER is written into `reason` instead — the order it would point at is about to stop existing; (b) the route does **not** use the `auditLog()` middleware (that helper keys off a single `req.params.id` and can't snapshot each order in a bulk delete) — `deleteOrders` writes a per-order `AuditLog` carrying the **full order document** in `before`, which after the delete is the only surviving copy and therefore doubles as the recovery path. Batch capped at 100/call so one request can't hold a very long transaction open. Frontend: shadcn `checkbox` added; `OrdersPage` gets per-row + select-all-on-page checkboxes, a "N selected · Delete" bar, and an `AlertDialog` that lists the orders and states the consequences plainly — **"N items will be returned to stock"** (computed only from non-released orders; counting a cancelled order's units would double-credit stock) and "this is permanent". Selection resets on page/filter/search change, so you can never delete a row you can no longer see. Verified: 6 new RTL tests on the selection/confirmation logic (including "opening the dialog must not delete" and the exact ids sent), plus a committed **real-DB regression script** `backend/scripts/verify-order-delete.mjs` that creates one order in each bucket, deletes all three, and asserts inventory lands exactly back on baseline (stock 12→12, reservedStock 2→2 with the pre-existing reservation from other orders correctly untouched, 3 units reported returned = 1 reserved + 2 committed + 0 cancelled, 3 recoverable audit snapshots). Frontend 33/33, backend 70/70, lint/build clean.

75. **Site-wide performance pass — caching, one right-sized `.lean()` query per public product read, and deferring everything storefront visitors don't need on page one.** Backend: `Cache-Control` (with `stale-while-revalidate`) added to the four uncached public catalog routes (`/products` 60s, `/brands`/`/categories`/`/settings` 300s) via a small `cacheControl(seconds)` middleware mirroring the sitemap route's existing pattern; new indexes on `price`/`series`/`isNewArrival`/`createdAt` (the last two back the homepage's own "New arrivals"/"newest" queries, previously uncovered — confirmed live via `db.products.getIndexes()` after Mongoose's default `autoIndex` picked them up with no manual `syncIndexes()` needed, since this was a pure addition, not the rename/option-change case that actually requires one); `buildPublicFilter`'s sequential brand/category slug lookups parallelized with `Promise.all`. **`listProducts`/`getProductBySlug`/`getRelatedProducts` switched to `.lean()`** for the hydration-overhead savings — caught by the plan's own required verification step: `.lean({virtuals:true})` (the approach initially assumed "native Mongoose 6+") does **nothing** without the separate `mongoose-lean-virtuals` plugin, which isn't installed; a before/after curl diff on a real product proved `availableStock`/`profitMargin`/`isPreOrderActive` were silently dropping. Fixed with a small `withComputedVirtuals()` helper in `product.controller.js` that mirrors the three schema-virtual getters by hand (pure functions of already-selected fields, no extra query) — re-verified byte-identical against the pre-change baseline. Frontend: three Cloudinary transform tiers now exist (`cloudinaryCard` w_800 unchanged, new `cloudinaryHero` w_1600 for the hero — previously the single largest image on the site with **no transform at all** — and `cloudinaryThumb` w_200 for cart/checkout/receipt line-item thumbnails); every `<img>` site-wide gained `loading="lazy" decoding="async"` except the hero (`eager`/`fetchPriority="high"`, since it's almost certainly the homepage's LCP element). Firebase Auth SDK (~117KB/35KB gzip) moved from a static top-of-file import in `useAuth.js` to a dynamic `import("./firebaseAuth")` inside each of its 5 mutation hooks — `useCurrentUser()` (called unconditionally at app root by `CartMergeOnLogin`/`ProtectedRoute`) never touched Firebase itself, only a plain cookie check, so the SDK was loading for every guest who never logs in. `AdminLayout` (route shell, unlike its already-lazy child pages) and the Geist font (admin-only, previously in the global stylesheet) both now load only when `/admin` is actually visited — confirmed via the production build: entry chunk dropped 536.92→437.86 kB raw (168.03→136.21 kB gzip), the `>500kB` warning disappeared without touching `manualChunks`, and the Firebase/Geist chunks are absent from `dist/index.html`'s `modulepreload`/stylesheet lists. `index.html` gained `preconnect`/`dns-prefetch` for the API and Cloudinary origins. **Verification boundary, disclosed rather than papered over:** a full live login/register/logout against real credentials wasn't performed — the running dev frontend was pointed at the production backend, and creating/mutating real accounts there wasn't authorized; instead verified the dynamic-import mechanism itself fires correctly in a real browser (submitting the login form with intentionally invalid credentials reached Firebase's real API, was handled cleanly with zero JS errors, and — since Firebase rejects bad credentials before `establishSession()` runs — never touched the production backend at all).

76. **Homepage "Shop by shelf" made fully admin-configurable (Settings-driven tiles + editable heading/subtitle), replacing the hardcoded two-brands-plus-accessories derivation.** The section was previously assembled at runtime from fixed slugs — `hot-wheels-premium` + `mini-gt` brand tiles plus a hardcoded accessories tile whose label was the literal string "Protect & display" — none of it directly editable. Merchant wanted to control all of it from admin and was given the design choice explicitly: "auto-generate one tile per brand" vs. "a fully custom tile block in Settings" — picked **fully custom**. New `shopByShelf` array on the Settings singleton (`{ label, link, image }` per tile: label/link required, image optional, array position = order, `link` is a free-text path so a tile can point at a brand, category, or anywhere), plus `shopByShelfHeading`/`shopByShelfSubtitle` scalars for the section copy. Deliberately kept `shopByShelf` a **top-level array** rather than nesting into `{ title, subtitle, tiles }` — the merchant had already started entering tiles, and restructuring would have forced a data migration for zero benefit. Tile images reuse the exact `{url, cloudinaryId}` object and the existing settings-image upload endpoint every other settings image uses, so no new upload plumbing. Storefront `ShopByShelf.jsx` renders from `settings.shopByShelf`; heading/subtitle fall back to the shipped default copy when blank. **Migration safety:** when `shopByShelf` is empty the component still derives the legacy brand+accessories tiles, so the section never blanks on an un-migrated document (or the instant this deploys) — the first saved tile fully replaces the legacy path. That fallback is the sole remaining hardcoded route and can be removed once the merchant confirms their tiles are set. Admin: new "Shop by Shelf" card in Settings (placed after "Homepage Sections", since the `brandsStrip` toggle governs its visibility) with heading/subtitle inputs and a `useFieldArray` tile list (image upload via the existing `QrImageField`, label, link, remove, "Add Tile") — same pattern as hero slides / nav links / shipping zones. Verified against the running stack: the merchant's 3 already-entered tiles (with uploaded Cloudinary images) round-trip through both local and production `GET /settings`, and the real homepage rendered exactly those tiles in Playwright at desktop (1440) + mobile (390) with the legacy fallback correctly gone. The reported "not working properly" was the section **subtitle still reading "Two brands we trust — and the gear that keeps them mint." under the custom tiles** — the one piece of copy still hardcoded — fixed by making heading/subtitle editable in the same change. Backend 70/70, frontend build clean.

77. **Homepage "Featured products" big spotlight card made admin-configurable — pick the product + optional display-only overrides — without altering the product itself.** The `FeaturedSpotlight` section's large card was always `products[0]` of the `featured=true` query, rendering that product's real image/brand/title/description/price with a live Add-to-cart. Merchant wanted editorial control over that one attention-grabbing card, explicitly **with no style changes**. Chose (from three options offered) the "pick a product + per-field overrides" model over "pick product only" and "fully free-form card" — the key reason surfaced by the merchant's own follow-up question ("does this change the product?"): overrides had to be **display-only for this card**, never mutating the catalog. New `featuredSpotlight` object on Settings: `productSlug` (which product fills the card; blank → first featured product = today's behavior) plus `image`/`badge`/`brandLine`/`title`/`description` overrides. **Price is deliberately not overridable** (the card must not advertise a price the product doesn't sell at), and Add-to-cart / wishlist / click-through stay bound to the real product. Storefront: `Spotlight` now takes `overrides` and resolves each field as `override?.trim() || product.realValue` — **zero className changes**, only the data sources swapped. `HomePage` resolves the pinned product via the existing `useProduct(slug)` by-slug endpoint (same data the PDP uses, so brand + computed fields + stock for add-to-cart all come through correctly; a stale/removed slug 404s and gracefully falls back to the first featured product), and de-dupes it out of the row cards. Admin: new "Featured Spotlight" card in Settings — a product `Select` (populated from `useProducts({limit:100})`, with a "First featured product (default)" sentinel since Radix Select can't hold an empty value), an image-override upload (reusing `QrImageField`), and badge/brand-line/title/description inputs. Verified against the running site: with nothing configured the section renders identically to before (first featured product as spotlight + 3 row cards, no console errors); backend 70/70, frontend build + lint clean. The override path itself is exercised by the merchant in admin (same save/round-trip mechanism as decision #76's shopByShelf, already proven end-to-end) rather than by writing test data into the shared production settings document.

78. **Shop filter panel gains Featured + New arrivals toggles, and the "In stock only" filter fixed to use *available* stock.** Merchant wanted the two homepage merchandising flags (`isFeatured`, `isNewArrival`) usable as shop-page filters, and reported the in-stock filter "not working." **In-stock:** it was wired correctly end-to-end (verified in a real browser — toggle updates `?inStock=true`, refetches, no errors), but appeared to do nothing because all 32 catalog products are currently in stock. It also carried a latent correctness bug: `buildPublicFilter` filtered raw `stock > 0`, while the storefront's own out-of-stock rule (`ProductCard`: `availableStock <= 0`) uses **available** stock (`stock − reservedStock`). A fully-reserved item would show "Out of stock" on its card yet still pass "In stock only." Fixed to `$expr: { $gt: [{ $subtract: ["$stock", { $ifNull: ["$reservedStock", 0] }] }, 0] }` — `availableStock` is a virtual (not stored), so it can't be a plain field match; `$ifNull` guards legacy docs. **Featured/New arrivals:** the backend list endpoint already accepted `featured`/`newArrival` (the homepage uses them), so this was frontend-only — `useShopFilters` now reads both from the URL and counts them in `activeFilterCount`, and `FilterSidebar` gained two Switch rows beside "In stock only" (so they appear in both the desktop aside and the mobile filter sheet). Verified in-browser: Featured alone narrows the grid to the 6 featured products, Featured+New arrivals ANDs correctly with the URL tracking `?featured=true&newArrival=true`. Backend 70/70, frontend build + lint clean.

79. **Product-detail hover magnifier (image zoom-on-hover).** Merchant asked for the standard e-commerce "hover the product image, see a magnified view of wherever the cursor is." Implemented as an **in-place magnifier** rather than an Amazon-style side panel — the PDP is a two-column layout (sticky gallery left, details right), so a side panel would overlap the details column; magnifying within the image frame keeps everything else untouched. On hover, an absolutely-positioned overlay is sized to exactly match the *displayed* image box (measured on pointer-enter: the image is `h-full w-auto` centred, so it's letterboxed inside the container — the overlay uses that measured box, not the container, so the zoom aligns with what's on screen), with `background-image` = a new `cloudinaryZoom` (w_1600) tier, `background-size: 250%`, and `background-position` driven by the cursor's position within the image box. **Desktop-only by construction:** handlers are gated on `e.pointerType === "mouse"`, so touch never triggers it — a tap still opens the existing full-screen dialog (which was also bumped to `cloudinaryZoom` for a sharp full view). Perf-conscious: the base PDP image stays the lighter `cloudinaryCard` (w_800) so LCP is unchanged; the w_1600 source is fetched only when a mouse actually hovers/opens. Verified in a real browser via Playwright + CDP: hovering shows the overlay with the w_1600 source at 250%, `background-position` tracks the cursor (measured 40.7%/29.9% → 57.5%/72.1% across a move), the overlay matches the image box (991×518), no console errors, and a screenshot confirms the magnified region is sharp and the details column/badge are undisturbed. Frontend build + lint clean.

80. **Product-card hover slideshow — cycle a product's images every 2s while hovered (desktop, multi-image only).** Merchant wanted cards to preview a product's other photos on hover. `ProductCard` now builds `images = [thumbnail, ...gallery]` (both already returned by the list endpoint; 24/32 catalog products have ≥1 gallery image). On `pointerenter` gated to `e.pointerType === "mouse"` (touch never triggers it — a tap still just navigates), a `setInterval` advances the index every 2000ms, looping; `pointerleave` clears it and resets to the primary image. Only runs when `images.length > 1`. Rendering detail chosen for perf: the extra frames are rendered **only while hovering** (so the grid's initial paint still loads just the one thumbnail per card, preserving the perf pass), and they cross-fade via stacked `opacity` layers (`transition-opacity duration-500`) positioned to the same centred image box as the base. Verified in a real browser (Playwright): hovering a 2-image card showed image A→B at 2s→A at 4s (looping), the overlays unmounted and reset to primary on mouse-leave, single-image cards never cycle, and no console errors. Frontend build + lint clean.

75. **ProductCard hover slideshow replaced with manual prev/next glass arrows (merchant-requested change to shipped redesign behavior).** The redesign's card behavior was a 1s auto-advancing slideshow while hovered (with a cloned-first-frame seamless loop). Merchant instead wants explicit control: on hover, two round glassmorphism nav buttons (left/right) to step through the product's images — and none of it on mobile. Rewrite in `ProductCard.jsx`: the interval/clone/snap state machine is gone; a plain `index` drives the same lazily-mounted sliding track (frames still mount only while hovering, so grids stay cheap), and the arrows are `ChevronLeft/Right` in a frosted shell matching the site header's glass idiom (`bg-white/55`, `blur(10px) saturate(160%)`, hairline `border-white/60`). Design decisions: **clamped, not wrap-around** — the dimmed arrow at either end is what tells the customer they've run out of photos, and a `disabled` button swallows the click so it can't fall through to the card and open the PDP; arrows fade with hover and are `pointer-events-none` while faded; `tabIndex={-1}` because they're a mouse affordance (keyboard/touch users get the PDP gallery). **Mobile is doubly guarded**: `hovering` was already set only for `pointerType === "mouse"`, and the arrows are additionally `hidden md:flex`. Card still resets to the thumbnail when the hover ends. Two merchant-reported fixes folded in the same session: **(a) frame bleed** — card photos are wider than the card (`h-full` + `max-w-none` center-crop) and the slide frames had `overflow: visible`, so each slide's sides bled into the neighbouring frame (measured: a 450px image in a 316px frame, with slide 2's image starting 67px inside slide 1's viewport — the "image is cut" screenshot); fixed with `overflow-hidden` per frame, which also makes frame 1's crop pixel-identical to the resting thumbnail. **(b) arrow reveal rebuilt as pure CSS scoped to the IMAGE area** — a named group (`group/img` on the image container, `md:group-hover/img:opacity-100` + `pointer-events` on the arrows) replaces the JS `hovering`-state opacity toggle, so the arrows appear only while the pointer is over the image (title/price zone stays clean) and no JS state can ever leave them stuck visible. Gotcha recorded: the disabled end-arrow dims via **colors** (`disabled:bg-white/40 disabled:text-ink/30`), not `disabled:opacity-*`, because the group-hover `opacity-100` selector (two classes + pseudo) out-specifies a `disabled:` variant (one class + pseudo) and would un-dim it. Verified live via Playwright at 1440px: arrow opacity is 0 parked away, 0 over the title/price, 1 over the image; frames compute `overflow: hidden`; next-click translates the track −100% and flips the disabled states; no arrow click navigates to the PDP; at 390px w/ touch all arrows compute `display: none`. Frontend 33/33, lint/build clean.

76. **Announcement bar rebuilt as a fully data-driven, per-device customizable strip (replaces the two-field `announcementBar` setting).** Merchant request: colors (bar/text/icon/separator), separator style, per-message icons, **separate desktop and mobile announcements**, auto-scroll on/off — "remove existing announce setting & make it customizable & flexible". Merchant decisions (asked): page placement is an admin toggle ("Show on all pages", default off = the current home-only behavior), and OFF now genuinely hides the bar — the storefront's hardcoded `DEFAULT_SEGMENTS` fallback is **gone**, with the three shipped design lines migrated into the live Settings document as real editable desktop messages via a direct `$set` (never re-seed Settings; the old value was junk test text with `isActive: false`, so nothing real was lost). New shape (model/validation/seed all updated): bar-level `bgColor/textColor/iconColor/separatorColor` (hex-or-empty, "" = shipped design colors), `separatorStyle` enum (`dot|pipe|slash|diamond|star`), `showOnAllPages`, `scrollSpeed` (5–120 s/loop), and per-device `desktop`/`mobile` subdocs each `{ isActive, autoScroll, messages: [{ icon, text }] }` (max 8 messages, 120 chars). Icons come from a **new shared curated map `frontend/src/components/shared/settingsIcons.js`** — extracted from `WhyChooseUsSection` (its existing keys are load-bearing for stored Why-Choose-Us items and must never be renamed) and extended with Megaphone/Tag/Gift/BadgeCheck/Zap; `SettingsPage` and `WhyChooseUsSection` both import it (this also cleared the long-standing `only-export-components` lint warning on `WhyChooseUsSection`). The rewritten `AnnouncementBar.jsx` renders one `<Bar>` per device (`hidden md:*` / `md:hidden`), colors via inline style only when set, and **marquee** mode: the segment run rendered twice inside a `w-max` track animated `translateX(-50%)` (`@keyframes announce-marquee` in `index.css` — the app's first marquee), duration from `scrollSpeed` via a CSS var, paused on hover, disabled under `prefers-reduced-motion`. **Deploy safety (live Vercel site):** a Settings doc still carrying the old `{ text, isActive }` shape renders the legacy single-text bar instead of nothing, covering the window where the frontend deploys before the production DB is migrated — **production still needs the same one-off `$set` when this ships**. Admin section rebuilt with the existing `ColorField`, two `useFieldArray` message lists (icon Select with a `"none"` sentinel — Radix forbids `""` item values — text input, remove/add), per-device Show + Auto-scroll switches, separator-style Select, scroll-speed input, and the all-pages toggle; the complete new subtree was added to the form's `defaultValues` (PATCH replaces the whole subtree — a missing field would be dropped on every save). Verified: 7 new `AnnouncementBar.test.jsx` cases (hidden-when-off, legacy-shape fallback, device split classes, blank-message hiding, inline colors + diamond glyph, marquee duration + duplicated run, placement gate both ways) — frontend 40/40, backend 70/70, `verify-theme-split` PASS, lint/build clean; curl round-trip as admin (invalid hex and enum rejected 400, full valid subtree accepted and read back exactly); live Playwright at 1440/390 — marquee measurably moves (15s duration honored) with star separators + Megaphone icon on `/shop` under the all-pages toggle, mobile runs its own message, and after restoring the real config the home bar is pixel-identical to the original design, absent on `/shop`, absent on mobile.

81. **Hero settings merged into one admin section; the dead `heroBanner` slide array and hero autoplay controls removed end to end.** (Numbered 81 to avoid the pre-existing 75/76 collision in this log — historical numbers are left as-is since `decision #NN` cross-references point at them.) Merchant: *"few unnecessary things inside the hero section setting — merge hero in one section setting & clear unnecessary files… currently not using."* Traced consumption first: the redesigned storefront hero reads `homepageSections.hero.{enabled, variant, highlightCard, content}` and **only the IMAGE of `heroBanner[0]`**. The v2 hero is a single styled section, not a carousel — every slide's title/subtitle/CTA, slides 2+, and `hero.autoplay`/`autoplayInterval` (grep: zero storefront refs) were leftovers from the pre-redesign carousel. Changes: `heroBanner` + `heroSlideSchema` deleted from Settings model/validation/seed, `autoplay`/`autoplayInterval` deleted from `homepageSections.hero`, new `hero.image` (imageSchema) added as the visual's home; admin's "Hero Banner" slides card and the hero sub-block inside "Homepage Sections" collapse into ONE "Hero" SectionCard (show · style · single image · highlight card · per-style copy), leaving "Homepage Sections" as a pure toggle list; `HeroSlideImage` deleted (the generic `QrImageField` uploader covers the single hero image); `HomePage.jsx` reads `hero.image` with a `heroBanner[0].image` legacy fallback (announcement-bar deploy-safety pattern). Dev DB migrated by direct `$set`/`$unset` (never re-seed) — **production needs the same one-off migration at deploy time**. Verified: backend 70/70, frontend 41/41, lint/build clean; the migrated doc replays clean through the updated Zod schema (whole-object admin save can't reject); live Playwright confirms the home hero renders the exact same Cloudinary asset (matched by asset id) with highlight card intact.

82. **CartUp marketplace bulk-upload sheet generated from the live API by XML surgery, not openpyxl — and deliberately kept outside both apps.** Merchant was accepted as a CartUp seller and given a category template (`Marketplace/Die-Cast Vehicles Product.xlsx`, 39 columns, empty) to bulk-list the catalogue on a second channel. Three findings drove the design. **(a) openpyxl cannot round-trip this workbook.** The `payload_hidden` sheet — the one CartUp's importer actually reads — stores its formulas as `<f>=IF(product!A3&lt;&gt;"",...)</f>`, with a leading `=` *inside* the `<f>` element, which is not standard OOXML; openpyxl parses that as the string `"==IF(...)"` and writes it back verbatim, silently corrupting all 500 × 39 payload formulas. The template also carries a `legacyDrawing` (`vmlDrawing1.vml` + `comments1.xml`) holding the per-column help notes, which openpyxl drops on save. So `build_cartup_sheet.py` unzips the template, patches exactly `xl/worksheets/sheet1.xml` and `xl/sharedStrings.xml`, and rezips every other part byte-identical (`verify_cartup_sheet.py` asserts that byte-identity and that `sheet3.xml` still contains its `<f>=IF(` formulas). Rows 3–503 already exist fully styled with every cell as `<c r="A3" s="8" t="s"><v>46</v></c>` (shared-string 46 = `""`), so filling is pure substitution — no rows or cells inserted. **(b) Six columns are dropdown-validated against a `veryHidden` `value_hidden` sheet**; any value not matching verbatim makes payload_hidden's VLOOKUP return `#N/A` and the import fail. CartUp's 3,402-brand list contains **no Hot Wheels, Mattel, MINI GT, or TSM** — `No Brand` (id 519) is used and `Marketplace/cartup-brand-request.md` drafted for their support. Because there's no Excel here to recalculate, the verifier simulates the VLOOKUP itself (first match, column 2) and prints the ids it would resolve to. **(c) Images cannot come from the sheet at all** — CartUp's policy sheet requires the *ImageKey* from an upload batch made inside their dashboard, so the Cloudinary URLs are useless in the image columns; `download_images.py` pulls all 50 photos renamed by Seller SKU, `image-map.csv` carries a blank `imageKey` column, and `splice_image_keys.py` writes the keys back using the same XML surgery. Data comes from the **live public API** (`GET /products?limit=100`), not `docs/Inventory.md` (retail column is pre-23-Jul and stale) and not `catalog.data.js` (no images, stock, or sale prices); the verifier re-fetches and fails on any drift in title, stock, or price. Scope is 29 cars — `ACC-001/002/003` excluded as not die-cast vehicles. Pricing is site price × 1.14 rounded to ৳10 (merchant-chosen, to absorb CartUp commission); Special Price is **off by default** since AK/AL are date-validated and are the likeliest cause of a rejected import. Seller SKU is prefixed `DCBD-` because CartUp requires global uniqueness across all sellers. Age grading follows the manufacturers' official grades — Mattel 3+, TSM Models 14+ — mapped onto CartUp's buckets as `10 Years+` / `18 Years+` (never understating a safety grade) with the official grade stated verbatim in every description. Bengali copy in `cartup_copy.py` is a rendering of the merchant's own live English copy with brand/casting/livery names left in Latin script; no product facts are invented, and the packaging/age facts appended to each description cite sources in `cartup_copy.SOURCES`. **Python 3 stdlib (`zipfile`) rather than Node**, since neither app has a zip library and this is an ops tool, not app code — nothing in `frontend/` or `backend/` changes, and there is no deploy-path risk. Verified: 29 rows, all six dropdowns resolve to real ids, all mandatory columns populated (images excepted by design), numeric columns are numeric cell types, Seller SKUs unique, 0.58 MB (limit 2 MB), every non-patched zip part byte-identical, and the splice path exercised end-to-end with synthetic keys in a scratch copy (79 keys across 29 products, structure and checks still clean afterwards).

83. **CartUp image columns take plain CDN URLs, and the workbook must never be edited outside the generator — a Google Sheets round-trip cached `#ERROR!` into all 19,500 `payload_hidden` cells.** Follow-up to #82, after the merchant uploaded photos in CartUp and returned the sheet. Three corrections. **(a)** CartUp's "ImageKey" is just a URL — their media library issues `https://cdn.cartup.com/product/<uuid>.webp` and that string goes straight into the image column; there is no separate opaque key, so #82's two-step "upload, download the key batch, splice" model was wrong. Replaced by a durable `cartup-image-urls.csv` (sellerSku, position, url) that `build_cartup_sheet.py` reads to fill columns C–J **and AG** (Variant Image always mirrors slot 1 — one variant per product), plus `import_cartup_urls.py` to harvest URLs out of any workbook CartUp returns. `splice_image_keys.py` deleted: its splice-after-build design meant the next rebuild silently wiped the images. **(b)** The returned workbook had been opened in Google Sheets, which cannot evaluate the template's non-standard `<f>=IF(...)</f>` formulas (the same encoding that rules out openpyxl) and therefore cached **`#ERROR!` into every one of the 19,500 payload cells**, stripped all 16 dropdown validations, flipped `value_hidden` from `veryHidden` to `hidden`, and padded the sheet to 1,000 rows. The pristine template ships those formulas with *no cached values at all*, which is the state the XML-surgery generator preserves — so a round-tripped file is strictly worse than a generated one and must never be uploaded. Template restored to its exact original 593,105 bytes from the pre-edit unzip plus the byte-identical parts already carried in the generated output; the merchant's copy archived under `Marketplace/from-cartup/` (gitignored) as harvest-only input. **(c)** Verifying that each CartUp image actually belonged to its product could not be done by hash — CartUp re-composites the photos onto decorative backgrounds, so the 5 MINI GT images matched their originals at average-hash distance 0 while every Hot Wheels image scored ≥49/256 against *everything*. Identification was done visually from labelled contact sheets by reading the card art, which found `HWPC-001` (Cyberpunk 2077) carrying a photo of the **2021 Toyota GR Supra** — i.e. `HWFF-005`'s product, and HWFF-005 was the one row with no image. URL moved to its real owner. Both `import_cartup_urls.py` and `verify_cartup_sheet.py` now refuse to let one image be claimed by two products (the generic form of this failure), the verifier asserts Variant Image equals Product Image 1 on every row, and a missing image is now a hard FAIL rather than a warning since CartUp rejects those rows. Verified: 28/29 rows imaged, no shared images, all six dropdowns still resolving, every non-patched zip part still byte-identical, 0.58 MB, verifier exits 1 on the one remaining gap as intended.

84. **CartUp's importer reads `payload_hidden`, so the generator must compute it — an uncomputed payload is why the first upload came back "No products were found in your excel upload".** Decision #82 reasoned that CartUp could not be reading payload_hidden's cached values "because they'd never be valid"; the upload failure disproved it. Every cell on that sheet is a formula (pass-through `=IF(product!A3<>"",product!A3,"")`, or `VLOOKUP` into a `value_hidden` table for the six dropdown columns) and formulas only acquire values when a spreadsheet app recalculates on save — which the XML-surgery build never does. So all 19,500 payload cells shipped as formulas with no `<v>`, a server-side reader saw 19,500 blanks, and the visible `product` sheet being perfect was irrelevant. Fix: `compute_payload()` derives each payload column's source column and lookup table **by parsing the row-2 formulas** rather than a hard-coded map (so a template revision surfaces as an error instead of silently mis-mapping), resolves the VLOOKUPs against tables read from the workbook's own `value_hidden` table definitions, and writes results as cached `<v>` beside the untouched `<f>` — numeric results with no `t` attribute, string results with `t="str"`, unused rows as empty strings, matching what Excel itself would produce. Patched parts go from two to three (product, sharedStrings, payload); all others stay byte-identical, and openpyxl is now imported but *only to read* the template's lookup tables. Because this failure was invisible to every existing check, the verifier gained: payload row *n−1* vs product row *n* equality on name/sellerSku/parentSku/stock/price/image1/variantImage, a numeric-id requirement on the six resolved columns, a no-data-past-last-row assertion, a cached-`#ERROR!`/`#N/A`/`#NAME` failure (the signature of a spreadsheet round-trip), and a printed `payload_hidden (what CartUp actually imports): 29/29 rows carry values` line. Verified by negative test: reverting only the payload part to the template's uncomputed state yields **377 failures** where the previous verifier passed clean. Output 0.60 MB, 29/29 rows, all 16 required fields, exit 0.

85. **CartUp rejects `#` in product names; rewritten to `No.` at generation time, scoped to the two name columns only.** Second upload attempt, with decision #84's payload fix in place, got through to content validation — CartUp returned an error workbook whose Seller SKU / Parent SKU / Variant Combination sheets were all **empty** (confirming the `DCBD-` prefix, SKU uniqueness and one-variant-per-parent design), leaving a single rule across 7 rows: *"Product name in English cannot contain '#'."* The flagged rows carry collector or race numbers — Boulevard #150, Ferrari SF-25 #44, VCARB #6, MINI GT #1022/#1046/#1094/#1106 — and CartUp's row numbers map directly onto the product sheet. A sheet-wide scan found `#` in five columns (A, B, S, T, U) across 8 rows, but only the 7 with `#` in **Name (English)/(Bengali)** were flagged; row 6 (HWCC-002, `#20` in its descriptions) was not. The rule is therefore name-scoped and so is the fix: `cartup_name()` replaces `#` with `No.` (`HASH_REPLACEMENT`) on both name columns plus What's-in-the-box, which is derived from the title. `No.` rather than deleting the character, because deletion would leave `Ferrari SF-25 44` and `VCARB 6`; descriptions deliberately keep their `#` since they validated and read better. `verify_cartup_sheet.py` gained a `#`-in-names check and its live-API title comparison now runs the live title through `cartup_name()`, so the intended divergence (`MINI GT No.1022` on CartUp vs `MINI GT #1022` on diecastbd.com — site copy unchanged) is not misread as drift. Negative-tested by setting `HASH_REPLACEMENT = "#"`: 14 failures, exactly the 7 flagged rows across both name columns.

86. **CartUp pricing split into a two-layer MRP + Special Price model.** Merchant asked to "increase the price 7% for cartup for retail price & add current price as a special price" — ambiguous between compounding on the already-agreed +14% commission markup and replacing it, a ৳11,935 difference across current stock, so both were costed and put to them; they chose to compound. Now: **Special Price (AJ) = site price × `MARKUP` (1.14)**, which is what actually gets paid and still absorbs CartUp's commission, and **Price/MRP (AI) = Special × `MRP_UPLIFT` (1.07)**, the struck-through "was" figure, each rounded to ৳10 independently (so the shown discount is 6.5–6.7%, not exactly 7%). `marketplace_price()` keeps its meaning as "what you're paid" and a new `listed_price()` expresses the MRP layer; `MRP_UPLIFT = 1.0` disables the promo and sells flat, which is the single knob to turn if the date columns ever cause trouble. **Every product gets a special price, not only the three with live site sale prices** — the promo is the pricing model rather than a mirror of site promotions, so HW5P-001 / HWCC-007 / HWF1-002 keep their site-only sales and all 29 rows price off the site's regular price. This also reverses decision #82's `SPECIAL_PRICES = False` default: the AK/AL date columns were held back then as the likeliest cause of a rejected import, and are now exercised — written as **text in `dd/mm/yyyy hh:mm`**, the format the columns' own validation message specifies, and confirmed flowing through payload_hidden as `promoStartAt`/`promoEndAt` strings with `promoPrice` numeric. Window 28/07/2026–28/07/2027; **when it lapses every listing jumps to the 7%-higher MRP**, so it is recorded as a dated action in the README rather than left implicit. Verifier checks AI against `listed_price()` and AJ against `marketplace_price()` separately, on top of the existing special < MRP / ≤75%-discount / both-dates-present policy checks. Catalogue at current stock: site ৳85,335 → paid ৳97,270. Site prices unchanged; nothing in frontend/backend touched.

87. **CartUp's Special Start/End columns need REAL Excel dates; text in the documented display format is rejected.** Third upload attempt: the decision-#85 renames cleared, and every one of the 29 rows came back with *"Special StartAt should be a valid date."* Decision #86 had written the dates as `dd/mm/yyyy hh:mm` text because that is the format the columns' own validation message quotes — but AK/AL carry an Excel `type="date"` validation, so that string is the *display* format, not the storage format, and text there is something a human typing into Excel could never produce. Fixed by writing genuine Excel dates: serial numbers in cells carrying a date number format. The template ships **no `<numFmts>` element at all** and 13 cellXfs, none of them a date format, so `add_date_style()` inserts `numFmtId="164"` (first id available to a document) and appends a 14th cellXf cloning the font/fill of style 8 — the style every product data cell already uses, so the date cells look like their neighbours and only the number format differs. Applied on **both** the product sheet and `payload_hidden`: the payload half is load-bearing because CartUp reads that sheet, and a date serial in a General-formatted cell reads back as a bare number to any importer that infers date-ness from the number format. `xl/styles.xml` therefore joins the patched-parts set, which is now four (product sheet, sharedStrings, payload sheet, styles) — everything else still byte-identical. Round-trip confirmed: openpyxl reads both sheets back as `datetime` with `number_format='dd/mm/yyyy\ hh:mm'`. Verifier asserts `datetime` type on both date columns on both sheets plus start < end, negative-tested by clearing `DATE_FIELDS` (reproduces the text form, fails every row). Two adjacent gaps closed in the same pass: the `#` check now also covers payload_hidden's name columns (it had only looked at the visible sheet), and the verifier prints the approved file's path, build time, size and sha256, after a stale pre-fix download was mistaken for an unfixed rule. Fallback if the dates ever misbehave again: `MRP_UPLIFT = 1.0` drops the promo and sells flat at the markup price.

88. **Prerendering rewritten browser-free, because the Playwright version had been failing silently in production for months — every URL on diecastbd.com was serving one identical shell.** Search Console reported four exclusion reasons; diagnosing them against the live site (not the repo) found the real cause: `GET /`, `/about`, `/faq`, `/shop`, `/privacy-policy`, `/products/<slug>` and `/brand/<slug>` all returned the **same 4067 bytes** — same generic `<title>`, and **no `<link rel="canonical">` at all**. That is precisely what "Alternate page with proper canonical tag" describes: to the crawl that happens before JS renders, ~48 URLs looked like one page. The old `scripts/prerender.mjs` wrapped `preview()` and `chromium.launch()` in a `try {} finally {}` with **no `catch`**, so a launch failure escaped to `main().catch()` → `.finally(() => process.exit(0))` and the build stayed green having baked zero routes; a second swallow (`catch { log('skip …') }`) hid per-route failures. Confirmed by `content-disposition`: `/` served `dist/index.html` while every other route served `filename="app.html"`, i.e. no per-route files existed. **Everything that matters here is head-level, and a head is a string** — so the browser is gone entirely: the script fetches the API with plain Node and splices tags in. Route discovery reads the backend's own `/sitemap.xml` (already derived from Mongo: `status:"active"`/`isPublished`/`isActive`), so the prerendered set and the sitemap cannot drift and the missing "list pages" endpoint stops mattering. Result: 48/48 routes, ~8 s instead of ~3 min, no Chromium download (`vercel-build` drops `npx playwright install`; playwright stays a devDep for the QA scripts). **The anti-drift mechanism is shared code, not review discipline:** new `src/lib/seo/{constants,collectionCopy,routes,injectHead}.js` hold every title/description/canonical/JSON-LD formula, imported by React (`@/lib/seo/…`) *and* by the build script (relative path) — alias-free, JSX-free, `import.meta.env`-free, with `siteUrl` passed in because Node cannot import `lib/siteUrl.js`. Pages now build a model and hand it to a new `<SeoHead model>`; `<Seo>` is a prop-driven wrapper over the same component. **The keystone is one line in `main.jsx`:** `document.querySelectorAll("[data-prerendered]").forEach(el => el.remove())` before `createRoot`. react-helmet-async v3 on React 19 renders metadata as ordinary elements that React **appends** — it never removes what is already in `<head>` — so without the strip every prerendered route would ship two `<title>`s, two canonicals and two Product JSON-LD entities, and Google may discard an ambiguous canonical entirely. With it, JS-blind crawlers get the baked head and everyone else gets React's live head (live price, live stock), never both. **Loud failure replaces silent success:** `process.exitCode` instead of `process.exit(0)`, `PRERENDER_STRICT=1` + `PRERENDER_MIN_ROUTES` fail the build (Vercel then keeps the current deployment serving — blast radius is "the deploy doesn't ship", never "the site breaks"), a `dist/prerender-report.json`, and two new verifiers wired into `npm run build`: `verify-prerender.mjs` (exactly one title/canonical/description per route, none equal to the generic shell title, all titles distinct, `app.html` neutral, `index.html` ≠ `app.html`) and `verify-hydration-parity.mjs` (Playwright, opt-in). Verified: 48/48 baked; product pages carry real `Product`/`Offer` JSON-LD with live price 2690 and `InStock`; parity check shows baked and hydrated heads **identical** with exactly one of each tag on `/`, `/shop`, `/faq`, `/privacy-policy`, a brand and a product page; frontend 105/105, backend 121/121, lint clean. Gotcha for future work: `vite preview` is **not** a faithful local stand-in — its SPA fallback rewrites `/faq` to `index.html`, so the parity script ships its own tiny static server that mimics Vercel's filesystem-then-rewrite order (verified against production, where `/robots.txt` and `/index.html` are served as real files despite the `/(.*)` catch-all).

89. **`noindex` replaces `Disallow` for private routes, and soft-404s finally say `noindex` — the two are opposite halves of the same mistake.** `frontend/public/robots.txt` was `Disallow`-ing exactly the routes that needed removing from the index, and **that is what prevented their removal**: a URL Google is forbidden to crawl is a URL where it can never *see* the `noindex`. Such a page stays indexed URL-only ("Indexed, though blocked by robots.txt") indefinitely. So every `Disallow` is gone (with a comment in the file explaining why, so it isn't helpfully restored — crawl budget is not a counter-argument when all of them return the same cached `app.html`), replaced by `X-Robots-Tag: noindex` per-route in `frontend/vercel.json` (works with no JS) plus a rendered `<meta name="robots">` via a new `noindex` prop on `<Seo>`, applied to cart/checkout/order-confirmation/account/orders/wishlist/auth/unauthorized. The mirror-image bug: `NotFoundPage` had **no `<Seo>` at all**, yet Vercel's SPA fallback returns **HTTP 200** for every unknown URL — so mistyped URLs, deleted products and unpublished pages were indexable soft-404s. It now renders `noindex`. **That change is only safe with a 404-vs-error split, which was the subtle part:** `CmsPage`, `PageView`, `ProductDetailPage` and `CollectionPage` all rendered `NotFoundPage` on *any* query error, so a single transient API failure while Googlebot was rendering would have painted `noindex` onto live products, brand pages and the four sitemap-listed policy pages. Each now branches on `error?.response?.status === 404` and otherwise shows a new neutral `PageLoadError` that says nothing to crawlers; `CollectionPage` additionally guards on the brands/categories *list* query failing, since a failed list is indistinguishable from "no such brand". Related: `useProduct` gained `retry: (n, e) => e?.response?.status !== 404 && n < 2` — TanStack's default retried 404s three times with backoff, parking a dead product URL on the loading spinner for ~7 s before the 404 could render, long enough for a crawler to give up before ever seeing the `noindex`; genuine failures still retry. Also fixed, all verified live first: `/share-image` returned a hard **404** while being referenced as `og:image` **and** `twitter:image` on every page (`seoDefaults.shareImage` is null in production) — it now falls back to the shipped `android-chrome-512x512.png`, a real brand asset, never invented artwork (#11), with the honest caveat that it is square and will letterbox under `summary_large_image`, so the merchant should still upload a 1200×630; `/index.html` (a live 200 duplicate of the home page) now 301s to `/`, while **`/app.html` gets the header and never a redirect** because it is the destination the entire SPA rewrite depends on; `api.diecastbd.com` had **no robots.txt** (JSON 404 = "crawl everything") and now serves one plus a blanket `X-Robots-Tag: noindex, nofollow` — with `/sitemap.xml` explicitly allow-listed, which is load-bearing, since `diecastbd.com/sitemap.xml` is a Vercel rewrite that proxies that exact route and upstream headers pass through, so a blanket header would have quietly deindexed the storefront; and the sitemap now filters CMS pages against a reserved-slug set, because a page slugged `shop` or `about` is shadowed by the real route and could only ever advertise a duplicate `<loc>` for a URL showing different content. Verified in a real browser against production data: `/no-such-page-xyz`, `/products/no-such-product` and `/brand/no-such-brand` all render `robots=noindex` with the 404 title, `/cart` and `/login` render `noindex`, and `/shop` correctly renders **no** robots meta.

90. **Slugs are immutable after creation, for products, pages, brands and categories.** Follow-up to #89, merchant-approved. `updateProduct`, `updatePage`, `updateBrand` and `updateCategory` all recomputed the slug whenever the title/name changed, and **no redirect mechanism exists anywhere in the system** — no `previousSlugs`, no redirects collection, nothing at the edge. So every rename silently moved a public URL and permanently orphaned the indexed one; worse, the old URL didn't even 404, it returned HTTP 200 with an empty SPA shell. The reasoning is decision #18's, applied to a different identifier: *"Coupon codes are immutable after creation… changing a distributed code would silently break every customer who already has it."* A product URL is exactly that kind of distributed identifier, with Google and every customer who shared a link holding the old value. Now `delete updates.slug` on all four update paths; `uniqueSlugFromTitle` lost its `excludeId` parameter since it is only reachable from create. **Extended beyond the products/pages case that prompted it** because `/brand/<slug>` and `/category/<slug>` are indexable landing pages carrying sitemap priority **0.9 — higher than products' 0.8** — so renaming a brand was the most damaging version of the same bug. Display names stay freely editable; only the URL is frozen. The `delete` is defense-in-depth rather than the mechanism: none of the update Zod schemas has a `slug` key and `validate()` rebuilds `req.body` from validated data only, so a smuggled slug never arrives — made explicit anyway, for the same reason the Phase 2 role-escalation fix whitelisted fields in the controller instead of trusting the middleware alone. Two admin previews had to change with it or they would have lied: `PageFormPage` and `SimpleCatalogManager` both rendered a *title-derived* slug preview, which on an edit form promised a URL change that will now never happen — both show the **stored** slug when editing (with a line saying renaming won't move the URL) and keep the derived preview only on create. Cost, stated plainly to the merchant: fixing a typo in a title no longer fixes the URL, which is how Shopify handles handles. The fuller `previousSlugs: [String]` + editable-slug-field design remains available as a follow-up; note a true 301 for a DB-driven path is impossible from static hosting, so it would be a client-side replace-navigation plus a correct canonical. Verified: backend 121/121, frontend 105/105, lint clean.

91. **Checkout asks for a district and a thana from searchable dropdowns, and no longer asks for a postcode.** The address form collected free-text `City`, `District` and `Postal code`. Free text produced addresses a courier can't route (`dhaka`, `Dhak`, blank district), and the postcode asked for something most Bangladeshi customers don't know — three fields to get one routable area wrong. It is now two dependent dropdowns: pick a district, then a thana inside it. **The data is copied from public sources, never recalled** (project rule): `frontend/src/lib/bdGeo.js` carries the 64 districts, 8 divisions and 494 upazilas from the MIT-licensed `bd-geodata` package, **plus the 50 Dhaka Metropolitan Police and 16 Chattogram Metropolitan Police thanas** from Wikipedia's DMP/CMP articles. The metro merge is not padding — Dhaka *district* officially contains only Savar, Dhamrai, Keraniganj, Nawabganj and Dohar, because Dhaka *city* is policed by thanas rather than divided into upazilas. On the upazila data alone **Dhanmondi, Gulshan, Mirpur, Uttara and Banani are simply absent**, and the apparent Mirpur/Mohammadpur matches belong to Kushtia and Magura; the merchant's own address is in Dhanmondi, so it would not have been selectable. ~~Every other district's city is covered by its Sadar upazila, which is why only these two are merged.~~ **CORRECTED by decision #95 — this was wrong.** Khulna and Rajshahi have no Sadar upazila and no metro thanas in the upazila data, so on this dataset their cities were entirely unselectable. All eight metropolitan forces are now merged. One duplicate spelling collapsed (`Karnafuli`/`Karnaphuli`, an upazila and a CMP thana sharing a name) — a street address disambiguates them. `aka` holds the pre-2018 spellings so typing "Chittagong" or "Jessore" still finds Chattogram and Jashore. The dataset is ~10 KB raw / 3 KB gzipped and lands **only in the lazy checkout/account chunk**, never the storefront bundle (asserted against `dist/`).

    **Data model: `thana` is added, `city` is kept and demoted, `postalCode` is retained but no longer collected.** `city` was `required` on both `Order.shippingAddress` and `Address`; it is now optional, with the new `thana` alongside it. Renaming in place would have broken the merchant's existing orders and saved address, so old records keep reading correctly and the display layer resolves `thana || city`. That resolution lives in exactly one place per app — `frontend/src/lib/address.js` and `backend/src/utils/address.js` — because the address was previously re-formatted inline at **eight display sites and three emails**, with `postalCode` printed at some and not others. `Order.shippingAddress` is a purchase-time snapshot, so orders placed under the old shape are never migrated and never need to be.

    **The backend stays deliberately looser than the form.** The checkout form requires both dropdowns; `shippingAddressBodySchema` and `createAddressSchema` accept either shape and only enforce `thana || city` via a refine. A browser tab opened before the deploy still posts `{city, postalCode}`, and rejecting those would lose real orders in the window after a release — for a live store an order that arrives beats a validation error the customer can't act on. `updateAddress` `$unset`s the legacy `city`/`postalCode` once a `thana` arrives, so an edited address can never read "Dhaka" beside a district of Khulna (both keys are stripped from `$set` first — setting and unsetting one path in a single update is a MongoDB conflict error). Editing an address saved under the old shape leaves both dropdowns **blank rather than guessing a thana from the old city** — the customer picks once.

    **`SearchableSelect` floats on desktop and opens in the document flow on mobile**, which is the reason it isn't Radix Popover or cmdk (neither is a dependency). Floating everywhere doesn't work: the address form renders inside `ResponsiveModal`, whose mobile `SheetContent` is `overflow-y-auto` — an absolutely-positioned panel is clipped there; portalling out of that escapes the Sheet/Dialog focus trap, which then refuses to hand focus to the search box; and on a phone the virtual keyboard leaves an anchored panel room for about two options. In flow, the scroll container simply scrolls to reveal it. None of that holds on desktop — no keyboard, and `ResponsiveModal` switches to a `Dialog` that doesn't clip — where pushing every later field down is merely jarring, so the panel overlays instead (merchant-reported: *"the adress form in web gets expanded with the dropdown. i want the dropdown floating… mobile experience is ok"*). The breakpoint is **`md` (768px) specifically to match `ResponsiveModal`'s own `useMediaQuery("(min-width: 768px)")`** — at 640–767px it still renders the clipping bottom sheet, so floating from `sm` would break in that band. The floating panel flips above the trigger when there isn't room below and there is room above, so a field low in the window doesn't hang the list off the edge. Two containment details matter: Enter inside the search box calls `stopPropagation`, or `AddressForm`'s "Enter on an INPUT saves the address" handler would submit a half-filled form the moment someone pressed Enter to pick a district; Escape is contained too, or it would close the whole address modal. Changing district clears a thana that doesn't exist in the new one, on the same tick as the district write.

    Verified against the dev database and a real browser, not just unit tests: an order with the new shape persists `{district, thana}`; a simulated stale tab posting `{city, postalCode}` still succeeds; an address with no area at all is rejected. In Chromium at desktop and iPhone 13 widths — no postcode field, thana disabled until a district is chosen, "chittagong" resolving to Chattogram, 30 thanas for Chattogram, switching to Khulna clearing the thana, and inside the **mobile bottom sheet** the open panel measured fully within the viewport (not clipped) with Enter selecting a thana without saving the form. Test orders and the guest they created were deleted afterwards and today's analytics rollup recomputed to zero. Backend 140/140, frontend 127/127 (22 new), lint clean, build clean. **Shipping-zone selection stays manual** — merchant's call, and `settings.model.js` documents the invariant that zone names are admin-renamable free text, so deriving a zone from the district would couple routing to a string the admin can change.

92. **Sales, revenue and profit are three different numbers, and an order counts from the moment it is confirmed.** The dashboard and Reports both showed `order.total` labelled "Revenue". That total includes the delivery charge, which the courier deducts — the store never receives it — so every headline figure overstated earnings, and there was no profit anywhere despite all 32 products already carrying a `costPrice`. Reported revenue was high for a **second, independent reason** the merchant hadn't asked about: the rollup excluded only `cancelled` and `refunded`, so unconfirmed `pending` orders were counted as earned. The model is now one definition, in `analytics/orderMetrics.js`, used by both the daily rollup and the dashboard summary: **total sales** = `order.total` (what the customer paid) · **revenue** = `total − shippingFee` (= subtotal − discount) · **COGS** = `Σ costPrice × qty` · **profit** = revenue − COGS. It lives in its own module for the same reason `effectivePrice` does — the money bugs in this project have all been drift bugs, and two places computing money separately is how they start.

    **`REVENUE_ORDER_STATUSES = ["confirmed", "packed", "shipped", "delivered"]`** replaces `NON_REVENUE_ORDER_STATUSES`, merchant's rule: *"If admin confirm order from the dashboard, then it should be count as a sales, revenue, and profit. If the product is like reversed or returned or like canceled, then it should be deducted."* Deliberately a **positive** list — a status added later then defaults to not counting, instead of silently counting until someone notices, which is exactly how `pending` came to be counted. All four consumers moved together (rollup, summary, the `transitionOrderStatus` recompute trigger, and per-customer lifetime spend); the shared constant exists precisely so they cannot disagree, and lifetime spend now also stops counting unconfirmed orders. An earlier merchant answer of "only delivered" was reversed by them after seeing that it would leave the Today card at ৳0 until parcels landed.

    **Cost is snapshotted onto the order item, not read live from the Product.** `orderItemSchema` gained `costPrice` beside the existing `price` snapshot, because reading it live would mean editing a product's cost silently rewrites last month's profit — the same reasoning that already makes `price`, `title` and `sku` snapshots. The trap: **`costPrice` is `select: false`**, so both order-creation paths had to add it explicitly (`populate({ path: "items.product", select: "+costPrice" })` for the cart, `.select("+costPrice")` for Buy Now/guest); without that every new order would have silently recorded a null cost and reported unknown profit forever. A null cost is reported as **cost unknown** and surfaced (`unitsMissingCost`, with a banner on Reports) rather than treated as 0, which would claim a 100% margin.

    **Rollup rows are a derivation, so redefining `revenue` needed a recompute, not a migration.** `AnalyticsDaily` gained `totalSales`, `shippingFees`, `cogs`, `unitsSold`, `unitsMissingCost`, and per-product `revenue`/`profit` on `topProducts`; `profit` is a **virtual, deliberately not stored** — it is exactly `revenue - cogs`, and a stored copy is one more thing that can disagree with its own inputs. `scripts/backfill-order-costs.mjs` (idempotent, **`--dry-run` by default** because `MONGODB_URI` is the live Atlas cluster) backfills historical costs and recomputes every day that has orders.

    Dashboard gained a Today/7/30/90 chip row driving **both** the KPI row and the chart (previously the chart had its own control and the KPIs were fixed to "today"), plus **Awaiting confirmation** — pending orders' value and count, deliberately not revenue, so that excluding them doesn't make them invisible — and **Stock at cost**. Top products now rank by profit rather than units, since a cheap bestseller can top the volume chart while earning the least. Summary figures are computed from the orders collection rather than summed from daily rows, so a confirmation made a second ago is reflected immediately instead of waiting for the nightly rollup.

    **Follow-up, same day — the coupon discount must be allocated to lines.** The merchant asked whether a discount is handled correctly and whether a later coupon edit could change an old order. The headline figures were already right (`revenue = total − shippingFee = subtotal − discount`, and both `discount` and `couponCode` are snapshotted on the Order — `calculateDiscount` runs only at checkout, never on read), but the question exposed a real defect in `topProductsByProfit`: it valued each line at `price × qty` and **never allocated the order-level coupon**, so per-product profit summed to exactly the discount MORE than the order made (৳60 and ৳40 on the two live coupon orders) — the Top-products panel silently contradicting the profit headline beside it. The discount is now spread across lines in proportion to line value, summed from the lines rather than read from `order.subtotal` so the shares provably total 1, and rounded once at the end rather than per line. A parametrised test now asserts the invariant directly — per-product profit reconciles to order profit within ±1 taka, including when the discount exceeds a single line — plus one proving nothing is re-derived from the live coupon. Verified against the three real orders: every one reconciles to a difference of 0, and all-time per-product revenue/profit now equal ৳6,670/৳2,799 exactly. Stored rollups were recomputed so saved `topProducts` carry the corrected split.

    Verified with a full status lifecycle against the dev database, not just unit tests: placing an order left revenue untouched and put ৳230 in Awaiting confirmation; confirming booked sales +৳230 / revenue +৳160 / cost +৳70 / profit +৳90 / units +1; packed → shipped → delivered changed nothing; cancelling deducted it exactly back to baseline; restoring returned it; refunding deducted it again. That the cost moved at all on a *new* order is what proves the `+costPrice` selects work. Live figures reconcile by hand: ৳6,870 sales − ৳200 delivery = ৳6,670 revenue, − ৳3,871 cost = ৳2,799 profit at 42% margin. A regression test now imports the **real** `countsAsRevenue` — the previous version kept its own copy of the status list, so it would have passed even if production and the rollup disagreed, the one thing it exists to catch. Backend 158/158 (18 new), frontend 127/127, lint and build clean.

91. **SEO growth phase: baked BODIES join baked heads — a /collections link hub, collection landing-page content, and a guides pipeline — built from market research, not intuition.** Context: with #88–90 live, GSC still showed all 39 catalogue URLs in "Discovered – currently not indexed" (never crawled), and investigation found why that stalls: **zero crawlable `<a href>` existed in any served HTML** (heads were baked, every body was an empty `#root`), so the sitemap was the only signal any URL existed. Three research passes (16 live SERP queries, ~50-keyword map, competitor teardowns) established the market: the only real SEO operator (Torklub, Chattogram) ranks off exactly ONE 1,300-word landing page with a price table + the niche's only FAQPage schema; "mini gt price in bangladesh" has NO dedicated page anywhere (4 of top 5 results are social/manufacturer); TikTok ranks #1 for "hot wheels shop dhaka"; Bengali-script queries return water heaters (English-only confirmed, merchant-approved); and the "noindex" GSC mystery resolved as stale `www` crawls from 2026-07-06 — all four original alert reasons were benign, 8 URLs total. **What shipped:** (a) `injectRoot()` in `lib/seo/injectHead.js` — body HTML string-spliced into `<div id="root">`, deliberately NOT tagged `data-prerendered` (the main.jsx strip would blank it; untagged content persists until React's first paint, and `createRoot` REPLACES children rather than hydrating, so there is no mismatch — verified by the hydration-parity script passing on body-baked routes); (b) `/collections`, a real user-facing route + footer link (unconditional in all three footer variants, because merchant nav settings REPLACE the default link arrays) whose baked body carries every brand/category/product/guide as a plain anchor — 32 product links now in raw HTML, with a verify-prerender assertion that counts them (a hub that silently baked linkless would be the old silent-failure class again); (c) brand/category pages bake H1 + intro + product anchors + merchant content + FAQ text (brand body: ~158 → 326+ words in raw HTML, addressing the no-H1-in-HTML risk flagged by research); (d) Brand/Category models gain sanitized rich `content` + `faqs[]` (sanitizer extracted to `utils/sanitizeContent.js`, shared with pages; admin edits via the existing Tiptap editor + a FAQ repeater in SimpleCatalogManager), rendered on `CollectionPage` below a **live price table** built from the same products query the grid uses — honest by construction — with FAQPage JSON-LD emitted ONLY when real FAQs exist (recorded caveat: Google rarely shows FAQ rich results for e-commerce since 2023; the on-page text is the value); (e) guides pipeline completed with public `GET /pages` (published, minus system slugs), a Guides section on the hub, `og:type article` + `article:modified_time` + visible updated date on CMS pages — tolerant-fetched in the prerenderer so an older backend deploy can't fail a strict build; (f) product descriptions render as real paragraphs (blank-line split — a multi-paragraph description previously collapsed into one `<p>`). **Content:** drafted (never published directly) in `docs/content-drafts/` — 5 collection landing packages, 4 launch guides, 32 product descriptions at 120–180 words (median live description was TWENTY words, thinnest in the niche), every claim traceable to catalogue data, settings, or the store's own existing copy, with ⚠ VERIFY markers where merchant confirmation is needed; merchant approves and pastes via admin per the no-fabricated-content rule. **Reviews deliberately deferred** (no system, no traffic to generate them; schema without real reviews violates policy + house rules). Merchant decisions recorded: content Claude-drafts/merchant-approves; English-only; GBP yes; **catalogue expanding to mainlines + CCA/MSZ/Bburago/Tomica** — new brands need zero code (brand creation auto-yields landing page, sitemap entry, baked head, hub slot, content fields; only a curated `collectionCopy.js` title entry is worth adding per brand), and the expansion makes the broad "hot wheels price in bangladesh" head term directly winnable rather than month-4 deferred. Verified: frontend 134/134, backend 165/165, 48/48 strict prerender, hydration parity green on `/collections` and `/brand/mini-gt`, anchor counts asserted in verify-prerender.

93. **The homepage's product data is baked into the HTML alongside settings — and the hero preload was downloading a 335 KB image nobody used.** Decision #~94's settings bake got the shell and hero painting immediately, but each carousel still made its own round trip after JS booted, so the page appeared as headings plus grey skeletons and the card text landed later — measured at ~830 ms after the header on fast-3G, ~230 ms unthrottled. `scripts/prerender.mjs` now also fetches the three homepage product queries (plus brands and categories, already in hand) and inlines the responses as `__HOME_DATA__`; `lib/bakedData.js` reads it and `useProducts`/`useBrands`/`useCategories` take it as `initialData`. **Result on fast-3G, A/B'd on identical build, server, port and API: the header→card-text gap went 830 ms → 1 ms and the skeleton phase disappeared entirely** (`firstSkeleton — never`), for +73 ms on header/LCP from the 7.3 KB-gzipped payload.

    Three design points carry the weight. The prerender **re-queries the API** for each homepage query rather than filtering the catalogue it already fetched — "featured"/"newArrival"/"hero" and the sort are the server's definitions, and re-implementing them here would drift the first time one changed. The params live in `features/home/homeQueries.js`, imported by **both** HomePage and the prerender, because a literal that drifted from the baked list wouldn't error — it would silently miss and quietly restore the slow path. And every consumer pairs `initialData` with **`initialDataUpdatedAt: 0`**, so the build-time copy paints first and is immediately revalidated (verified: all five queries still fire on load), which is what keeps a deploy-time price or stock level from sticking. Products are trimmed by a **denylist** of prose/SEO/bookkeeping fields, not an allowlist of card fields: ProductCard destructures nine fields but also hands the whole product to WishlistButton, useAddToCart and the pricing helpers, so a missed allowlist entry would break the card only for the first second of a cold load — miserable to find, and worth more than the 3 KB an allowlist would save.

    **The bigger find was a bug in the existing preload.** It emitted the raw Cloudinary URL while `HeroSection`'s `<img>` requests a `srcSet` of `f_auto,q_auto` variants, so the browser downloaded **two different images**: 335 KB of untransformed PNG for the preload, which nothing ever displayed, plus the 19 KB variant the element actually used. On fast-3G that is roughly 1.7 s of bandwidth spent to accelerate a render it was instead starving. The preload now carries `imagesrcset`/`imagesizes` mirroring the element, with the `sizes` string shared from `lib/cloudinary.js` as `HERO_SIZES` so the two cannot drift again. Settings-image bytes fell 449 KB → 114 KB, and **fast-3G LCP went ~10.6 s → ~8.06 s**, independent of the data bake.

    Verified by measurement, not inspection: a controlled A/B (same build, same origin, same proxied API, three runs each) rather than local-vs-live, after an earlier attempt produced a false "cards never load" for the un-baked variant — that was the preview origin missing from the backend CORS allowlist, the same trap that caused the original prerender bug. Frontend 146/146 (12 new, covering key canonicalisation and a malformed payload degrading to a normal fetch rather than breaking boot), lint and build clean.

94. **Sold-out products sink to the bottom of the shop, and a sold-out card shows its photo clearly with one red badge.** Merchant's three asks, all on the shop listing. Sold-out items previously appeared wherever the sort put them, sat under a 55%-opaque wash that made the photo hard to see, and still carried "New" / "Save ৳X" badges advertising something nobody can buy.

    **Ordering is server-side, and it has to be.** `availableStock` is a virtual (`stock − reservedStock`) that exists nowhere on disk, so it can appear in neither `.sort()` nor a plain filter — the same constraint the `inStock` filter already works around with `$expr`. Sorting the loaded page client-side would also be wrong rather than merely incomplete: with infinite scroll, a sold-out item on page 1 would still sit above in-stock items on page 2. Implemented as **two ordered partitions** (`IS_AVAILABLE` / `IS_SOLD_OUT`, extracted so the `inStock` filter and this ordering can never disagree about what "available" means) rather than an aggregation: sorting on the expression needs `$addFields` + `$sort`, which would mean replacing both `.populate()` calls with `$lookup` and re-deriving the lean/virtuals handling on the busiest read path — a lot of risk for an ordering tweak. Nesting the caller's filter under `$and` preserves its own `$expr` instead of clobbering it.

    The page-straddling arithmetic lives in `product.pagination.js` as a pure function, because an off-by-one there doesn't throw — it silently repeats one product across two pages or drops another, exactly the class of bug the `_id` sort tie-breaker exists to prevent and just as invisible. A parametrised test walks every page of ten catalogue shapes and asserts the reconstructed list is complete, duplicate-free and correctly ordered.

    **Scoped to the shop by an opt-in `soldOutLast` param, not made global.** The brand/category landing pages build `ItemList` structured data from product order, so changing their ordering would change their SEO output — and the merchant asked to be consulted before any SEO change. Homepage carousels are likewise untouched (and their baked payload, decision #93, doesn't carry the param).

    On the card: the wash is gone, so the photo is fully visible — a sold-out piece is still the thing a collector is deciding to wait for. When out of stock the badge row renders **only** a red `Sold Out` (`--color-danger`), suppressing Pre-order / New / Save; the centre overlay pill is removed rather than duplicated. Verified live: sold-out items land at indices 30–31 of 32 at every page size (4/7/10/24) with 32 unique products and zero duplicates, the straddling page reads two in-stock then two sold-out, and in the browser the two sold-out cards carry `["SOLD OUT"]` alone while in-stock cards keep `["NEW","SAVE ৳10"]`. Backend 180/180 (15 new), frontend 146/146, lint and build clean.

95. **All eight metropolitan police forces are merged into the address data, because "only Dhaka and Chattogram need it" was wrong.** Merchant, using Steadfast for delivery: *"the sources doesn't have all the thana matched with the district… some of the thana is not found, it's a big problem for fill out the address."* Decision #91 merged the Dhaka and Chattogram metro thanas and asserted every other district's city was covered by a Sadar upazila. Checked properly this time, by scanning all 64 districts for an entry matching their own city name: **Khulna and Rajshahi had neither a Sadar upazila nor any metro thana** — on the shipped data, a customer in the country's 3rd and 4th largest cities could not select where they live at all. The rule is general: a city with its own metropolitan police is not divided into upazilas, so it appears in no upazila dataset. Merging the remaining six forces — Rajshahi (12), Khulna (8), Gazipur (7), Sylhet (6), Rangpur (6), Barisal (4), each from that force's Wikipedia article — adds 42 thanas, 559 → 601. Barisal's four approved-but-unbuilt stations are excluded: the list offers only places that exist. Two Sadar upazilas still carried the pre-2018 district spelling ("Bogra Sadar" under a district displayed as "Bogura", "Jessore Sadar" under "Jashore"), so a customer who picked the district and then searched that same name in the thana box found nothing; both renamed.

    **On mapping to Steadfast specifically:** they publish no public area list and no city/zone/area API — their documented endpoints are consignment status and balance, and their stated coverage is "8 divisions, 495 upazilas, 330 municipal corporations". So there is no table to machine-map against, and inventing one would be exactly the fabrication this project forbids. Parity is instead achieved structurally: Steadfast's coverage is built on the country's administrative geography, so covering every upazila plus every metropolitan thana covers the same ground. If the merchant exports the area list from their Steadfast merchant panel, an exact name-level mapping becomes possible and is worth doing.

    Verified in the running checkout, not just in the data: after picking each district, searching the thana box finds Sonadanga / Khalishpur / Khulna Sadar / Daulatpur (Khulna, 17 thanas), Boalia / Rajpara / Motihar (Rajshahi, 20), Tongi East / Joydebpur (Gazipur, 12), Jalalabad / Kotwali Model (Sylhet, 19), and "bogura" now finds Bogura Sadar. Frontend 155/155 (9 new, including a parametrised guard that each metro city's thanas are present *without* its upazilas being lost), lint and build clean; the dataset stays in the lazy checkout chunk at 7.1 KB gzipped and never reaches the storefront bundle.

96. **The checkout address list IS Steadfast's coverage list, pulled from their API.** Merchant, after #95: *"the sources doesn't have all the thana matched with the district… since we are using Steadfast courier some of the thana is not found"*, then *"why not added area like Bashundhara R/A"*. Both symptoms had one cause. DiecastBD ships via Steadfast, so every customer address is re-typed into Steadfast's panel — and our list was the official *administrative* geography while theirs is a *delivery* geography. They diverge two ways: spelling (we said `Jatrabari`/`Mirpur Model`/`Hazaribagh`, they say `Jattrabari`/`Mirpur`/`Hazaribag`) and granularity (they deliver to `Bashundhara R/A`, `Panthapath`, `Gulistan`, `Purbachal` — zones that appear in no upazila dataset because they are not thanas). No amount of adding official thanas could fix that; the list had to become theirs.

    Steadfast publishes no area list on the web, but their API does: **`GET /police_stations`**, undocumented in the public write-ups and found by reading a community client library, then confirmed against the merchant's own API doc. 727 stations across 65 "districts" → 724 zones across 64 after merging their `Dhaka City` + `Dhaka Sub-Urban` split (a customer picks a district and does not know which side of that line they live on), dropping their `Zone Not Clear` catch-all and collapsing duplicate rows. `frontend/scripts/generate-bd-geo.mjs` regenerates the file from the live endpoint; credentials come from the environment and are never committed.

    **The stored value is the courier's name; our official spellings survive as hidden search aliases** (142 attached), so a customer typing `Jatrabari` or `Uttara East` still lands on `Jattrabari` / `Uttara` and the merchant copies a string Steadfast recognises verbatim. Deriving those aliases is where the real risk sat: **edit distance alone proposed `Gulistan ← Gulshan` (opposite ends of Dhaka), `Lama ← Ruma`, `Amtali ← Taltali`, `Ramganj ← Ramgati` and `Matlab North ← Matlab South`** — aliases that would let someone search their own area and be handed a different one, i.e. misroute a parcel. The rule that kills all of them: *a name Steadfast already lists independently is a place, not a misspelling of another place.* On top of that, qualifier-aware matching pairs `Kotwali - CTG ← Kotwali` and `Bayazid Bostami ← Bayazid`, prefix matching maps their single `Uttara`/`Tongi` onto our East/West pair, and a short hand-reviewed list covers Bengali/English equivalents no string metric can see (`South Surma = Dakshinsurma`, `Motlab dokkhin = Matlab South`). Every one of the 142 pairings was read before shipping, and five of the rejected pairs are pinned by a regression test.

    28 official thanas have no Steadfast equivalent — most of Khulna's and Rangpur's metropolitan thanas. That is deliberately not treated as data loss: Steadfast covers those cities under its own zone names (Khulna city is `Khulna Sadar`, `Circuit House`, `Gollamari`, `Mujgunni`), and offering a place the courier will not collect from would be the worse error. Note this supersedes #95's approach — merging all eight metropolitan police forces was the right fix for an *administrative* list, but the list should not have been administrative.

    **Full parity, merchant's call.** Showing Steadfast's district on the admin page still left the merchant translating, so on request BOTH fields now mirror Steadfast verbatim: 65 districts under their spellings (Chittagong, Bogra, Cumilla) and 724 zones, nothing rewritten. The visible cost was raised before it shipped and accepted: Steadfast splits the capital, so a Dhaka customer picks "Dhaka City" or "Dhaka Sub-Urban" — a courier's internal boundary nobody knows about their own address. Softened in SEARCH rather than by re-labelling, which would have broken parity again: both halves answer to "Dhaka", and each carries its own zone names as hidden keywords, so typing "Savar" surfaces Sub-Urban and "Dhanmondi" surfaces City. Modern district spellings stay searchable the same way. `findDistrict` resolves exact names before aliases, and first-alias-wins, so an order stored as plain "Dhaka" lands on Dhaka City (58 of the 66 zones) rather than the 8-zone half. The admin "Enter in Steadfast" block now renders only when the stored values differ from the courier's — i.e. only for pre-parity orders. Verified live: district search maps chattogram→Chittagong, bogura→Bogra, comilla→Cumilla, savar→Dhaka Sub-Urban, dhaka→both halves; zone search still resolves bashundhara→Bashundhara R/A and jatrabari→Jattrabari. 154/154.

    **Completed the handoff, same day.** The zones matched Steadfast verbatim but the DISTRICT label still didn't: we display modern names (Chattogram, Bogura, Comilla) where Steadfast keeps the pre-2018 ones, 12 of 64 differ, and Steadfast splits the capital into `Dhaka City` / `Dhaka Sub-Urban` — a distinction no customer should be asked to make, but the merchant must, and it is not guessable that Savar, Ashulia, Dhamrai, Dohar, Hemayetpur, Nawabganj and both Keraniganj zones sit on the Sub-Urban side. `steadfastLocation(district, thana)` returns the exact pair to select, and the admin order page renders it as an "Enter in Steadfast" block beside the address, so the merchant copies rather than translates. Null for pre-dropdown orders, and it falls back to the stored thana string when a zone isn't recognised, so an old order still renders something useful. Verified on a live order: `Dhaka / Hatirjheel` displays as `Dhaka City / Hatirjheel`.

    Verified in the running checkout: Dhaka offers 66 zones; `bashundhara` → `Bashundhara R/A`, `panthapath` → `Panthapath`, `gulistan` → `Gulistan`, `purbachal` → `Purbachal`; and alias search resolves `jatrabari` → `Jattrabari`, `mirpur model` → `Mirpur`, `uttara east` → `Uttara`, `hazaribagh` → `Hazaribag`, `sabujbagh` → `Sabujbag`. Frontend 152/152, lint and build clean; the dataset stays in the lazy checkout chunk (9.2 KB gzipped) and never reaches the storefront bundle.

97. **Steadfast parcels are created and tracked from the admin order list.** With checkout addresses already matching Steadfast verbatim (#96), the last manual step was the parcel itself: the merchant re-typed each order into Steadfast's panel and then had no visibility of it. `src/modules/courier/` now wraps their API — `POST /admin/courier/orders/:id` creates the consignment, `POST /admin/courier/sync` refreshes delivery status, `GET /admin/courier/status` reports whether credentials exist (and the account balance, the cheapest read-only proof they work). Credentials are two **optional** env vars, so an unconfigured deploy hides the courier controls instead of offering buttons that can only fail — the `RESEND_API_KEY` pattern.

    **The merchant's first ask was dropped, on evidence.** They wanted a delivery-success number looked up by phone. Steadfast's API has no such endpoint — their documented surface is create/status/balance/returns/payments/police_stations, with no fraud check or customer history. Recorded here so it doesn't look like an oversight: a courier-wide score needs a separate paid service, and an own-order-history version is computable from data we already hold whenever they want it.

    **`cod_amount` is `order.amountDue`, never `order.total`.** This is the line that would cost real money: orders support partial advance payment (#61), and sending the total on a partly-prepaid order makes the rider collect at the door money the customer has already paid. The whole payload builder is a pure module (`steadfast.payload.js`) precisely so this is testable without creating a parcel — 37 tests cover it, including that a partly-prepaid order sends the remainder and a fully-prepaid one sends 0. Phone numbers are normalised to Steadfast's required 11 digits (accepting `+880`/`880` forms) and a number that can't be normalised is a **hard refusal**, because an unroutable parcel cannot be corrected once collected.

    **Creating a parcel deliberately does not change the order status.** Handing a box to a courier is not the same event as the merchant marking an order shipped, and routing it through `transitionOrderStatus` would move stock buckets as a side effect of a courier action. Merchant's call. Sending is idempotent by **refusal** (409 if a consignment already exists), because a duplicate is a second real parcel and a second collection attempt — not something to absorb silently. Cancelled and refunded orders are refused too.

    Steadfast's `delivery_status` is stored **verbatim** rather than mapped onto our order status: they answer different questions ("what have we done" vs "where is the box"), and collapsing them would lose the four `*_approval_pending` states, which are exactly the ones needing action — `CourierChip` styles those amber, never as done, since treating `delivered_approval_pending` as delivered is how money goes uncollected. Sync runs when the orders list opens, skipping finished parcels and anything checked within 5 minutes, with individual failures isolated so one unreachable consignment can't blank the list.

    Verified without dispatching anything: 230 backend tests (50 new), credentials confirmed live through the read-only balance call, and the cancelled-order guard confirmed against a real order. **No test parcel was created** — `create_order` dispatches a real collection, so the one live write waits on the merchant's explicit go-ahead. Browser pass found a bug neither lint nor the build could: the send dialog had been mounted inside the `totalPages > 1` branch, so with a single page of orders it never rendered at all.

98. **Admin can record money that arrived outside checkout, and link a parcel booked directly in Steadfast.** Merchant: *"sometimes order come from facebook or messenger & send some advance money"*, plus wanting to attach a consignment they created in Steadfast's own panel. Two gaps in #97: it assumed every order was paid the way checkout says and that every parcel starts here.

    **`PATCH /admin/orders/:id/payment`** records an advance received and/or a discount. The arithmetic is a pure module (`orders/orderAdjustment.js`) beside `orderMetrics.js` and `steadfast.payload.js`, and it enforces **both** invariants on the way out — `total === subtotal - discount + shippingFee` and `amountPaid + amountDue === total`. `subtotal` and `shippingFee` are never touched: an adjustment describes money, not a re-order. An advance above the total is a **refusal, not a clamp** — silently clamping would leave a mistyped order looking settled — and it is re-checked against the NEW total, so applying a discount and an advance together can't leave `amountDue` negative. `paymentOption` is deliberately left alone: it records what was chosen at checkout, while the amountPaid/amountDue pair already tells the truth about what was collected. Merchant chose *advance received* as the input (the direction they think in — "he sent me 500") with the COD derived, and a **free discount amount** rather than a coupon code, since Messenger deals have no code. Every change writes a human-readable line into the order's own `statusHistory` and recomputes the analytics rollup, because the total moved.

    **`POST /admin/courier/orders/:id/link`** attaches a consignment created in Steadfast's panel, validated against them before saving — a typo would otherwise sit on the order looking authoritative while tracking silently never worked. `sentAt` stays null: we did not send it, and claiming otherwise would misdate it.

    **The dangerous thing this uncovered: Steadfast returns HTTP 401 for an unknown consignment id, with valid credentials.** Verified live — a `get_balance` immediately afterwards still returns 200. The two 401s are distinguishable only by their body: bad credentials answer JSON carrying an `attempts_left` countdown that really does lock the account, while an unknown consignment answers the bare text `Unauthorized Access` with no counter. The original client called `res.json()`, which threw the bare-text body away and reported a useless "HTTP 401". `classifySteadfastError()` now separates them, so a typo reads as "Steadfast does not recognise that consignment" instead of blaming credentials that were never wrong — and, more importantly, **sync aborts the whole batch on a real auth failure** rather than working through every in-flight parcel firing one lockout-counting 401 each. Editing a COD after the parcel is sent is allowed with a prominent warning (merchant's call): Steadfast has no update-order endpoint, so their copy will not change.

    Verified live against a real order: advance ৳500 → collect ৳1,390; then a ৳200 discount → total ৳1,890 → ৳1,760, collect ৳1,260; both invariants held at every step; an advance of ৳99,999 and a bogus consignment were both refused with accurate messages. The order was restored to its exact original figures afterwards. Backend 250/250 (33 new), frontend 154/154, lint and build clean, no overflow at 390px.

---

## 8. Phase Roadmap

| Phase | Scope | Status |
|---|---|---|
| 0 | Architecture, dependencies, folder structure, DB/API design | ✅ Done |
| 1 | Project initialization (frontend/backend scaffolds, Tailwind, Firebase, MongoDB, Git) | ✅ Done |
| 2 | Authentication system end-to-end + self-service profile CRUD | ✅ Done |
| 3 | Database models, seed script, Cloudinary, Product/Brand/Category CRUD (admin) | ✅ Done |
| 4 | Homepage, fully dynamic | ✅ Done |
| 5 | Product listing: filters, pagination, search, sorting | ✅ Done |
| 6 | Product details: gallery, related, wishlist, SEO | ✅ Done |
| 7 | Cart system: persistent, guest, merge, stock validation | ✅ Done |
| 8 | Checkout, orders, emails, confirmation | ✅ Done (COD only; bKash deferred) |
| 9 | Order management: admin, customer, tracking, analytics | ✅ Done |
| 10 | Admin dashboard: complete CRUD, inventory, customers/roles, coupons, media, reports, settings editor | ✅ Done |
| 11 | About, Contact, FAQ, Newsletter (admin-facing pieces) | ✅ Done |
| 12 | Testing, performance, accessibility, SEO polish, deployment, documentation | 🔄 In progress — testing, perf (code-splitting), security hardening, SEO (sitemap/robots), a11y, and docs done; **production deploy** is the only remaining step (needs Vercel/Render accounts — see `docs/DEPLOYMENT.md`) |

Credentials wired in so far: MongoDB Atlas, Firebase (client + Admin SDK), Cloudinary, Resend (sending from `noreply@diecastbd.com`, domain verified — delivers to any recipient). bKash intentionally not requested yet.
