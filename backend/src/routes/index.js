import { Router } from "express";

const router = Router();

// Module routers mount here as each domain is built (Phase 2+), e.g.:
// router.use("/auth", authRoutes);
// router.use("/products", productRoutes);

export default router;
