User-agent: *
Allow: /

# DELIBERATELY NO Disallow RULES. Please don't add them back without reading this.
#
# The private/transactional routes (/cart, /checkout, /account, /orders,
# /wishlist, /login, /register, /forgot-password, /unauthorized,
# /order-confirmation, /admin/*) are kept out of the index with
# `X-Robots-Tag: noindex`, set per-route in frontend/vercel.json, plus a
# rendered <meta name="robots"> from the Seo component.
#
# Disallow and noindex defeat each other: a URL Google is forbidden to CRAWL is
# a URL where it can never SEE the noindex that would remove it. A page that is
# both Disallow-ed and linked from anywhere can still be indexed URL-only
# ("Indexed, though blocked by robots.txt") and then stays there. To get a URL
# OUT of the index you have to let Google fetch it and serve noindex — which is
# what we now do. See docs/plan.md decision #95.
#
# Crawl budget is not a reason to reinstate them: every one of those paths
# returns the same cached app.html shell.

# ---------------------------------------------------------------------------
# Commercial crawlers, blocked on 2026-09-22 for COST, not for SEO.
#
# This site's API is a single Vercel serverless function, and a crawler that
# RENDERS a page fires the same API calls a shopper does. Vercel Fluid Active
# CPU reached its monthly ceiling, and these crawlers send no customers: SEO
# tool suites index the web to sell competitor reports, and the AI scrapers
# train on it. Blocking them is the only lever that reduces rendered page loads
# without touching anything a buyer or a search engine sees.
#
# Deliberately NOT here, and must never be added: Googlebot, Bingbot,
# DuckDuckBot, and the social preview bots (facebookexternalhit, Twitterbot,
# WhatsApp) — they are the traffic. The `User-agent: *` rule above still has no
# Disallow, for the reason written there.
#
# Remove any of these freely if the CPU pressure goes away; nothing depends on
# them being blocked.
User-agent: AhrefsBot
User-agent: SemrushBot
User-agent: MJ12bot
User-agent: DotBot
User-agent: DataForSeoBot
User-agent: BLEXBot
User-agent: PetalBot
User-agent: Bytespider
User-agent: GPTBot
User-agent: ClaudeBot
User-agent: CCBot
User-agent: PerplexityBot
Disallow: /

Sitemap: https://diecastbd.com/sitemap.xml
